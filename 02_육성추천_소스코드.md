# 육성추천 — 소스코드 전체 번들



====================================================================================================
# 파일: growth_recommendation/01_후보자통합.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
01_후보자통합.py — 육성추천(교육·육성 진단 리포트) 1단계: 데이터 계층
=========================================
design_docs/0000_지침의 STEP 1(파일 직접 조회)에 해당한다. 사번 하나를 받아
그 사람의 규칙엔진 입력값(V-01~V-06, design_docs/01_온톨로지 PART 3 참고)을
hr_data 원본 + 평가표 원문에서 조립한다.

이 단계는 "조회만" 한다 — Gap 판정(R-01~R-05, 03_규칙엔진)이나 리포트 문장
생성(04_LLM프롬프트)은 다음 단계(02_규칙엔진.py 등)의 책임이고 여기서는
전혀 다루지 않는다.

[hr_data 연동 현황 — 2026-09 검토 결과]
  - 자격증: design_docs/06_전사_자격증_현황_더미.xlsx와 hr_data/공통05_자격현황이
    컬럼이 완전히 동일해서 그대로 연동한다(변환 없음).
  - 교육이력: design_docs/05_전사_교육이력_현황_더미.xlsx와 hr_data/공통04_교육이력은
    개념은 같지만 컬럼명이 달라 _EDU_COLUMN_MAP으로 이름만 맞춘다. 더미에는 있는데
    hr_data엔 없는 컬럼(교육차수/교육역량(*)/교육평가점수/입력구분)은 빈 값으로
    둔다 — 특히 "교육역량(*)"이 없으면 03_규칙엔진 R-02-5(법정교육 판정)를 정확히
    옮길 수 있는지 다음 단계(규칙엔진 이식)에서 반드시 다시 확인해야 한다.
  - 직무경험(연차): hr_data엔 "직무경험(년차)"를 직접 담은 파일이 없어서, 공통02_
    발령이력에서 "그 사번의 가장 최근(현재) 직무" 구간을 찾아 오늘까지의 연차로
    새로 계산한다(design_docs/01_온톨로지 TRM-0273: "직무 변경 시 1년차부터 리셋"
    원칙 그대로 적용).
  - 평가표: hr_data에 대응하는 파일이 없다 — growth_recommendation/평가표/의 원본
    docx를 사람 이름으로 찾아 그대로 읽는다(진짜 회사 데이터가 생기면 사번 기준
    파일명으로 바뀔 수 있으므로, 사번->이름은 공통01에서 조회해 매칭한다).
"""

import json
import re
import sys
from datetime import date
from pathlib import Path

import pandas as pd

BASE_DIR = Path(__file__).resolve().parent
ROOT_DIR = BASE_DIR.parent
# pipeline_service.py는 talent_recommendation/ 안에 있다 — server.py가 이미
# 그 폴더에서 실행되므로 평소엔 암묵적으로 sys.path에 잡혀 있지만, 이 스크립트를
# 단독 실행할 때(검증용)는 명시적으로 추가해줘야 한다.
for _p in (str(ROOT_DIR), str(ROOT_DIR / "talent_recommendation")):
    if _p not in sys.path:
        sys.path.insert(0, _p)

from pipeline_service import DATA_DIR  # noqa: E402  (talent_recommendation과 동일한 hr_data 참조 방식)

EVAL_DIR = BASE_DIR / "평가표"

SOURCE_FILES = {
    "인사종합": DATA_DIR / "공통01_인사종합_샘플.xls",
    "발령이력": DATA_DIR / "공통02_발령이력_샘플.xlsm",  # hr_data엔 .xlsx도 있는데 내용이 서로 다름 — hr_card/service.py가 쓰는 .xlsm을 그대로 따라간다(같은 사람에 대해 플랫폼마다 다른 발령이력을 보여주면 안 되므로).
    # 자격증→교육 재분류 확정 시 이 두 파일에 실제로 행을 옮겨 쓴다(2026-09-15,
    # 사용자 요청 — 엑셀을 직접 열어봐도 실제로 옮겨져 있어야 함). pandas가 옛
    # .xls 포맷은 쓰기를 지원하지 않아(읽기 전용) .xlsx로 실제 파일 자체를
    # 변환했다 — 원본과 내용이 100% 동일함을 변환 시 직접 대조 확인했고, 이동/
    # 승급/직책자 파이프라인의 파일 읽기 코드는 전부 확장자로 엔진을 자동
    # 선택하는 방식이라(engine="xlrd" if .xls else "openpyxl") 확장자만 바꿔도
    # 별도 코드 수정 없이 그대로 동작한다.
    "교육이력": DATA_DIR / "공통04_교육이력_샘플.xlsx",
    "자격현황": DATA_DIR / "공통05_자격현황_샘플.xlsx",
}

# design_docs/01_온톨로지 표9(Band 코드)의 "수준 번호"(1=사원~8=책임)와, hr_data
# 직급명칭에 실제로 쓰이는 "B1(2)~B5(9)" 괄호 숫자는 값이 다르다 — 괄호 숫자가
# 수준번호보다 정확히 1 크다(오프셋 +1). 이건 추측이 아니라 두 가지 근거로
# 직접 확인했다:
#   1) hr_data/공통01의 직급명칭은 B1(2)~B3(7) 범위만 쓰는데, 이 범위가
#      hr_documents/(DOC-S-030) 부록4. 페르소나 카드.docx 표의 앞 6행(B1(2),
#      B1(3), B2(4), B2(5), B3(6), B3(7))과 정확히 일치한다 — 즉 hr_data와
#      DOC-S-030은 같은 "Bx(y)" 표기를 쓰고 있고, 기존 온톨로지 OBJ-009(Band)의
#      정의("B1(2)...B5(9)")도 이 표와 동일한 8단계·2~9 범위다.
#   2) DOC-S-030의 B1(2) 설명("안정적 수행" — 지시받은 업무를 정확히 수행)은
#      표9의 사원 역할정의와, B3(6) 설명("팀 관점 성장")은 표9의 과장 역할정의
#      ("단일 팀 실무·인력 총괄")와 내용이 맞는다 — 이건 괄호 숫자를 수준번호보다
#      1 크게 볼 때만 성립한다(B1(2)=수준1=사원, B3(6)=수준5=과장).
# 그래서 파싱할 때 괄호 숫자에서 1을 빼서 수준번호로 바꾼다(2026-09-14 발견·수정
# — 처음엔 괄호 숫자를 수준번호로 착각해 모든 사람이 한 단계씩 높게 나오는 버그가
# 있었다: 예를 들어 대리(B2(5))인 사람이 과장으로 잘못 표시됨).
BAND_LEVEL_TABLE = {
    1: ("사원", "입문실무자"),
    2: ("주임", "기초독립실무자"),
    3: ("계장", "실무주도자"),
    4: ("대리", "직무리더"),
    5: ("과장", "팀리더"),
    6: ("차석", "본부직무총괄"),
    7: ("선임", "전사조직총괄"),
    8: ("책임", "전사리더"),
}

_BAND_GRADE_RE = re.compile(r"^(B\d)\((\d+)\)$")

# hr_data(공통04_교육이력) -> design_docs(05_전사_교육이력_현황_더미) 컬럼명 매핑.
# 오른쪽에 없는 더미 컬럼(교육차수/교육역량(*)/교육평가점수/입력구분)은 hr_data에
# 대응 값이 없어 비워둔다.
_EDU_COLUMN_MAP = {
    "No.": "No.",
    "사번": "사번",
    "성명": "성명",
    "조직": "소속",
    "직급": "직급",
    "시행교육\n과정명": "교육과정명",
    "교육유형": "교육구분(*)",
    "교육기간": "교육기간(*)",
    "교육기간.1": "교육기간(*).1",
    "교육기관명": "교육기관명",
    "교육장소": "교육장소",
    "교육일수": "교육일수(*)",
    "교육시간": "교육시간(*)",
    "인당비용\n금액": "인당비용금액",
    "교육점수": "교육점수(*)",
    "이수학점": "이수학점(*)",
    "이수\n여부": "이수여부",
}
_EDU_EXTRA_BLANK_COLUMNS = ["교육차수", "교육역량(*)", "교육평가점수", "입력구분"]

_CACHE = {}


def _read_excel_safe(path, **kwargs):
    if not path.exists():
        raise FileNotFoundError(f"원본 파일 없음: {path}")
    return pd.read_excel(path, **kwargs)


def _read_excel_cached(path):
    """_read_excel_safe에 파일 수정시각(mtime) 기반 캐시를 더한 버전 — 발령이력/
    자격현황/교육이력처럼 사람 한 명 처리할 때마다(그리고 배치로 여러 명 처리할
    때는 그 인원수만큼) 반복 조회되는 파일에 쓴다(2026-09-17, 사용자 지적으로
    확인한 성능 문제 대응 — 실측: 리포트 1명 생성에 같은 파일을 최대 5번,
    그중 교육이력은 2번 다시 읽고 있었고 배치는 인원수만큼 곱해졌다). 그냥 한
    번 읽고 영영 캐시하면 reclassify_cert_to_education()처럼 이 모듈 스스로
    자격현황/교육이력을 고쳐 쓰는 경우 반영이 안 되므로(hr_card.
    _load_master_roster()가 이미 겪은 문제와 동일), 파일이 실제로 바뀌었을
    때만(mtime 변경) 다시 읽는다. kwargs가 필요 없는 호출부만 캐시 대상이다 —
    지금 실제로 캐시가 필요한 호출부(발령이력/자격현황/교육이력/인사종합)는
    전부 인자 없이 부른다."""
    key = str(path)
    mtime = path.stat().st_mtime if path.exists() else None
    cached = _CACHE.get(key)
    if cached is not None and cached[1] == mtime:
        return cached[0]
    df = _read_excel_safe(path)
    _CACHE[key] = (df, mtime)
    return df


def _clean(v):
    if v is None:
        return None
    try:
        if pd.isna(v):
            return None
    except (TypeError, ValueError):
        pass
    if isinstance(v, pd.Timestamp):
        return v.strftime("%Y-%m-%d")
    return v


def _rows_for(df, sabun_col, 사번):
    matched = df[df[sabun_col].astype(str).str.strip() == 사번]
    return [{k: _clean(v) for k, v in row.items()} for row in matched.to_dict(orient="records")]


def _load_roster():
    return _read_excel_cached(SOURCE_FILES["인사종합"])


def parse_band_grade(직급명칭):
    """"B2(5)" 같은 hr_data 직급명칭 문자열을 (Band코드, 레벨명, "역할명(원본 직급코드)")로
    바꾼다. 형식이 안 맞거나 표에 없는 숫자면 (None, None, None) — 임의로 지어내지
    않는다(design_docs 0000_지침 [절대 금지]: 설계문서에 없는 Band 역할명 임의 생성).

    표기 형식(2026-09-14, 사용자 확정): 04_LLM프롬프트 최신 버전은 "역할명(밴드약칭)"
    (예: "직무리더(대리)")을 규정하지만, 실제 리포트를 검토한 사용자가 사내에서
    통용되는 원본 직급코드("직무리더(B2(5))")로 표시해달라고 명시적으로 요청해
    그렇게 바꿨다 — 04번 문서가 말하는 "구버전 코드 표기 잔존" 사고와는 다르다
    (그건 표기 규칙 자체가 없어 실수로 코드가 새 나간 경우이고, 이건 표기 규칙을
    이렇게 쓰기로 사용자가 직접 정한 경우다). 05_문장검증.check_role_grade_format도
    이 형식을 검증하도록 함께 바꿨다."""
    if not 직급명칭:
        return None, None, None
    직급명칭 = str(직급명칭).strip()
    m = _BAND_GRADE_RE.match(직급명칭)
    if not m:
        return None, None, None
    level_num = int(m.group(2)) - 1  # 괄호 숫자 -> 수준번호 오프셋 보정(위 주석 참고)
    entry = BAND_LEVEL_TABLE.get(level_num)
    if not entry:
        return None, None, None
    band_code, level_name = entry
    return band_code, level_name, f"{level_name}({직급명칭})"


def get_basic_info(사번):
    """V-01(식별 정보)·V-02(Band 정보) — 공통01_인사종합에서 조회."""
    df = _load_roster()
    matched = df[df["사번"].astype(str).str.strip() == 사번]
    if matched.empty:
        return None
    row = matched.iloc[0]
    band_code, level_name, role_label = parse_band_grade(row.get("직급명칭"))
    return {
        "사번": 사번,
        "성명": _clean(row.get("성명")),
        "소속팀": _clean(row.get("부서명")),
        # "본부명" 컬럼은 원본 hr_data(공통01_인사종합)에서 전부 빈 값이라(실제
        # 본부 정보는 "상위부서명"에 들어있다 — talent_recommendation의 이동/
        # 승급/직책자 파이프라인도 전부 이 컬럼을 쓴다), 2026-09-15 육성추천
        # 대상자 선택 화면에 본부 필터를 추가하며 실제로 전원 본부가 빈 채로
        # 나오는 걸 발견해 고쳤다.
        "본부": _clean(row.get("상위부서명")),
        "직무명": _clean(row.get("직무명")),
        "직급명칭_원본": _clean(row.get("직급명칭")),
        "Band코드": band_code,
        "역할명": level_name,
        "역할등급_표기": role_label,
        "입사일자": _clean(row.get("입사일자")),
    }


def _years_months(start_date, end_date):
    """두 날짜 사이의 만 나이 방식 경과기간을 (연, 개월)로 계산한다 — 일수를
    365로 나누는 근사(예전 방식, 윤년·월별 일수 차이 때문에 부정확)가 아니라
    달력 기준으로 정확히 센다."""
    years = end_date.year - start_date.year
    months = end_date.month - start_date.month
    if end_date.day < start_date.day:
        months -= 1
    if months < 0:
        years -= 1
        months += 12
    return years, months


def get_job_tenure(사번):
    """V-03(직무 정보) 중 직무연차 — 공통02_발령이력에서 "현재(가장 최근) 직무"
    구간의 시작일부터 오늘까지 경과 연수를 새로 계산한다. hr_card/service.py가
    쓰는 것과 같은 규칙: 직무종료일이 없거나 9999-12-31이면 그 직무를 아직
    수행 중이라는 뜻이다.

    직무연차(정수)는 그대로 두되(02_규칙엔진의 Gap 판정 임계값·페르소나 카드
    조회가 정수 연차를 전제로 하므로 바꾸면 안 됨), 리포트 화면에 보여줄
    "N년 M개월" 표기를 직무경험_표기에 별도로 담는다(2026-09-14, 사용자 지적 —
    "2년차"로만 보이면 2년 11개월도 2년으로 내림되어 실제보다 적어 보임)."""
    df = _read_excel_cached(SOURCE_FILES["발령이력"])
    rows = _rows_for(df, "사번", 사번)
    if not rows:
        return None, []
    for r in rows:
        if r.get("직무종료일") == "9999-12-31":
            r["직무종료일"] = None
    rows.sort(key=lambda r: r.get("직무시작일") or "", reverse=True)
    current = next((r for r in rows if not r.get("직무종료일")), rows[0])
    start = current.get("직무시작일")
    tenure_years, tenure_label = None, None
    if start:
        start_date = pd.to_datetime(start).date()
        years, months = _years_months(start_date, date.today())
        tenure_years = years
        tenure_label = f"{years}년 {months}개월"
    return {
        "직무명": current.get("직무명"),
        "직무시작일": start,
        "직무연차": tenure_years,
        "직무경험_표기": tenure_label,
    }, rows


def get_certifications(사번):
    """V-04(자격 정보) — hr_data/공통05_자격현황을 그대로 쓴다(컬럼 100% 동일 확인됨).
    이 함수는 원본 그대로 반환한다(교육이수 기록 필터링은 build_candidate에서)."""
    df = _read_excel_cached(SOURCE_FILES["자격현황"])
    return _rows_for(df, "사번", 사번)


def get_job_group_peer_certs(job_group, exclude_사번=None, min_sample=2, common_threshold=2, large_sample_size=10):
    """04_LLM프롬프트 §7 — 같은 직무군(job_group) 동료들이 실제로 보유한 자격증을
    참고 사례로 모은다. MGMT/ESG/HQ/ENERGY처럼 자격명이 구체적으로 정해지지 않은
    본부의 자격 Gap을 LLM이 근거 없이 지어내지 않도록 하기 위함(2026-09-16, 원인2
    대응). §7 안전장치 그대로: 동료가 2명 미만이면 샘플 부족으로 빈 결과를 주고
    (근거 없이 소수 사례를 일반화하지 않음), 10명 초과면 2명 이상이 공통으로
    가진 자격만 채택한다(1명만 가진 특이 자격이 노이즈로 섞이지 않도록).
    02_규칙엔진을 이 함수 안에서 지연 임포트하는 이유 — 02_규칙엔진이 모듈
    최상단에서 이미 이 파일(01_후보자통합)을 임포트하므로, 반대 방향을 최상단에
    두면 순환 임포트가 된다."""
    import importlib
    from collections import Counter

    _engine_mod = importlib.import_module("02_규칙엔진")

    roster = _load_roster()
    peers = []
    for _, row in roster.iterrows():
        사번 = str(row.get("사번") or "").strip()
        if not 사번 or 사번 == exclude_사번:
            continue
        직무명 = _clean(row.get("직무명"))
        if not 직무명:
            continue
        소속팀 = _clean(row.get("부서명"))
        if _engine_mod.map_job_to_group(직무명, 소속팀) == job_group:
            peers.append(사번)

    if len(peers) < min_sample:
        return {"peers_found": len(peers), "certs": []}

    df_cert = _read_excel_cached(SOURCE_FILES["자격현황"])
    counter = Counter()
    for peer_사번 in peers:
        seen = set()
        for r in _rows_for(df_cert, "사번", peer_사번):
            자격 = r.get("자격종류")
            if 자격 and 자격 not in seen and not _is_training_completion_cert(r):
                counter[자격] += 1
                seen.add(자격)

    # 직무기술서 교차검증(2026-09-16, 사용자 확정) — 동료가 인사이동 전 다른
    # 직무 때 취득한, 지금 직무군과 무관한 개인 자격이 섞이는 문제 대응. 이
    # job_group에 속한 모든 직무의 직무기술서 자격(필수+우대+권장) 합집합을
    # 허용목록으로 써서, 이 목록에 없는 자격은 동료가 몇 명을 가졌든 제외한다.
    # job_structured_data.json이 이 job_group을 다루지 않아 허용목록이
    # 비어있으면(커버리지 없음) 걸러내지 않고 기존 결과를 그대로 쓴다.
    allowlist = _engine_mod.get_job_group_cert_allowlist(job_group)
    if allowlist:
        counter = Counter({name: cnt for name, cnt in counter.items() if name in allowlist})
    if not counter:
        return {"peers_found": len(peers), "certs": []}

    if len(peers) > large_sample_size:
        items = [(name, cnt) for name, cnt in counter.items() if cnt >= common_threshold]
    else:
        items = list(counter.items())
    items.sort(key=lambda x: -x[1])
    return {"peers_found": len(peers), "certs": [name for name, _cnt in items[:8]]}


# ---------------------------------------------------------------------------
# 자격증현황 오분류 데이터 처리 — 2026-09-14, 사용자가 실제 리포트를 검토하다
# 발견. hr_data/공통05_자격현황에 "안전점검원양성교육이수"처럼 실제로는 자격증이
# 아니라 교육이수 기록인 행이 섞여 있다(등급 컬럼이 "양성교육이수"이거나
# 자격종류명 자체가 그 접미어로 끝남 — 같은 사람 안에서도 등급 값이 있을 때와
# 없을 때가 섞여 있어 이름 접미어 검사를 반드시 같이 봐야 한다).
#
# 원본 hr_data 파일은 이동/승급/직책자·인사카드 등 다른 화면도 같이 읽는 공용
# 자원이다. 처음엔 이 파일들을 직접 고쳐 쓰지 않고 growth_recommendation
# 전용 JSON 대기열(자격_재분류_확정.json)로만 반영했었는데, 그러면 인사카드나
# hr_data 원본 파일 자체는 여전히 잘못된 채로 남아 화면마다 다르게 보이는
# 문제가 있었다(2026-09-15, 사용자 지적) — 담당자가 "확인"을 누르면 실제로
# hr_data 원본에서 자격증현황 행을 지우고 교육이력에 새 행을 추가하는 것으로
# 바꿨다. 이후로는 인사카드·이동/승급/직책자 등 이 두 파일을 읽는 모든 화면이
# 재실행 없이 바로 같은 결과를 본다 — 별도의 확인 여부 저장소가 필요 없어졌다
# (파일 자체가 이미 옮겨졌으므로 "확인됨" 여부를 따로 추적할 필요가 없음).
# ---------------------------------------------------------------------------

_TRAINING_COMPLETION_SUFFIX = "양성교육이수"
_RECLASSIFY_BACKUP_DIR = BASE_DIR / "output" / "자격재분류_백업"


def _is_training_completion_cert(row):
    자격종류 = (row.get("자격종류") or "").strip()
    등급 = (row.get("등급") or "").strip() if isinstance(row.get("등급"), str) else row.get("등급")
    return 자격종류.endswith(_TRAINING_COMPLETION_SUFFIX) or 등급 == _TRAINING_COMPLETION_SUFFIX


def _atomic_write_excel(df, path):
    """쓰다가 중간에 실패해도(디스크 꽉 참, 프로세스 강제종료 등) 원본 파일이
    반쯤 쓰인 채로 깨지지 않도록, 임시 파일에 먼저 쓰고 나서 교체한다. 매
    변경 전 원본을 타임스탬프 백업 폴더에 복사해둬 문제가 생기면 되돌릴 수
    있게 한다(hr_card/service.py가 명시한 "동시접근·백업 안전장치 없이는
    엑셀 되쓰기를 하지 않는다"는 기존 원칙을 이 한 가지 용도에 한해 충족)."""
    import shutil
    import tempfile

    path = Path(path)
    if path.exists():
        _RECLASSIFY_BACKUP_DIR.mkdir(parents=True, exist_ok=True)
        stamp = date.today().isoformat()
        backup_path = _RECLASSIFY_BACKUP_DIR / f"{stamp}_{path.name}"
        if not backup_path.exists():  # 같은 날 여러 번 확인해도 그날의 첫 원본만 보존
            shutil.copy2(path, backup_path)

    fd, tmp_name = tempfile.mkstemp(suffix=".xlsx", dir=str(path.parent))
    import os
    os.close(fd)
    try:
        df.to_excel(tmp_name, index=False, engine="openpyxl")
        os.replace(tmp_name, path)
    except Exception:
        os.unlink(tmp_name)
        raise


def _cert_row_to_raw_education_row(cert_row, edu_df):
    """자격증현황(원본 스키마) 행 하나를 교육이력 원본 파일의 실제 컬럼명으로
    옮겨 담는다 — get_education_history가 쓰는 _EDU_COLUMN_MAP은 "표시용"
    이름이라 원본 파일에 실제로 쓸 때는 원본 컬럼명(예: "시행교육\n과정명",
    "이수\n여부")을 그대로 써야 한다."""
    next_no = int(edu_df["No."].max()) + 1 if len(edu_df) and edu_df["No."].notna().any() else 1
    row = {col: None for col in edu_df.columns}
    row.update({
        "No.": next_no, "사번": cert_row.get("사번"), "성명": cert_row.get("성명"),
        "조직": cert_row.get("소속"), "직급": cert_row.get("직급"),
        "시행교육\n과정명": cert_row.get("자격종류"), "교육유형": "사내(자격증현황 재분류)",
        "교육기간": cert_row.get("취득일자"), "교육기간.1": cert_row.get("취득일자"),
        "이수\n여부": "Y",
        "비고": f"자격증현황 재분류 확정({date.today().isoformat()})",
    })
    return row


def reclassify_cert_to_education(사번, 자격종류):
    """담당자가 알림창에서 "확인"을 누르면 호출 — hr_data 원본에서 이 자격증
    행을 실제로 지우고 교육이력에 새 행을 더한다(2026-09-15). 이미 옮겨졌으면
    (중복 확인 클릭 등) 아무 일도 하지 않는다 — 멱등."""
    cert_path = SOURCE_FILES["자격현황"]
    edu_path = SOURCE_FILES["교육이력"]
    df_cert = _read_excel_cached(cert_path)
    mask = (df_cert["사번"].astype(str).str.strip() == 사번) & (df_cert["자격종류"] == 자격종류)
    if not mask.any():
        return False

    cert_row = df_cert[mask].iloc[0]
    df_edu = _read_excel_cached(edu_path)
    new_row = _cert_row_to_raw_education_row(cert_row, df_edu)
    df_edu = pd.concat([df_edu, pd.DataFrame([new_row])], ignore_index=True)
    df_cert = df_cert[~mask].reset_index(drop=True)

    _atomic_write_excel(df_cert, cert_path)
    _atomic_write_excel(df_edu, edu_path)
    return True


def get_education_history(사번):
    """V-05(교육이력) — hr_data/공통04_교육이력을 design_docs 스키마 컬럼명으로
    맞춰서 반환한다. _EDU_EXTRA_BLANK_COLUMNS는 hr_data에 대응 값이 없어 빈 값."""
    df = _read_excel_cached(SOURCE_FILES["교육이력"])
    rows = _rows_for(df, "사번", 사번)
    remapped = []
    for r in rows:
        new_row = {_EDU_COLUMN_MAP.get(k, k): v for k, v in r.items()}
        for col in _EDU_EXTRA_BLANK_COLUMNS:
            new_row.setdefault(col, None)
        remapped.append(new_row)
    return remapped


def get_missing_team_legal_trainings(사번, 소속팀, min_sample=2):
    """08_리포트생성지시서 PART 5 체크리스트 ⑩("본부별 법정교육 대상 기준
    파일 — 미입력 시 담당자 확인 처리")의 대안(2026-09-16, 사용자 확정) — 그
    기준 파일 자체가 없어서, "같은 소속팀 동료 2명 이상이 이수한 법정교육을
    이 사람만 이수 기록이 없는" 경우로 간접 추정한다.

    실제 데이터로 검증한 근거: 법정교육 중 "청렴·반부패 교육"류는 전 직원이
    부서 무관하게 이수하고, "긴급자동차 교통안전교육"류는 현장·안전 계열
    소속팀에만 몰려 있다 — 같은 소속팀 동료 기준으로 비교하면 이 둘이 자동
    구분된다. 다만 "산업안전보건법_신규채용교육"처럼 이수자가 1명뿐인 과정도
    있어(아마 신규채용 시 1회성) min_sample(기본 2명) 미만이면 비교 기준에서
    제외한다 — 그렇지 않으면 예전에 입사한 사람 전원이 잘못 미이수로 잡힌다.

    추정치일 뿐 확정 판정이 아니므로, 호출부(02_규칙엔진.py)는 이 결과를
    Gap으로 만들지 않고 S-07 담당자 확인 안내로만 사용해야 한다."""
    df_edu = _read_excel_cached(SOURCE_FILES["교육이력"])
    roster = _load_roster()
    same_team = roster[roster["부서명"].astype(str).str.strip() == str(소속팀).strip()]
    peer_사번_set = {
        str(s).strip() for s in same_team["사번"]
        if str(s).strip() and str(s).strip() != 사번
    }
    if not peer_사번_set:
        return []

    from collections import Counter
    course_counter = Counter()
    for peer_사번 in peer_사번_set:
        seen = set()
        for r in _rows_for(df_edu, "사번", peer_사번):
            course = r.get("시행교육\n과정명")
            if course and r.get("교육유형") == "법정교육" and r.get("이수\n여부") == "Y" and course not in seen:
                course_counter[course] += 1
                seen.add(course)
    common_courses = {c for c, cnt in course_counter.items() if cnt >= min_sample}
    if not common_courses:
        return []

    my_courses = {
        r.get("시행교육\n과정명") for r in _rows_for(df_edu, "사번", 사번)
        if r.get("교육유형") == "법정교육" and r.get("이수\n여부") == "Y"
    }
    return sorted(common_courses - my_courses)


# 평가표 원문 파싱 — 최상위 표제(콜론 없음)와 하위 라벨(콜론으로 끝남)만으로
# 문단을 나눈다. 실제 파일 10건을 직접 열어 이 형식(제목 → "업적" → "강점: "/
# "보완사항: " → "역할 및 핵심가치" → "역할: "/"핵심가치: ")이 전부 동일함을
# 확인했다(2026-09-14).
_EVAL_HEADINGS = {"업적", "역할 및 핵심가치"}
_EVAL_LABELS = {"강점:", "보완사항:", "역할:", "핵심가치:"}


def parse_evaluation_docx(path):
    import docx

    d = docx.Document(path)
    paragraphs = [p.text for p in d.paragraphs]
    title = paragraphs[0].strip() if paragraphs else ""

    sections = {"업적_서술": []}
    current_key = "업적_서술"
    for text in paragraphs[1:]:
        stripped = text.strip()
        if stripped in _EVAL_HEADINGS:
            continue  # 상위 표제는 구획만 나누고 내용은 없음
        if stripped in _EVAL_LABELS:
            current_key = stripped.rstrip(":")
            sections.setdefault(current_key, [])
            continue
        sections.setdefault(current_key, []).append(text)

    parsed = {k: "\n".join(v).strip() for k, v in sections.items()}
    parsed["제목"] = title
    parsed["원문_전체"] = "\n".join(paragraphs)
    return parsed


def find_evaluation_file(성명, year=2025):
    candidate = EVAL_DIR / f"{성명} 평가표({year}).docx"
    if candidate.exists():
        return candidate
    matches = list(EVAL_DIR.glob(f"{성명}*평가표*{year}*.docx"))
    return matches[0] if matches else None


def get_evaluation(사번, 성명, year=2025):
    path = find_evaluation_file(성명, year)
    if path is None:
        return None
    return parse_evaluation_docx(path)


def build_candidate(사번, year=2025):
    """한 사람의 규칙엔진 입력값(V-01~V-06)을 전부 조립한다."""
    basic = get_basic_info(사번)
    if basic is None:
        raise ValueError(f"공통01_인사종합에 없는 사번입니다: {사번}")
    job, job_history = get_job_tenure(사번)
    evaluation = get_evaluation(사번, basic["성명"], year=year)

    # 확정된 재분류 건은 reclassify_cert_to_education()이 hr_data 원본에서 이미
    # 자격증현황 -> 교육이력으로 실제로 옮겨놨으므로, 여기 raw_certs에는 아직
    # 담당자 확인 전인 건만 남는다(별도 "확인됨" 목록을 추적할 필요가 없어짐).
    raw_certs = get_certifications(사번)
    real_certs, pending_reclassify = [], []
    education = get_education_history(사번)
    for c in raw_certs:
        if not _is_training_completion_cert(c):
            real_certs.append(c)
        else:
            pending_reclassify.append(c)

    return {
        "기본정보": basic,
        "직무정보": job,
        "발령이력": job_history,
        "자격증": real_certs,
        "교육이력": education,
        "자격_재분류대기": pending_reclassify,
        "평가표": evaluation,
        "평가표_찾음": evaluation is not None,
    }


# 000__채팅창_명령어.txt에 나온 10명(더미 검증용) — 이 스크립트 자체 검증에 쓴다.
_PILOT_사번 = [
    "H43328", "H15172", "H86138", "H14965", "H90674",
    "H44543", "H47973", "H34268", "H98500", "H77957",
]


def main():
    ok, fail = 0, []
    for 사번 in _PILOT_사번:
        try:
            c = build_candidate(사번)
        except Exception as e:
            fail.append((사번, str(e)))
            continue
        b = c["기본정보"]
        j = c["직무정보"]
        print(
            f"{사번} {b['성명']:4s} | {b['소속팀']:8s} | {b['역할등급_표기']:10s} | "
            f"직무:{j['직무명']:8s}({j['직무연차']}년차) | 자격증 {len(c['자격증'])}건 | "
            f"교육이력 {len(c['교육이력'])}건 | 평가표 {'O' if c['평가표_찾음'] else 'X'}"
        )
        if not c["평가표_찾음"]:
            fail.append((사번, "평가표 파일을 찾지 못함"))
        else:
            ok += 1
    print(f"\n총 {len(_PILOT_사번)}명 중 정상 조립 {ok}명, 실패 {len(fail)}건")
    for 사번, reason in fail:
        print(f"  실패: {사번} - {reason}")


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/02_규칙엔진.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
02_규칙엔진.py — 육성추천 Phase 2: 규칙엔진 이식(결정론적 부분만)
=========================================
design_docs/03_규칙엔진.docx의 STEP-1(입력검증)~STEP-4(우선순위결정) 의사코드를
그대로 파이썬으로 옮긴다. 03_규칙엔진 자신이 "고정 룩업 테이블이 아니라 종합
추론 함수"라고 명시적으로 못박은 판정(자격/교육이 실제 직무에 쓰이는지 평가표와
대조, 교육과정명이 기초/심화/전문 중 어디인지, 페르소나 축이 확인되는지 등)은
이 단계에서 구현하지 않는다 — 사내 AI 게이트웨이로 판단할 Phase 3의 몫이다.
아래에서 이런 함수는 전부 `_PENDING`을 반환하고, V.notices에 "왜 이 판정을
지금 못 내리는지"를 남긴다(임의로 True/False를 지어내지 않는다).

[Phase 2에서 실제로 결정론적으로 구현한 것]
  - validate_input: V-01~V-13 검증/기본값/치환 (완전 이식)
  - run_R01: GAS(가스솔루션본부)의 자격 계층 판정(meets_or_exceeds_gas_cert)만
    결정론적 — MGMT/ESG/HQ/ENERGY의 자격 분류(is_job_linked_cert 등)는 PENDING
  - run_R02: MAP_JOB_TO_GROUP(02_RAG 직무명→직무군 매핑표, 완전 이식) · R-02-5
    법정교육 판정(hr_data 교육유형 필드가 마침 EDU_LEGAL/사내/사외 3분류와 동일해
    결정론적으로 그대로 씀) — 교육과정명이 L1/L2/L3 중 어디인지(is_job_edu_L1
    등), R-02-4 리더십교육 판정(hr_data에 "역량유형" 필드 자체가 없어 근거 없이
    지어낼 수 없음)은 PENDING
  - run_R03: AX Gap — 01_온톨로지 F-03-B 키워드표 + 03_규칙엔진의 AI_USAGE_KEYWORDS가
    전부 "교육과정명/평가표 텍스트에 이 키워드가 있는가" 수준의 결정론적 매칭이라
    완전히 이식 가능했다(예외적으로 3-5 R-05와 달리 이 항목만은 원래 설계부터
    키워드 매칭 방식임)
  - run_R04: 산출물 Gap — 03_규칙엔진 3-4 하단에 "V.제출산출물목록은 현재 가변층
    입력 명세에 없어 초기 운영 단계에서는 SKIP한다"고 이미 문서에 명시돼 있어
    그 지침대로 실행하지 않는다(이 스크립트가 처음 판단해서 뺀 게 아니다)
  - run_R05: GET_PERSONA(hr_documents/(DOC-S-030) 부록4. 페르소나 카드.docx의
    3개 표 — 교육/현업적용/근거 페르소나 — 완전 이식)만 결정론적 — 이 페르소나
    축이 교육이력/평가표에서 확인되는지(is_axis_confirmed_by_*)는 PENDING
  - calc_score/sort_by_score(동점 처리): 완전 이식

[Band 코드 괄호 숫자 관련 — 01_후보자통합.py에서 발견/수정한 버그]
  hr_data 직급명�칭("B2(5)" 등)의 괄호 숫자는 01_온톨로지 표9의 "수준 번호"보다
  1 크다(오프셋 보정 필요) — 자세한 근거는 01_후보자통합.py의 BAND_LEVEL_TABLE
  주석 참고. 이 스크립트는 01_후보자통합.parse_band_grade()가 이미 보정한
  "Band코드"(사원~책임 문자열)를 받아서 쓰므로 이 파일 자체는 그 버그와 무관하다.
"""

import json
import re
import sys
from datetime import date
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
ROOT_DIR = BASE_DIR.parent
for _p in (str(ROOT_DIR), str(ROOT_DIR / "talent_recommendation")):
    if _p not in sys.path:
        sys.path.insert(0, _p)
if str(BASE_DIR) not in sys.path:
    sys.path.insert(0, str(BASE_DIR))

import importlib
_candidate_mod = importlib.import_module("01_후보자통합")  # 파일명이 숫자로 시작해 일반 import 불가

_PENDING = "PENDING_LLM_JUDGMENT"  # Phase 3(사내 AI 게이트웨이)로 넘길 판정의 자리표시자

# ---------------------------------------------------------------------------
# 고정층(Fixed Layer) 참조 데이터 — 전부 design_docs/01_온톨로지.docx,
# design_docs/02_RAG.docx, hr_documents/(DOC-S-030) 부록4. 페르소나 카드.docx의
# 표를 그대로 옮긴 값이다. 임의로 만든 값이 하나도 없다.
# ---------------------------------------------------------------------------

BAND_ENUM = ("사원", "주임", "계장", "대리", "과장", "차석", "선임", "책임")
BAND_LEVEL = {code: i + 1 for i, code in enumerate(BAND_ENUM)}  # 사원=1 ~ 책임=8

# 01_온톨로지 표13 — 소속팀 -> 본부코드 매핑 규칙(V-01d)
MAP_TEAM_TO_DEPT = {
    "GAS": ["공급솔루션", "공무솔루션", "기술혁신", "남부패트롤", "동부패트롤", "서부패트롤", "북부패트롤", "디지털배관TF"],
    "ENERGY": ["사업전략", "에너지공급", "에너지사업개발", "에너지솔루션", "에너지혁신지원", "에너지혁신지원TF", "에너지 O&MTF"],
    "MGMT": ["경영기획", "재무전략", "IT솔루션"],
    "ESG": ["ESG전략", "고객가치혁신", "컨텐츠전략TF", "컨텐츠기획TF"],
    "HQ": ["인사/조직문화", "경영관리", "AI솔루션"],
}
_TEAM_TO_DEPT = {team: dept for dept, teams in MAP_TEAM_TO_DEPT.items() for team in teams}

CERT_TYPE_DEPTS = {"GAS"}  # 자격형 본부(그 외는 전부 교육형) — 03_규칙엔진 4-1-1·4-3

# 02_RAG 표12 — 직무명 -> 직무군(job_group) 코드 매핑표(v2.4)
_JOB_GROUP_MAP = {
    "safety": ["공급시설안전관리", "사용시설관리", "상황관리", "안전기획", "산업안전보건환경", "안전운영",
               "정밀안전진단", "전기방식", "정압기", "원격감시시설", "시공관리", "설계", "검측관리",
               "배관탐사·측량", "공급시설관리", "배관망분석", "CNG충전소", "GIS", "인허가"],
    "new_biz": ["권역내외사업개발", "권역내외사업기획", "권역내외사업운영", "권역내사업개발", "권역내사업기획",
                "권역내사업운영", "사업개발_LPG", "사업운영_LPG", "사업개발_태양광", "권역내외사업개발_LPG",
                "권역내외사업개발_태양광", "권역내외사업운영_LPG"],
    "ai_it": ["AI기획", "AI플랫폼", "데이터분석", "IT기획", "정보보안", "개인정보보호", "AI", "DT솔루션"],
    "planning": ["전략기획", "정책기획", "사업포트폴리오관리", "컴플라이언스"],
    "finance": ["재무기획", "세무관리", "자금관리", "회계관리"],
    "hr": ["인사기획", "인재육성", "인사운영", "급여보상", "노무"],
    "mgmt": ["계약", "구매", "자산관리", "자산운영", "복리후생", "사업장안전및보안", "경영관리", "운영지원"],
    "esg": ["ESG경영", "홍보마케팅"],
    "customer": ["요금솔루션", "계량자원관리", "채권관리", "고객지원"],
}
_JOB_TO_GROUP = {job: grp for grp, jobs in _JOB_GROUP_MAP.items() for job in jobs}
# 0단계(별칭 정규화) — 표기만 다른 같은 직무
_JOB_ALIAS = {"권역내 사업개발": "권역내사업개발", "배관탐사/측량": "배관탐사·측량", "배관망분석(AR배관망)": "배관망분석"}
JOB_GROUP_LABEL = {  # F-04-C 표21의 "직무군" 행 이름(뒤의 "직무" 접미어는 뺌) — Gap content 문구에 씀
    "safety": "안전", "new_biz": "신사업", "ai_it": "AI·IT", "planning": "경영기획",
    "finance": "재무", "hr": "인사", "mgmt": "경영관리", "esg": "ESG", "customer": "고객가치혁신",
}

# talent_recommendation/job_structured_data.json — 인재추천 모듈이 이미 관리하는
# 실제 64개 직무의 정식 직무기술서(직무코드별 필수/우대/권장 자격증 목록 포함).
# 04_LLM프롬프트 §7이 요구하는 "MGMT/ESG/HQ/ENERGY처럼 자격명이 특정되지 않은
# 본부의 자격 Gap을 사내 사례로 구체화" 근거로 이 파일을 쓴다(2026-09-16, 원인2
# 대응) — 직무기술서 데이터가 없어서 사용자가 "인정 자격 목록"을 새로 만들어야
# 한다고 판단했었는데, 이미 이 파일에 정리돼 있었다. 직무명은 이 파일 안에서
# 61개 전부 고유해서(중복 확인 완료) 직무명 기준 조회가 안전하다.
_JOB_DESC_PATH = ROOT_DIR / "talent_recommendation" / "job_structured_data.json"
_JOB_DESC_CACHE = None


def _load_job_descriptions():
    global _JOB_DESC_CACHE
    if _JOB_DESC_CACHE is None:
        try:
            with open(_JOB_DESC_PATH, encoding="utf-8") as f:
                raw = json.load(f)
            _JOB_DESC_CACHE = {j["직무명"]: j for j in raw.get("직무기술서", {}).values() if j.get("직무명")}
        except (FileNotFoundError, json.JSONDecodeError, KeyError):
            _JOB_DESC_CACHE = {}  # 파일이 없거나 형식이 바뀌어도 조용히 "참고자료 없음"으로 진행 — 리포트 생성 자체를 막지 않는다
    return _JOB_DESC_CACHE


def get_job_cert_reference(직무명):
    """직무기술서의 자격증 필수/우대/권장 목록을 조회한다. 직무명이 파일에 없으면
    None(호출부가 "참고자료 없음"으로 처리 — 임의로 지어내지 않음)."""
    job = _load_job_descriptions().get(직무명)
    if not job:
        return None
    certs = job.get("자격증") or {}
    필수, 우대, 권장 = certs.get("필수") or [], certs.get("우대") or [], certs.get("권장") or []
    if not (필수 or 우대 or 권장):
        return None
    return {"필수": 필수, "우대": 우대, "권장": 권장}


def get_job_group_cert_allowlist(job_group):
    """job_group에 속한 모든 직무의 직무기술서 자격(필수+우대+권장)을 합친
    집합 — 01_후보자통합.get_job_group_peer_certs가 동료 사례를 거를 때 쓰는
    "이 직무군에서 인정되는 자격" 허용목록이다(2026-09-16, 사용자 확정).

    배경: 동료가 인사이동 전 다른 직무를 할 때 취득한, 지금 job_group과
    무관한 개인 자격까지 "동료 보유 사례"로 그대로 추천되는 문제가 실제로
    있었다(박시은 사례 — 전략기획 동료 목록에 정보통신산업기사·전기기능사가
    섞여 나옴). 이 허용목록에 없는 자격은 동료가 몇 명을 가졌든 제외한다 —
    LLM이 관련성을 재판단하는 게 아니라 기계적 교차검증이라 04_LLM프롬프트
    §7-1④(재판단 금지) 원칙과 부딪히지 않는다."""
    jobs_in_group = _JOB_GROUP_MAP.get(job_group, [])
    allowlist = set()
    for job in jobs_in_group:
        job = _JOB_ALIAS.get(job, job)
        ref = get_job_cert_reference(job)
        if ref:
            allowlist.update(ref["필수"])
            allowlist.update(ref["우대"])
            allowlist.update(ref["권장"])
    return allowlist

# 01_온톨로지 표18 — F-03-B AX 역량 수준별 분류 키워드(교육과정명 포함 여부로 판단)
AX_KEYWORDS = {
    "L1": ["ChatGPT", "생성AI", "AI활용", "AI로", "LLM", "Copilot", "Excel활용", "RPA", "자동화", "VBA", "Python", "SQL"],
    "L2": ["DX", "디지털전환", "데이터분석", "빅데이터", "통계데이터", "서비스기획", "업무혁신"],
    "L3": ["데이터 기반 의사결정", "AI 전략", "경영AI", "Product Management", "비즈니스 AI"],
}
# 03_규칙엔진 R-03(detect_L2_informal) 원문의 리스트 그대로
AI_USAGE_KEYWORDS = ["LLM", "LLM 기반", "AI를 활용", "AI 활용", "생성형 AI를 업무", "AI 도구를 활용", "AI 프롬프트"]
AX_COMPLAINT_KEYWORDS = ["AI", "데이터", "디지털", "자동화", "AX"]  # 03_규칙엔진 R-03 fix_kw

# hr_documents/(DOC-S-030) 부록4. 페르소나 카드 — 수준번호(1~8) x 연차(1~4) ->
# {학습방향(표0 "교육 페르소나"), 현업행동(표1 "현업 적용 페르소나"), 기대산출물(표2 "근거 페르소나")}
# 원문 순서 그대로(사원=1행 ~ 책임=8행, 연차=1~4열) 옮겼다 — B3(7)(차석) 행은 원본
# 문서 자체에 연차3·연차4 칸이 완전히 같은 문장으로 중복 기재돼 있어 그대로 옮겼다
# (임의로 다르게 지어내지 않음).
PERSONA_CARDS = {
    1: {  # 사원 / B1(2)
        1: ("규정/기준: 법·규정/내부기준 입문 / 커뮤니케이션·협업: 사실 기반 기록/보고",
            "기준 조항을 확인한 뒤 업무에 적용한다.",
            "업무처리 근거서(적용 기준 조항 포함)"),
        2: ("커뮤니케이션·협업: 업무요청 확인/보고 / 문제해결: 자기점검(실수 유형)",
            "업무 처리 전후로 누락·오류를 스스로 점검한다.",
            "업무처리 근거서(전/후 점검 흔적 포함)"),
        3: ("규정/기준: 기준 적용 케이스 / 문제해결: 사례 기반 원인-조치",
            "반복되는 실수 유형을 정리하고 다음 업무에 반영한다.",
            "이슈·개선 보고서(원인/조치/재발방지)"),
        4: ("커뮤니케이션·협업: 문서화 기본 / 문제해결: 프로세스 흐름 이해",
            "업무 흐름을 정리하고 확인 포인트를 설정한다.",
            "표준 산출물(업무흐름+체크포인트)"),
    },
    2: {  # 주임 / B1(3)
        1: ("커뮤니케이션·협업: 결과 설명/보고 구조 / 문제해결: 원인 언급 기초",
            "결과물의 사용처와 영향을 설명한다.",
            "이슈·개선 보고서(사용처/영향 포함)"),
        2: ("문제해결: 데이터 기초(베이스라인/지표) / 커뮤니케이션·협업: 쟁점 정리",
            "이슈 발생 시 결과뿐 아니라 원인을 함께 보고한다.",
            "검토서(지표 정의 포함)"),
        3: ("규정/기준: 리스크 정의(유형/신호) / 문제해결: 원인→대응 연결",
            "개선 과제에 대해 확인 지표를 정의하고 전후를 비교한다.",
            "이슈·개선 보고서(원인·리스크·대응 포함)"),
        4: ("문제해결: 개선 실행(재발방지) / 표준화·확산: 템플릿 초안",
            "동일 유형 이슈에 재발방지 조치를 적용한다.",
            "표준 산출물(템플릿/표준 양식)"),
    },
    3: {  # 계장 / B2(4)
        1: ("문제해결: 우선순위 판단(근거) / 커뮤니케이션·협업: 설명/보고",
            "여러 업무의 우선순위를 근거와 함께 정리한다.",
            "검토서(우선순위 근거 포함)"),
        2: ("문제해결: 대안 도출·비교 / 데이터/측정: 간단 효과 확인",
            "문제상황에서 대안을 비교하고 선택 근거를 남긴다.",
            "대안비교서(선택 근거 포함)"),
        3: ("문제해결: 이슈 관리(선택 근거) / 커뮤니케이션·협업: 협업 주도(결정/액션)",
            "협업 과제에서 결정·할 일·담당·기한을 정리해 실행을 추적한다.",
            "결정·액션 기록(회의록)"),
        4: ("문제해결: 팀 흐름 관점 / 표준화·확산: 표준 초안",
            "반복 업무를 표준 목차와 필수 항목으로 정리한다.",
            "표준 산출물(반복업무 표준 초안)"),
    },
    4: {  # 대리 / B2(5)
        1: ("문제해결: 경험 구조화(반복 패턴) / 커뮤니케이션·협업: 설명 정리",
            "유사 업무의 반복 패턴과 주의점을 구조화한다.",
            "업무처리 근거서(패턴/주의점 포함)"),
        2: ("문제해결: 리스크 인식·대응(조기 신호/기준) / 커뮤니케이션·협업: 조율(합의/기록)",
            "업무 중 이상 징후(주의 포인트)와 대응 방법을 정리한다.",
            "표준 산출물(대응 기준 초안)"),
        3: ("커뮤니케이션·협업: 관계 조율/대안제시 / 문제해결: 개선 방향 정리(점검→개선)",
            "충돌 상황에서 대안을 제시하고 조정한다.",
            "검토서(조정안/대안 포함)"),
        4: ("표준화·확산: 체크포인트/운영 기준 초안 / 커뮤니케이션·협업: 팀 공유/피드백",
            "체크포인트를 정리해 팀에 공유하고 피드백을 반영한다.",
            "표준 산출물(체크포인트/운영 기준)"),
    },
    5: {  # 과장 / B3(6)
        1: ("표준화·확산: 노하우 공유 구조 / 커뮤니케이션·협업: 공유 스토리",
            "내 경험을 재사용 가능한 팁으로 공유한다.",
            "확산 산출물(공유자료/게시글)"),
        2: ("리더십: 코칭/피드백(질문·리뷰) / 커뮤니케이션·협업: 피드백 대화",
            "팀원 산출물을 질문 중심으로 리뷰한다.",
            "확산 산출물(리뷰/피드백 기록)"),
        3: ("문제해결: 팀 성과 요인/병목현상 관점 / 리더십: 개선 과제 리딩(팀 단위)",
            "팀의 반복 이슈를 선정해 개선 포인트를 제안한다.",
            "이슈·개선 보고서(팀 관점 개선 제안)"),
        4: ("표준화·확산: 팀 가이드/템플릿 정착 / 확산: 사용 피드백 반영",
            "팀 가이드/템플릿을 배포하고 사용 피드백을 반영한다.",
            "표준 산출물(팀 가이드/템플릿)"),
    },
    6: {  # 차석 / B3(7) — 원문 자체가 연차3·4 칸을 동일 문장으로 중복 기재
        1: ("문제해결: 업무·인력·자원 배분 기초 / 커뮤니케이션·협업: 조직 관점 공유",
            "업무 수행에서 자원·기간·리스크를 함께 고려해 정리한다.",
            "검토서(자원·기간·리스크 포함)"),
        2: ("문제해결: 본부 이슈 검토(쟁점·리스크) / 데이터/측정: 옵션 비교(근거)",
            "본부 이슈에 대해 쟁점과 옵션을 정리한다.",
            "대안비교서(쟁점/옵션 포함)"),
        3: ("리더십: 조정/합의(조직 관점) / 문제해결: 충돌 조정(시나리오)",
            "자원/일정 충돌을 조정할 시나리오를 마련한다.",
            "이슈·개선 보고서(본부 관점 개선 제안)"),
        4: ("리더십: 조정/합의(조직 관점) / 문제해결: 충돌 조정(시나리오)",
            "본부 운영 기준을 정리하고 적용 범위·예외를 명확히 한다.",
            "표준 산출물(본부 운영 기준 초안)"),
    },
    7: {  # 선임 / B4(8)
        1: ("리더십: 관리자 리더십(코칭/성과관리) / 커뮤니케이션·협업: 의사결정 대화 구조",
            "조직 이슈에 대해 운영 개선안을 제안한다.",
            "운영 보고서(운영 결과 요약)"),
        2: ("문제해결: 전략-조직 정렬(연계 구조) / 규정/기준: 기준·원칙 문서화",
            "전략-조직-업무의 연결을 정리하고 정렬 근거를 제시한다.",
            "검토서(전략-조직 정렬도 포함)"),
        3: ("문제해결: 성과 구조(지표-행동-책임) / 문제해결: 구조적 원인 분석",
            "성과 구조를 지표-행동-책임으로 정리한다.",
            "설계서(성과 구조도)"),
        4: ("표준화·확산: 구조 정착(운영기준/반복 방지) / 리더십: 변화관리 기초",
            "반복 이슈에 대해 구조 개선안을 제안한다.",
            "품의/설계형 문서(구조 개선 제안서)"),
    },
    8: {  # 책임 / B5(9)
        1: ("리더십: 전략적 선택(Trade-off) / 커뮤니케이션·협업: 경영 의사결정 설명",
            "선택안의 트레이드오프를 비교하고 선택 이유를 정한다.",
            "대안비교서(Trade-off 포함)"),
        2: ("문제해결: 전사 포트폴리오/우선순위 / 데이터/측정: 지표 구조 이해",
            "전사 우선순위를 기준에 따라 정리한다.",
            "검토서(우선순위/배분 포함)"),
        3: ("문제해결: 전사 리스크(연쇄 영향/제약) / 문제해결: 대안 시나리오",
            "전사 리스크의 연쇄 영향을 정리한다.",
            "전사 리스크 검토서"),
        4: ("표준화·확산: 전사 기준/원칙·거버넌스 / 리더십: 방향 설정/내재화",
            "전사 기준/원칙 초안을 작성하고 범위 및 예외를 명확히 한다.",
            "전사 표준 산출물(기준/원칙·거버넌스 초안)"),
    },
}

# 4-4. Gap 코드 -> 한글 표현 변환표(design_docs 03_규칙엔진 원문 그대로)
GAP_LABELS = {
    "GAP_CERT_GAS_1": "가스기능사 취득 필요",
    "GAP_CERT_GAS_2": "가스산업기사 취득 필요",
    "GAP_CERT_HQ_1": "직무 연계 민간/공인 자격 1개 이상 취득 필요",
    "GAP_CERT_HQ_2": "직무 전문·심화 자격 1개 이상 취득 필요",
    "GAP_CERT_ENERGY_1": "직무 관련 기사 자격 취득 권장",
    "GAP_EDU_LEADER": "차세대 리더교육 또는 리더교육 미이수",
    "GAP_EDU_LEGAL": "법정교육 미이수 — 즉시 이수 필요(법적 리스크)",
    "GAP_AX_URGENT": "AX 공통역량(L1 도구활용) 미확보 + 평가에서 AX 보완 요구 확인",
    "GAP_AX_BASIC": "AX 공통역량(L1 도구활용) 미확보",
    "GAP_AX_LEVEL": "과장 이상은 AX L2(업무전환) 수준 권장",
    "GAP_AX_FORMAL": "AX 실무 활용 역량은 있으나 공식 교육 미이수 — 체계화 필요",
}


def map_job_to_group(직무명, 소속팀=None):
    """02_RAG 표12 그대로: 0단계(별칭 정규화) → 1단계(총괄 특례) → 2단계(매핑)
    → 3단계(unknown). '총괄' 직무는 새 직무가 아니라 팀장 역할이라, 소속팀의
    기능(본부)을 기준으로 job_group을 판단한다."""
    job = _JOB_ALIAS.get(직무명, 직무명)
    if job == "총괄":
        dept = _TEAM_TO_DEPT.get(소속팀)
        DEPT_TO_GROUP_FOR_CHONGGWAL = {"GAS": "safety", "ENERGY": "new_biz", "MGMT": "planning", "ESG": "esg", "HQ": "hr"}
        grp = DEPT_TO_GROUP_FOR_CHONGGWAL.get(dept)
        return grp or "unknown"
    return _JOB_TO_GROUP.get(job, "unknown")


def meets_or_exceeds_gas_cert(certs, required):
    """03_규칙엔진 3-1 meets_or_exceeds_gas_cert 그대로."""
    HIERARCHY = ["가스기능사", "가스산업기사", "가스기사"]
    if required not in HIERARCHY:
        return required in certs
    required_level = HIERARCHY.index(required)
    for c in certs:
        if c in HIERARCHY and HIERARCHY.index(c) >= required_level:
            return True
    return False


# 02_RAG 6-3 "이수 교육 최신성 가중치" 표 그대로. 00_운영노하우 오류8 —
# 개정 이력에는 "R-02-1/2/3에 get_edu_weight() 반영"이라고 적혀 있었지만 실제
# 코드에는 전혀 없었다(문서만 개정되고 코드는 롤백/누락된 오류18류 회귀).
# 2026-09-16 실제로 구현.
_YEAR_RE = re.compile(r"(19|20)\d{2}")


def extract_edu_year(edu_row):
    """"교육기간(*)"(원본 취득/시작일 문자열)에서 이수 연도를 뽑는다. 형식이
    깨져 있거나 값이 없으면 None — 정보가 없다는 뜻이지 오래됐다는 뜻이
    아니므로 get_edu_weight가 이 경우 가중치를 낮추지 않는다."""
    raw = edu_row.get("교육기간(*)") or edu_row.get("교육기간(*).1") or ""
    m = _YEAR_RE.search(str(raw))
    return int(m.group(0)) if m else None


def get_edu_weight(edu_row, 기준연도):
    """02_RAG 6-3 표 그대로: 0~1년 경과=1.0, 2~3년 경과=0.7, 4년 이상=0.5.
    이수연도를 못 찾으면 1.0(정보 없음을 "오래됨"으로 임의 해석하지 않음)."""
    year = extract_edu_year(edu_row)
    if year is None:
        return 1.0
    elapsed = 기준연도 - year
    if elapsed <= 1:
        return 1.0
    if elapsed <= 3:
        return 0.7
    return 0.5


def _completed_courses(edu_history):
    return [e for e in edu_history if e.get("이수여부") == "Y"]


def _stale_completed_courses(edu_history, 기준연도):
    """03_규칙엔진.docx 3-2 원문 그대로(2026-09-16 정정) — 가중치는 Gap 판정
    자체에는 영향을 주지 않는다. 원문 의사코드는 basic/adv/expert_courses를
    가중치 없이 그대로 써서 Gap 여부를 정하고, get_edu_weight()는 "이미 지난
    단계"(yr 조건의 else 분기)에서 발견된 오래된 이수 건을 S-07 재이수 안내로
    수집하는 용도로만 쓴다.

    이전 버전은 가중치<0.7인 이수 건을 "이수 완료" 근거 자체에서 제외해버려서,
    원래는 없었을 Gap이 최신성 때문에 새로 뜨는 회귀를 만들었다(원문에 없는
    동작). 이 함수는 Gap 판정에는 관여하지 않고 순수하게 S-07 안내용 stale
    목록만 반환한다 — 원문처럼 "이미 지난 단계"로 정확히 한정하려면 각 이수
    건이 어느 레벨(L1/L2/L3)에 해당하는지 개별 분류가 필요한데, 지금 구조는
    레벨 충족 여부를 judge가 한 번에 종합 판정하고 개별 과정을 레벨별로
    분류해 반환하지 않는다 — 그래서 범위를 넓혀(연차 조건과 무관하게 전체
    이수 건 중 오래된 것) 안내하는 것으로 근사한다. Gap 판정을 절대 건드리지
    않으므로 과도하게 알려주는 것 외의 부작용은 없다."""
    return [e for e in _completed_courses(edu_history) if get_edu_weight(e, 기준연도) < 0.7]


def classify_ax_level(교육이력):
    """F-03-B 키워드표(표18) 기준 — 교육과정명에 L1/L2/L3 키워드가 있는지로
    AX_교육_이수수·AX_교육_최고수준을 판단한다(교육이력 전체를 보되, 이수여부
    무관하게 과정명만 본다는 03_규칙엔진 본문에 별도 이수여부 조건이 없어
    그대로 따름)."""
    count = 0
    highest = None  # None < L1 < L2 < L3
    order = {"L1": 1, "L2": 2, "L3": 3}
    for e in 교육이력:
        name = e.get("교육과정명") or ""
        matched = None
        for level in ("L3", "L2", "L1"):  # 상위 레벨부터 검사(둘 다 걸리면 상위 우선)
            if any(kw in name for kw in AX_KEYWORDS[level]):
                matched = level
                break
        if matched:
            count += 1
            if highest is None or order[matched] > order[highest]:
                highest = matched
    return count, highest


# ---------------------------------------------------------------------------
# STEP-1: 입력 검증(validate_input) — 03_규칙엔진 2-2 그대로
# ---------------------------------------------------------------------------

def validate_input(V):
    errors, warnings, notices = [], [], []

    if not re.match(r"^H\d{5}$", V["사번"]):
        errors.append("VL-01: 사번 형식 오류")

    if V["Band코드"] not in BAND_ENUM:
        errors.append(f"VL-05: Band 코드 허용값 외 — {V['Band코드']}")

    dept = _TEAM_TO_DEPT.get(V["소속팀"])
    if dept is None:
        errors.append(f"VL-03: 소속팀 본부 매핑 실패 — {V['소속팀']}")
    else:
        V["본부코드"] = dept

    if errors:
        return {"has_error": True, "error_messages": errors}

    if V.get("직무연차") is None:
        V["직무연차"] = 1
        warnings.append("VL-08: 직무연차 미입력 — 1년차 기본 적용")

    if not V.get("교육이력"):
        V["교육이력"] = []
        warnings.append("VL-10: 교육이력 없음 — 전체 교육 Gap 처리")

    V.setdefault("제출산출물목록", [])

    year_level = min(V["직무연차"], 4)
    if V["Band코드"] in ("과장", "차석") and V["직무연차"] == 1:
        year_level = 2
        notices.append("VL-12: B3 신규직무 — year_level=2 치환 적용")
    elif V["Band코드"] in ("선임", "책임") and V["직무연차"] == 1:
        year_level = 3
        notices.append("VL-13: B4/B5 신규직무 — year_level=3 치환 적용")
    V["year_level"] = year_level

    return {"has_error": False, "normalized_input": V, "warnings": warnings, "notices": notices}


# ---------------------------------------------------------------------------
# STEP-2: Gap 판단
# ---------------------------------------------------------------------------

def run_R01(V, judge=None):
    """자격 Gap — GAS 계층 판정만 결정론적, 나머지 본부는 자격명 분류
    (is_job_linked_cert/is_expert_cert/is_industry_cert)가 종합추론 영역이다.
    judge(Phase 3의 LLMJudge)가 있으면 실제로 판정해서 Gap 발동 여부를
    확정하고, 없거나 게이트웨이 호출이 실패하면 이전처럼 PENDING으로 남긴다."""
    gaps = []
    dept = V["본부코드"]
    band_num = BAND_LEVEL[V["Band코드"]]
    certs = V["보유자격증"]

    if dept == "GAS" and band_num >= 3 and not meets_or_exceeds_gas_cert(certs, "가스기능사"):
        gaps.append({"code": "GAP_CERT_GAS_1", "content": GAP_LABELS["GAP_CERT_GAS_1"], "urgent": False, "ref": "F-02-B GAS 능력 기준표"})
    if dept == "GAS" and band_num >= 5 and not meets_or_exceeds_gas_cert(certs, "가스산업기사"):
        gaps.append({"code": "GAP_CERT_GAS_2", "content": GAP_LABELS["GAP_CERT_GAS_2"], "urgent": False, "ref": "F-02-B GAS 능력 기준표"})

    if dept in ("MGMT", "ESG", "HQ") and band_num >= 3:
        _judge_cert_gap(V, judge, gaps, "GAP_CERT_HQ_1", "직무 연계 민간/공인 자격", certs,
                        "R-01-3: 계장 이상 — 직무 연계 자격 1개 이상 보유 여부(is_job_linked_cert)")
    if dept in ("MGMT", "ESG", "HQ") and band_num >= 5:
        _judge_cert_gap(V, judge, gaps, "GAP_CERT_HQ_2", "직무 전문·심화 민간/공인 자격", certs,
                        "R-01-4: 과장 이상 — 직무 전문·심화 자격 1개 이상 보유 여부(is_expert_cert)")
    if dept == "ENERGY" and band_num >= 5:
        _judge_cert_gap(V, judge, gaps, "GAP_CERT_ENERGY_1", "직무 관련 기사(공학 계열 국가기술자격)", certs,
                        "R-01-5: ENERGY 과장 이상 — 직무 관련 기사 보유 여부(is_industry_cert)")
    return gaps


def _judge_cert_gap(V, judge, gaps, gap_code, category_desc, certs, pending_note):
    """자격 분류 판정 공통 처리 — judge가 없거나 실패하면 이전처럼 PENDING,
    있으면 실제 판정 결과로 Gap 발동 여부를 확정한다(CONFIRMED가 아니면
    Gap 유지 — 03_규칙엔진 3-1-0 AMBIGUOUS 처리 원칙과 동일).

    2026-09-16 정정 — 3-1-0은 "R-01(자격)·R-02(교육) 공통 적용" 게이트라고
    명시하는데, 지금까지 cert_meets_category는 보유 자격증 목록만 보고
    판정해 조건B(평가표에서 현재 직무 관련 실제 활용 확인)를 전혀 묻지
    않았다 — R-02(education_level_met)는 이미 하고 있던 것과 비대칭이었다.
    평가표 원문을 추가로 넘겨 R-02와 동일한 게이트를 적용한다."""
    if judge is None:
        V["_pending_judgments"].append(f"{pending_note} — 보유 자격증 {certs}")
        return
    verdict = judge.cert_meets_category(category_desc, certs, V["직무명"], V.get("평가표_전체") or "",
                                         current_job=V["직무명"], current_team=V["소속팀"])
    if verdict is None:  # 게이트웨이 호출 자체가 실패
        V["_pending_judgments"].append(f"{pending_note} — AI 판단 실패(게이트웨이 응답 없음), 보유 자격증 {certs}")
        return
    if verdict.status != "CONFIRMED":
        gap = {"code": gap_code, "content": GAP_LABELS[gap_code], "urgent": False, "ref": f"F-02-B 능력 기준표 (AI 판정: {verdict.reason})"}
        gaps.append(gap)
        if verdict.status == "AMBIGUOUS":
            V["notices"].append(f"[{gap_code}] {category_desc} 판정이 애매합니다({verdict.reason}) — 담당자 확인 권장.")


# 02_RAG 표12/01_온톨로지 표21(F-04-C) — job_group별 기초(L1)/심화(L2)/전문(L3) 정의.
# 판정 LLM 프롬프트에 그대로 근거로 넣는다(임의 해석 없이 문서 정의를 그대로 전달).
JOB_GROUP_LEVEL_DEFS = {
    "safety": ("안전 기준·절차 이해, 현장 안전 관리", "사고·이슈를 통한 위험원인 이해, 예방 관점 수행", "경험·노하우를 기준·원칙으로 정리, 조직 안전 운영체계 설계"),
    "new_biz": ("산업·정책 환경 이해, 기초 과제 수행", "사업구조·리스크 이해, 판단 학습", "사업 판단 기준 정립, 실행 선택 설계"),
    "ai_it": ("업무 수행 IT 기본 능력 확보", "데이터·시스템 활용 업무효율화", "IT 능력 구조화, 조직 경쟁력 확보"),
    "planning": ("사업·재무·제도 환경 이해, 기초 자료 정리·지원", "전략·재무·제도 데이터 연결, 대안 비교·분석", "전략·포트폴리오·정책 통합, 전사 의사결정 주도"),
    "finance": ("재무·회계·세무 데이터 규정 기준 처리", "수치 간 관계·구조 이해, 대안 해석·비교 판단", "경영 판단 기준 제시, 중장기 재무 구조·정책 설계"),
    "hr": ("인사 제도·규정 이해, 기준 따라 정확 처리", "운영 결과 분석, 제도 관점 대안 설명", "전사관점 전략·제도 설계, 중장기 관점 주도"),
    "mgmt": ("제도·규정 이해, 프로세스 따라 수행", "운영 결과 분석, 대안 비교·설명", "경영관리 영역 전반 구조적 설계·개선"),
    "esg": ("ESG 개념·기준 이해, 정해진 활동·공시 수행", "ESG 활동·데이터 분석, 개선 과제 도출", "ESG 전략·브랜드·경영 관점 설계, 전사 ESG 방향성 정립"),
    "customer": ("요금·계량·수납·안내 정확성·일관성 확보", "고객 불편·민원 데이터 분석, 개선과제 도출", "고객 신뢰·수익성 함께 고려, 고객가치 구조 설계"),
}
_LEVEL_GAP_CODE = {"L1": "GAP_EDU_BASIC", "L2": "GAP_EDU_ADV", "L3": "GAP_EDU_EXPERT"}
_LEVEL_INDEX = {"L1": 0, "L2": 1, "L3": 2}
# 04_LLM프롬프트 [내부 표현 한글화 변환 기준] — "직무교육 내부 L1/L2/L3 → 각각
# '기초 수준/심화 수준/전문 수준'으로만 표기(괄호 안 L1/L2/L3를 붙이지 않음)".
# 2026-09-16 정정 — gap["content"]가 이 변환 없이 원문 "L1/L2/L3"를 그대로
# 담고 있었다. 이 content는 report_facing_content()를 거쳐 narrate_* 프롬프트에
# 그대로 들어가는데, humanize()(notices/pending_judgments 전용 경로)를 거치지
# 않아 LLM이 입력에 있는 "L2"를 그대로 출력 문장에 베껴써서 STEP 2.5(내부용어
# 노출)를 실제로 반복 실패시켰다(H98500 실사용 사례).
_LEVEL_KO = {"L1": "기초", "L2": "심화", "L3": "전문"}


def run_R02(V, judge=None):
    """교육 Gap — job_group 매핑과 R-02-5(법정교육)만 결정론적. L1/L2/L3 이수
    판정(is_job_edu_*)과 R-02-4(리더십교육 — hr_data에 역량유형 필드 자체가
    없음)는 judge가 있으면 실제 판정, 없으면 PENDING. RAG 6-3 최신성 가중치는
    Gap 판정에는 관여하지 않고(원문 그대로 — 오래된 이수 건도 여전히 이수
    완료로 인정) S-07 재이수 안내로만 별도 수집한다(_stale_completed_courses)."""
    gaps = []
    job_group = map_job_to_group(V["직무명"], V["소속팀"])
    yr = V["직무연차"]
    band_num = BAND_LEVEL[V["Band코드"]]
    edu_history = V["교육이력"]
    job_label = JOB_GROUP_LABEL.get(job_group, job_group)
    completed = _completed_courses(edu_history)
    for e in _stale_completed_courses(edu_history, V["기준연도"]):
        course = e.get("교육과정명") or "(과정명 미상)"
        year = extract_edu_year(e)
        V["notices"].append(f"[이수 최신성] '{course}'({year}년 이수) — 이수 후 4년 이상 경과, 재이수 여부 확인 권장")

    if job_group == "unknown":
        V["_pending_judgments"].append(f"R-02: 직무명 '{V['직무명']}'을 job_group으로 분류하지 못함(02_RAG 3단계 — 담당자 확인 필요)")

    levels_to_check = []
    if yr == 1:
        levels_to_check.append("L1")
    if yr >= 2:
        levels_to_check.append("L2")
    if yr >= 4:
        levels_to_check.append("L3")

    for level in levels_to_check:
        gap_code = _LEVEL_GAP_CODE[level]
        pending_note = f"R-02({level}): {job_label} 직무 {_LEVEL_KO[level]} 교육 이수 여부"
        if judge is None or job_group == "unknown":
            V["_pending_judgments"].append(pending_note + (" — 종합추론 필요" if judge is None else " — job_group 미분류라 판정 불가"))
            continue
        level_def = JOB_GROUP_LEVEL_DEFS.get(job_group, ("", "", ""))[_LEVEL_INDEX[level]]
        verdict = judge.education_level_met(level, level_def, job_label, completed, V.get("평가표_전체") or "",
                                             current_job=V["직무명"], current_team=V["소속팀"])
        if verdict is None:
            V["_pending_judgments"].append(pending_note + " — AI 판단 실패(게이트웨이 응답 없음)")
            continue
        if verdict.status != "CONFIRMED":
            gaps.append({"code": gap_code, "content": f"{job_label} 직무 {_LEVEL_KO[level]} 교육 미이수", "urgent": False, "ref": f"F-04-C {job_label} 직무 교육 {level} 기준 (AI 판정: {verdict.reason})"})
            if verdict.status == "AMBIGUOUS":
                V["notices"].append(f"[{gap_code}] {job_label} {_LEVEL_KO[level]} 교육 이수 판정이 애매합니다({verdict.reason}) — 담당자 확인 권장.")

    if band_num >= 5:
        if judge is None:
            V["_pending_judgments"].append("R-02-4: 리더십교육(COMP_LEADER) 이수 여부 — hr_data 교육이력에 역량유형 필드가 없어 판단 보류")
        else:
            verdict = judge.leadership_education_done(completed)
            if verdict is None:
                V["_pending_judgments"].append("R-02-4: 리더십교육 이수 여부 — AI 판단 실패(게이트웨이 응답 없음)")
            elif verdict.status != "CONFIRMED":
                gaps.append({"code": "GAP_EDU_LEADER", "content": GAP_LABELS["GAP_EDU_LEADER"], "urgent": False, "ref": f"F-04-B B3 이상 필수 교육 (AI 판정: {verdict.reason})"})
                if verdict.status == "AMBIGUOUS":
                    V["notices"].append(f"[GAP_EDU_LEADER] 리더십교육 이수 판정이 애매합니다({verdict.reason}) — 담당자 확인 권장.")

    legal_not_done = [e for e in edu_history if e.get("교육구분(*)") == "법정교육" and e.get("이수여부") == "N"]
    if legal_not_done:
        gaps.append({"code": "GAP_EDU_LEGAL", "content": GAP_LABELS["GAP_EDU_LEGAL"], "urgent": True, "ref": "F-04-A 법정교육 정의",
                     "미이수_교육": [e.get("교육과정명") for e in legal_not_done]})

    # R-02-5b(2026-09-16, 사용자 확정) — 08_리포트생성지시서 PART5 체크리스트
    # ⑩("본부별 법정교육 대상 기준 파일 미입력 시 담당자 확인 처리")의 대안.
    # 그 기준 파일이 없어 "같은 소속팀 동료 2명 이상이 이수한 법정교육을 이
    # 사람만 이수 기록이 없는" 경우로 간접 추정한다. 확정 Gap이 아니라
    # 추정이므로 top3 점수 경쟁에 넣지 않고 S-07 담당자 확인 안내로만 남긴다.
    already_flagged = {e.get("교육과정명") for e in legal_not_done}
    missing_legal = _candidate_mod.get_missing_team_legal_trainings(V["사번"], V["소속팀"])
    for course in missing_legal:
        if course in already_flagged:
            continue
        V["notices"].append(f"'{course}' 법정교육을 같은 소속팀 동료 다수가 이수했으나 이 사람은 이수 기록이 확인되지 않습니다 — 직무 관련성과 실제 이수 여부를 담당자가 확인해주세요.")
    return gaps


def run_R03(V):
    """AX 역량 Gap — 키워드 매칭 기반이라 완전히 결정론적."""
    gaps = []
    band_num = BAND_LEVEL[V["Band코드"]]
    ax_count, ax_level = classify_ax_level(V["교육이력"])
    보완사항 = V.get("평가표_보완사항") or ""
    has_ax_complaint = any(kw in 보완사항 for kw in AX_COMPLAINT_KEYWORDS)

    if ax_count == 0 and ax_level is None:
        평가표_전체 = V.get("평가표_전체") or ""
        if any(kw in 평가표_전체 for kw in AI_USAGE_KEYWORDS):
            ax_level = "L2_informal"

    if ax_count == 0 and has_ax_complaint and ax_level != "L2_informal":
        gaps.append({"code": "GAP_AX_URGENT", "content": GAP_LABELS["GAP_AX_URGENT"], "urgent": False, "ax_complaint": True, "ref": "F-03-B AX 역량 키워드 + 평가 보완 키워드"})
    elif ax_count == 0 and ax_level != "L2_informal":
        gaps.append({"code": "GAP_AX_BASIC", "content": GAP_LABELS["GAP_AX_BASIC"], "urgent": False, "ax_complaint": False, "ref": "F-03-B AX 역량 키워드"})

    if ax_level == "L1" and band_num >= 5:
        gaps.append({"code": "GAP_AX_LEVEL", "content": GAP_LABELS["GAP_AX_LEVEL"], "urgent": False, "ref": "F-03-A AX 역량 수준 기준"})

    if ax_level == "L2_informal" and has_ax_complaint:
        gaps.append({"code": "GAP_AX_FORMAL", "content": GAP_LABELS["GAP_AX_FORMAL"], "urgent": False, "ax_complaint": True, "ref": "F-03-A AX 역량 수준 기준 (L2_informal)"})
    return gaps


def run_R05(V, judge=None):
    """페르소나 Gap — GET_PERSONA(DOC-S-030)는 결정론적이지만, 그 축이 교육/
    평가표에서 확인되는지(is_axis_confirmed_by_*)는 종합추론이다. judge가
    있으면 축마다 CONFIRMED/GAP_NO_EVIDENCE(교육·평가표 근거 전무)/
    GAP_EXPLICIT_DEFICIT(평가표 보완사항에 명시적 결핍) 셋 중 하나로 판정한다
    (03_규칙엔진 3-5의 두 Gap 분기와 1:1 대응)."""
    gaps = []
    band_num = BAND_LEVEL[V["Band코드"]]
    persona = PERSONA_CARDS[band_num][V["year_level"]]
    axis_names = ("학습방향", "현업행동", "기대산출물")
    # 04_LLM프롬프트 S-04 규정과 09_리포트_예시가 실제로 쓰는 자연어 라벨(v2.11
    # 기준) — gap["content"]에 원문 그대로("학습방향" 등)를 박아두면 안 된다.
    # 2026-09-16 정정 — 이 매핑이 없어서 AI 게이트웨이가 응답 못 할 때(fallback
    # 경로) "[학습방향] 관련 교육 이수 이력과..." 식으로 내부 축 이름이 리포트에
    # 그대로 노출되는 실제 경로가 있었다("내부 3축 분류명 노출 금지" 위반).
    axis_label_ko = {"학습방향": "핵심 학습 방향", "현업행동": "현업에서 보여야 할 행동", "기대산출물": "이 시기에 남겨야 할 결과물"}
    # RAG 6-3 최신성 가중치는 Gap 판정에 관여하지 않는다(원문 그대로) — 이수
    # 완료 근거는 가중치와 무관하게 그대로 쓴다. stale 알림은 run_R02가 이미
    # V.notices에 남긴다(중복 방지 위해 여기서 다시 계산하지 않음).
    completed = _completed_courses(V["교육이력"])
    for axis_name, axis_expectation in zip(axis_names, persona):
        pending_note = f"R-05[{axis_name}]: 기대 내용 '{axis_expectation}'이 교육이력/평가표에서 확인되는지"
        if judge is None:
            V["_pending_judgments"].append(pending_note + " — 종합추론 필요(is_axis_confirmed_by_education/evaluation)")
            continue
        verdict = judge.persona_axis_confirmed(axis_name, axis_expectation, completed, V.get("평가표_전체") or "", V.get("평가표_보완사항") or "",
                                                current_job=V["직무명"], current_team=V["소속팀"])
        if verdict is None:
            V["_pending_judgments"].append(pending_note + " — AI 판단 실패(게이트웨이 응답 없음)")
            continue
        axis_ko = axis_label_ko[axis_name]
        if verdict.status == "GAP_EXPLICIT_DEFICIT":
            gaps.append({"code": "GAP_QUAL", "content": f"{axis_ko} 관련 평가표 보완사항에서 명시적으로 보완 필요 확인", "urgent": False, "axis": axis_name,
                         "ref": f"F-01 페르소나 {axis_name} 축 + 평가표 보완사항 (AI 판정: {verdict.reason})"})
        elif verdict.status != "CONFIRMED":  # GAP_NO_EVIDENCE 또는 AMBIGUOUS
            gaps.append({"code": "GAP_QUAL", "content": f"{axis_ko} 관련 교육 이수 이력과 평가표 확인 근거가 모두 없음", "urgent": False, "axis": axis_name,
                         "ref": f"F-01 페르소나 {axis_name} 축 + 05번 교육이력 + 평가표 전체 대조 (AI 판정: {verdict.reason})"})
            if verdict.status == "AMBIGUOUS":
                V["notices"].append(f"[GAP_QUAL:{axis_name}] 페르소나 축 확인이 애매합니다({verdict.reason}) — 담당자 확인 권장.")
    return gaps


# ---------------------------------------------------------------------------
# STEP-3/4: 점수 산출·우선순위 결정 — 03_규칙엔진 4-2/5 그대로
# ---------------------------------------------------------------------------

def is_next_stage_curriculum(gap, V):
    NEXT_STAGE_CODES_CERT_TYPE = {"GAP_CERT_HQ_1", "GAP_CERT_HQ_2", "GAP_CERT_GAS_1", "GAP_CERT_GAS_2", "GAP_CERT_ENERGY_1", "GAP_EDU_LEADER"}
    NEXT_STAGE_CODES_EDU_TYPE = {"GAP_EDU_LEADER", "GAP_EDU_ADV", "GAP_EDU_EXPERT"}
    if V["본부코드"] in CERT_TYPE_DEPTS:
        return gap["code"] in NEXT_STAGE_CODES_CERT_TYPE
    return gap["code"] in NEXT_STAGE_CODES_EDU_TYPE


def calc_score(gap, V):
    score = 10
    if gap.get("urgent"):
        score += 20
    if gap.get("ax_complaint"):
        score += 10
    if is_next_stage_curriculum(gap, V):
        score += 5
    if V["직무연차"] == 1 and gap["code"] == "GAP_EDU_BASIC":
        score += 5
    if gap["code"] == "GAP_AX_URGENT":
        score += 8
    return score


def sort_by_score(gaps, V):
    order = ({"GAP_EDU_LEGAL": 0, "GAP_CERT_": 1, "GAP_EDU_": 2, "GAP_AX_": 3, "GAP_ART_": 4, "GAP_QUAL": 5}
              if V["본부코드"] in CERT_TYPE_DEPTS else
              {"GAP_EDU_LEGAL": 0, "GAP_EDU_": 1, "GAP_CERT_": 2, "GAP_AX_": 3, "GAP_ART_": 4, "GAP_QUAL": 5})

    def tiebreak(g):
        for prefix, rank in order.items():
            if g["code"].startswith(prefix):
                return rank
        return 99

    return sorted(gaps, key=lambda g: (-g["score"], tiebreak(g)))


def run_rule_engine(V, judge=None):
    """judge(Phase 3의 LLMJudge 인스턴스)를 주면 종합추론이 필요한 판정을
    실제로 사내 AI 게이트웨이에 물어서 Gap을 확정한다. 안 주거나(judge=None)
    게이트웨이 호출이 실패하면 Phase 2와 동일하게 PENDING으로 남는다."""
    V["_pending_judgments"] = []
    result = validate_input(V)
    if result["has_error"]:
        return {"error": result["error_messages"]}
    V = result["normalized_input"]
    V["warnings"] = result["warnings"]
    V["notices"] = result["notices"]

    gaps = []
    gaps += run_R01(V, judge)
    gaps += run_R02(V, judge)
    gaps += run_R03(V)
    # R-04(산출물 Gap)는 design_docs 03_규칙엔진 3-4 하단 지침대로 SKIP —
    # V.제출산출물목록이 가변층 입력 명세(V-04~V-06)에 아예 없다고 문서에 명시됨
    gaps += run_R05(V, judge)

    for gap in gaps:
        gap["score"] = calc_score(gap, V)
    gaps_sorted = sort_by_score(gaps, V)

    return {
        "top3_gaps": gaps_sorted[:3],
        "recommended_gaps": gaps_sorted[3:],
        "notices": V["warnings"] + V["notices"],
        "pending_judgments": V["_pending_judgments"],
    }


# ---------------------------------------------------------------------------
# Phase 1 candidate -> V 어댑터
# ---------------------------------------------------------------------------

def build_V(candidate, year=None):
    b = candidate["기본정보"]
    j = candidate["직무정보"]
    eval_ = candidate["평가표"] or {}
    return {
        "사번": b["사번"],
        "Band코드": b["Band코드"],
        "소속팀": b["소속팀"],
        "직무명": j["직무명"],
        "직무연차": j["직무연차"],
        "직무연차_표기": j.get("직무경험_표기") or f"{j['직무연차']}년차",
        "보유자격증": [c.get("자격종류") for c in candidate["자격증"] if c.get("자격종류")],
        "교육이력": candidate["교육이력"],
        "평가표_전체": eval_.get("원문_전체", ""),
        "평가표_보완사항": eval_.get("보완사항", ""),
        "기준연도": year or date.today().year,
    }


def main():
    for 사번 in _candidate_mod._PILOT_사번:
        candidate = _candidate_mod.build_candidate(사번)
        V = build_V(candidate)
        out = run_rule_engine(V)
        b = candidate["기본정보"]
        print(f"=== {사번} {b['성명']} ({b['역할등급_표기']}, {V['직무명']} {V['직무연차']}년차) ===")
        if "error" in out:
            print("  검증 실패:", out["error"])
            continue
        print(f"  확정 Gap(top3): {[ (g['code'], g['score']) for g in out['top3_gaps'] ]}")
        print(f"  추가 권장 Gap: {[ (g['code'], g['score']) for g in out['recommended_gaps'] ]}")
        print(f"  notices: {out['notices']}")
        print(f"  Phase 3로 넘길 판단 대기 항목 {len(out['pending_judgments'])}건")
        print()


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/03_규칙엔진_LLM판정.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
03_규칙엔진_LLM판정.py — 육성추천 Phase 3: 종합추론 판정(사내 AI 게이트웨이)
=========================================
02_규칙엔진.py가 PENDING_LLM_JUDGMENT로 남겨둔 판정(자격/교육이 실제 직무와
평가표에서 확인되는지, 교육과정명이 기초/심화/전문 중 어디인지, 페르소나 축이
채워지는지)을 실제로 사내 AI 게이트웨이(talent_recommendation/pipeline_service.py,
27_공통_육성연계매핑.py와 같은 엔드포인트·환경변수)에 물어서 확정한다.

[이 파일이 확실히 하는 것 — 27_공통_육성연계매핑.py와 동일한 안전장치]
  - 게이트웨이 호출이 실패하면(타임아웃 등) "관련 없음"으로 조용히 넘기지
    않는다. 재시도까지 다 써도 실패하면 None을 돌려주고, 호출부(02_규칙엔진.py)는
    그 항목을 PENDING으로 남긴다 — Gap 판정에 손대지 않는다.
  - "충족"과 "확신 없음"을 구분한다. LLM이 애매하다고 답하면 CONFIRMED로
    처리하지 않고 AMBIGUOUS로 남겨 S-07(담당자 확인)에 반영되게 한다
    (03_규칙엔진 3-1-0의 AMBIGUOUS 처리 원칙 그대로).
  - 같은 판정은 같은 답이 나오도록 temperature=0을 쓴다(27_공통_육성연계매핑과 동일).

[샌드박스 제약 — 사용자에게 미리 알린 내용]
  사내 AI 게이트웨이(http://caio.cncityenergy.com/llm/v1)는 사내망 전용 호스트라
  이 개발 환경에서는 DNS 자체가 안 풀린다(2026-09-14 확인). 이 파일의 판정 로직은
  02_규칙엔진.py의 PENDING 목록·판정 기준을 그대로 코드화한 것이지만, 실제
  게이트웨이 응답을 받아 판정 품질까지 끝까지 검증한 것은 아니다 — 그건 이
  플랫폼이 실제로 도는 사내망 환경에서 확인해야 한다. 게이트웨이가 안 열려 있는
  동안에도 이 파일은 죽지 않고 "AI 판단 실패"로 명확히 남기고 넘어간다.
"""

import os
import re
import sys
import time
from dataclasses import dataclass
from pathlib import Path

import requests

BASE_DIR = Path(__file__).resolve().parent
ROOT_DIR = BASE_DIR.parent
for _p in (str(ROOT_DIR), str(ROOT_DIR / "talent_recommendation"), str(BASE_DIR)):
    if _p not in sys.path:
        sys.path.insert(0, _p)

import importlib
_candidate_mod = importlib.import_module("01_후보자통합")
_engine_mod = importlib.import_module("02_규칙엔진")

# talent_recommendation/pipeline_service.py·27_공통_육성연계매핑.py와 동일한
# 엔드포인트/환경변수 — 이 플랫폼 전체가 같은 사내 AI 게이트웨이 하나를 쓴다.
LLM_BASE_URL = os.environ.get("LLM_BASE_URL", "http://caio.cncityenergy.com/llm/v1")
LLM_API_KEY = os.environ.get("LLM_API_KEY", "")
LLM_MODEL = os.environ.get("LLM_MODEL", "gemma-4-31B-it")
LLM_TIMEOUT = (3, 20)


def _call_llm(prompt, attempts=3):
    """27_공통_육성연계매핑._call_llm과 동일한 재시도/실패 처리. 실패하면
    None(호출부가 PENDING으로 남김) — "관련 없음"과 절대 혼동하지 않는다."""
    payload = {"model": LLM_MODEL, "messages": [{"role": "user", "content": prompt}], "stream": False, "temperature": 0}
    headers = {"Content-Type": "application/json"}
    if LLM_API_KEY:
        headers["Authorization"] = f"Bearer {LLM_API_KEY}"
    for attempt in range(attempts):
        try:
            res = requests.post(f"{LLM_BASE_URL}/chat/completions", json=payload, headers=headers, timeout=LLM_TIMEOUT)
            if res.status_code == 200:
                content = res.json()["choices"][0]["message"].get("content", "").strip()
                if content:
                    return content
        except (requests.exceptions.RequestException, ValueError, KeyError, IndexError):
            pass
        if attempt < attempts - 1:
            time.sleep(2 * (attempt + 1))
    return None


@dataclass
class Verdict:
    status: str  # "CONFIRMED" | "NOT_APPLICABLE" | "AMBIGUOUS" | "GAP_NO_EVIDENCE" | "GAP_EXPLICIT_DEFICIT"
    reason: str


def _job_time_scope_instruction(current_job, current_team):
    """03_규칙엔진 3-1-0 "평가표 복수 직무 시간축 근거 분리"(v1.4) 대응
    (2026-09-16 신규) — 인사이동으로 직무가 바뀐 사람의 평가표에는 예전
    직무 시절 성과·행동이 섞여 있을 수 있는데, 지금까지 판정 프롬프트들은
    평가표 원문을 통째로 넘기기만 하고 "예전 직무 구간 내용은 지금 직무
    충족 근거로 쓰지 마라"는 지시가 없었다. 평가표를 시간 구간별로 미리
    분리해두는 구조가 없어서, 같은 LLM 호출 안에서 그 구분·배제를 직접
    지시하는 방식으로 구현한다 — 원문 is_eval_confirmed()가 "먼저 현재
    직무와 연결되는 범위를 식별한 뒤 그 범위 안에서 근거를 찾는다"고 한
    절차를 한 번의 판정 안에 녹인 것."""
    return (
        f"[중요] 이 평가표에 현재 직무('{current_job}', 소속 '{current_team}')와 다른 과거 "
        "직무·소속이 명시적으로 구분되어 서술되어 있다면(예: 상반기/하반기 구분, 인사이동·전보 "
        "서술), 그 과거 구간의 성과·행동은 이 판정의 근거로 쓰지 마세요 — 오직 현재 직무 구간에 "
        "해당하는 내용만 근거로 삼습니다. 어떤 구간에 속하는지 명확하지 않으면 확신도를 반드시 "
        "'애매'로 답하세요.\n"
    )


def _parse_verdict(text, status_map):
    """"정답: ... / 확신도: ... / 이유: ..." 형식 응답을 파싱한다(27_공통_
    육성연계매핑과 동일한 형식 — 플랫폼 전체가 하나의 응답 관례를 쓰게 함).
    status_map은 '정답' 필드에 나올 수 있는 한글 라벨 -> 내부 status 코드."""
    if text is None:
        return None
    answer_m = re.search(r"정답\s*[:：]\s*(.+)", text)
    if not answer_m:
        return None
    answer = answer_m.group(1).strip().splitlines()[0].strip()
    confidence_m = re.search(r"확신도\s*[:：]\s*(.+)", text)
    is_confident = bool(confidence_m) and confidence_m.group(1).strip().splitlines()[0].strip().startswith("확실")
    reason_m = re.search(r"이유\s*[:：]\s*(.+)", text)
    reason = reason_m.group(1).strip().splitlines()[0].strip() if reason_m else ""

    # 2026-09-16 정정 — status_map을 삽입 순서 그대로 훑으면 "미충족"처럼 긴
    # 라벨 안에 "충족"처럼 짧은 라벨이 부분 문자열로 들어있는 경우 짧은 쪽이
    # 먼저 걸려 완전히 반대로 파싱되는 실제 버그가 있었다(education_level_met의
    # "미충족" 응답이 전부 "충족"으로 뒤집혀 R-02 조건B 게이트가 사실상
    # 무력화되고 있었음 — 실사용 영향이 큰 P0급 결함). 긴 라벨부터 먼저
    # 검사해 짧은 라벨이 긴 라벨의 부분 문자열로 오매칭되지 않게 한다.
    status = next((v for k, v in sorted(status_map.items(), key=lambda kv: -len(kv[0])) if k in answer), None)
    if status is None:
        return None  # 목록에 없는 답 — 환각으로 보고 폐기(27_ 패턴과 동일)
    if status == "CONFIRMED" and not is_confident:
        # 03_규칙엔진 3-1-0: 애매하면 충족으로 인정하지 않는다.
        return Verdict("AMBIGUOUS", reason or "확신 없이 충족으로 답함")
    return Verdict(status, reason)


class LLMJudge:
    """02_규칙엔진.py의 run_R01/R02/R05가 기대하는 judge 인터페이스. 메서드가
    None을 반환하면 "게이트웨이 호출 실패"라는 뜻이고, 호출부가 PENDING으로
    남긴다 — 이 클래스 자신은 절대 실패를 임의의 판정으로 둔갑시키지 않는다."""

    def cert_meets_category(self, category_desc, certs, job_name, evaluation_text, current_job=None, current_team=None):
        """R-01-3/4/5 — is_job_linked_cert/is_expert_cert/is_industry_cert.

        2026-09-16 정정 — 03_규칙엔진 3-1-0은 이 게이트(조건A: 근거 보유 +
        조건B: 평가표에서 현재 직무 관련 실제 활용 확인)를 "R-01(자격)·R-02
        (교육) 공통 적용"이라고 명시하는데, 이 함수는 지금까지 보유 자격증
        목록만 보고 조건B를 전혀 묻지 않았다(education_level_met은 이미
        하고 있던 것과 비대칭이었다) — 평가표 원문을 추가해 동일한 게이트를
        적용한다. current_job/current_team이 있으면 시간축 분리 지시(v1.4)도
        함께 준다."""
        if not certs:
            return Verdict("NOT_APPLICABLE", "보유 자격증 없음")
        listing = "\n".join(f"- {c}" for c in certs)
        time_scope = _job_time_scope_instruction(current_job, current_team) if current_job else ""
        prompt = (
            f"직원의 현재 직무는 '{job_name}'입니다. 아래 보유 자격증 목록 중에 "
            f"'{category_desc}'에 해당하는 자격증이 있습니까?\n\n[보유 자격증]\n{listing}\n\n"
            f"[평가표 원문]\n{evaluation_text[:3000] or '(평가표 없음)'}\n\n{time_scope}"
            f"이 요건이 충족된다고 볼 수 있으려면 두 조건을 모두 만족해야 합니다: "
            f"① 위 보유 자격증 중에 '{category_desc}'에 해당하는 자격증이 있고, "
            "② 그 자격증이 현재 직무와 관련해 실제로 활용되고 있다는 근거가 평가표(강점/역할/"
            "핵심가치/보완사항)에서 확인되어야 합니다. 자격증을 보유하고 있어도 평가표에서 적용 "
            "근거가 확인되지 않으면 미충족(해당없음)입니다.\n\n"
            "아래 형식으로만 답하고 다른 말은 붙이지 마세요:\n"
            "정답: <해당함 또는 해당없음 또는 판단보류 — 자격증 범주 자체가 분야상 전혀 관련 없으면 "
            "해당없음, 범주는 관련 있어 보이나 평가표 근거가 불명확하면 판단보류>\n"
            "확신도: <확실 또는 애매 — 판단이 조금이라도 헷갈리면 반드시 애매라고 답하세요>\n"
            "이유: <한 문장으로 간단히, 해당한다면 어떤 자격증인지 포함>"
        )
        return _parse_verdict(_call_llm(prompt), {"해당함": "CONFIRMED", "해당없음": "NOT_APPLICABLE", "판단보류": "AMBIGUOUS"})

    def education_level_met(self, level, level_def, job_label, completed_courses, evaluation_text, current_job=None, current_team=None):
        """R-02-1/2/3 — is_job_edu_L1/L2/L3. 03_규칙엔진 3-1-0 공통 게이트
        (조건 A: 이수 근거 + 조건 B: 평가표에서 현재 직무 관련 실제 적용 확인)를
        한 번의 판정으로 묻는다. current_job/current_team이 있으면 v1.4
        시간축 분리 지시도 함께 준다(2026-09-16 추가)."""
        courses_listing = "\n".join(f"- {c.get('교육과정명')}" for c in completed_courses if c.get("교육과정명")) or "(이수한 교육 없음)"
        time_scope = _job_time_scope_instruction(current_job, current_team) if current_job else ""
        prompt = (
            f"직원의 직무군은 '{job_label}'이고, 이 직무군의 {level} 수준 정의는 "
            f"'{level_def}'입니다.\n\n[이수한 교육과정명 목록]\n{courses_listing}\n\n"
            f"[평가표 원문]\n{evaluation_text[:3000] or '(평가표 없음)'}\n\n{time_scope}"
            f"이 직원이 {level} 수준을 충족한다고 볼 수 있으려면 두 조건을 모두 만족해야 합니다: "
            f"① 위 교육과정명 중에 이 {level} 수준 정의에 부합하는 교육을 실제로 이수했고, "
            "② 그 내용이 평가표(강점/역할/핵심가치/보완사항)에서 현재 직무와 관련해 실제로 "
            "적용·수행되고 있다는 근거로 확인되어야 합니다. 데이터상 이수 기록이 있어도 평가표에서 "
            "적용 근거가 확인되지 않으면 미충족입니다.\n\n"
            "아래 형식으로만 답하고 다른 말은 붙이지 마세요:\n"
            "정답: <충족 또는 미충족 또는 판단보류 — 이수한 교육 자체가 이 수준 정의와 전혀 무관하면 "
            "미충족, 관련 있어 보이나 평가표 적용 근거가 불명확하면 판단보류>\n"
            "확신도: <확실 또는 애매>\n"
            "이유: <한 문장으로, 어떤 교육/평가표 근거를 봤는지 포함>"
        )
        return _parse_verdict(_call_llm(prompt), {"충족": "CONFIRMED", "미충족": "NOT_APPLICABLE", "판단보류": "AMBIGUOUS"})

    def leadership_education_done(self, completed_courses):
        """R-02-4 — hr_data에 역량유형(COMP_LEADER) 필드가 없어, 교육과정명만
        보고 리더십/차세대 리더 교육에 해당하는 과정이 있는지 판정한다."""
        courses_listing = "\n".join(f"- {c.get('교육과정명')}" for c in completed_courses if c.get("교육과정명")) or "(이수한 교육 없음)"
        prompt = (
            f"아래는 한 직원이 이수한 교육과정명 목록입니다. 이 중에 '차세대 리더교육' 또는 "
            f"'리더교육'(팀 리딩, 코칭, 성과관리 등 관리자/리더십 역량을 다루는 교육)에 해당하는 "
            f"과정이 있습니까?\n\n[이수한 교육과정명 목록]\n{courses_listing}\n\n"
            "아래 형식으로만 답하고 다른 말은 붙이지 마세요:\n"
            "정답: <해당함 또는 해당없음>\n"
            "확신도: <확실 또는 애매>\n"
            "이유: <한 문장으로, 해당한다면 어떤 과정인지 포함>"
        )
        return _parse_verdict(_call_llm(prompt), {"해당함": "CONFIRMED", "해당없음": "NOT_APPLICABLE"})

    def gaps_overlap(self, content_a, content_b):
        """04번 §6-1 확장(2026-09-16, 원인1 대응) — top3 성장 포인트 중 서로
        다른 카테고리(자격/교육/리더십/정성 등)에서 나온 두 항목이 실질적으로
        같은 스킬·문제를 가리키는지 판정한다. R-01/R-02/R-05가 서로 다른
        규칙이라 이런 중복을 스스로는 못 걸러내므로, 이름이 아니라 내용
        기준으로 겹치는지 직접 물어서 확인한다."""
        prompt = (
            f"아래 두 성장 필요 항목이 실질적으로 같은 스킬·문제를 가리키는지 판단하세요.\n"
            f"항목 A: {content_a}\n항목 B: {content_b}\n\n"
            "표현이나 분류가 다르더라도 결국 같은 능력·주제를 가리키면 '같음'입니다. 서로 다른 "
            "능력·주제를 다루면(예: 하나는 법정교육 이수, 다른 하나는 전문 자격 취득처럼 성격 자체가 "
            "다르면) '다름'입니다.\n\n"
            "아래 형식으로만 답하고 다른 말은 붙이지 마세요:\n"
            "정답: <같음 또는 다름>\n확신도: <확실 또는 애매>\n이유: <한 문장>"
        )
        return _parse_verdict(_call_llm(prompt), {"같음": "CONFIRMED", "다름": "NOT_APPLICABLE"})

    def persona_axis_confirmed(self, axis_name, axis_expectation, completed_courses, evaluation_text, 보완사항, current_job=None, current_team=None):
        """R-05 — is_axis_confirmed_by_education/evaluation + 명시적 결핍 확인을
        한 번에 묻는다. 03_규칙엔진 3-5 판단 원칙 3가지를 프롬프트에 그대로
        반영: (1) 교육·평가표 둘 다 없으면 Gap, (2) 결핍이 명시되면 교육
        이수와 무관하게 Gap, (3) 둘 중 하나라도 있고 결핍 명시도 없으면 Gap 아님.
        current_job/current_team이 있으면 v1.4 시간축 분리 지시도 함께 준다
        (2026-09-16 추가)."""
        courses_listing = "\n".join(f"- {c.get('교육과정명')}" for c in completed_courses if c.get("교육과정명")) or "(이수한 교육 없음)"
        time_scope = _job_time_scope_instruction(current_job, current_team) if current_job else ""
        prompt = (
            f"이 직원의 Band·연차 기준 기대 성장 항목 중 '{axis_name}' 축의 기대 내용은 "
            f"'{axis_expectation}'입니다.\n\n[이수한 교육과정명 목록]\n{courses_listing}\n\n"
            f"[평가표 보완사항]\n{보완사항 or '(없음)'}\n\n[평가표 전체 원문]\n{evaluation_text[:3000] or '(평가표 없음)'}\n\n{time_scope}"
            "다음 세 가지 중 하나로 판정하세요(표현이 사전 단어와 다르더라도 같은 의미면 인정합니다):\n"
            "- 충족: 위 교육 이수 이력 또는 평가표(강점/역할/핵심가치) 서술 중 하나라도 이 축의 "
            "기대 내용이 실제로 수행되고 있다는 근거가 있고, 보완사항에 이 축에 대한 명시적 결핍도 없음\n"
            "- 명시적결핍: 평가표 보완사항에 이 축과 관련된 결핍·보완 필요가 명시적으로 지적됨 "
            "(교육 이수 여부와 무관하게 이 판정을 우선함)\n"
            "- 근거없음: 교육 이수 이력에도, 평가표 어디에도 이 축이 수행되고 있다는 근거가 전혀 없음\n\n"
            "아래 형식으로만 답하고 다른 말은 붙이지 마세요:\n"
            "정답: <충족 또는 명시적결핍 또는 근거없음>\n"
            "확신도: <확실 또는 애매>\n"
            "이유: <한 문장으로, 근거가 된 교육명 또는 평가표 문구 포함>"
        )
        return _parse_verdict(_call_llm(prompt), {
            "충족": "CONFIRMED", "명시적결핍": "GAP_EXPLICIT_DEFICIT", "근거없음": "GAP_NO_EVIDENCE",
        })


def main():
    judge = LLMJudge()
    gateway_ok = _call_llm("연결 확인용 메시지입니다. '정답: 확인' 이라고만 답하세요.") is not None
    if not gateway_ok:
        print(f"⚠ 사내 AI 게이트웨이({LLM_BASE_URL})에 연결할 수 없습니다.")
        print("  이 환경에서는 예상된 상황입니다(사내망 전용 호스트, 2026-09-14 확인) — ")
        print("  아래는 게이트웨이가 안 열려도 PENDING으로 안전하게 남는지 확인하는 실행입니다.\n")

    for 사번 in _candidate_mod._PILOT_사번:
        candidate = _candidate_mod.build_candidate(사번)
        V = _engine_mod.build_V(candidate)
        out = _engine_mod.run_rule_engine(V, judge=judge)
        b = candidate["기본정보"]
        print(f"=== {사번} {b['성명']} ({b['역할등급_표기']}, {V['직무명']} {V['직무연차']}년차) ===")
        if "error" in out:
            print("  검증 실패:", out["error"])
            continue
        print(f"  확정 Gap(top3): {[ (g['code'], g['score']) for g in out['top3_gaps'] ]}")
        print(f"  추가 권장 Gap: {[ (g['code'], g['score']) for g in out['recommended_gaps'] ]}")
        print(f"  notices: {len(out['notices'])}건")
        print(f"  여전히 PENDING(게이트웨이 미연결 등): {len(out['pending_judgments'])}건")
        print()


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/04_리포트서술.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
04_리포트서술.py — 육성추천 Phase 4: 리포트 서술(S-00~S-07) 생성 + STEP 2.5 검증
=========================================
02_규칙엔진.py(+ 03_규칙엔진_LLM판정.py)가 확정한 Gap·순위를 04_LLM프롬프트.docx
v2.11 스키마에 맞춰 실제 문장으로 조립한다. 이 파일의 원칙은 04번 문서 1장이
명시한 그대로다 — "LLM은 판단 결과를 자연어로 서술하는 역할만 한다. 판단·점수·
우선순위는 시스템이 이미 결정했고, LLM은 이 결과를 바꾸지 않는다."

[기계적으로 조립하는 것 — 새 문장을 짓지 않음]
  - S-01 기본정보, S-02 행1/행2(Gap 내용 그대로 치환), 행4/행5(S-05 1순위 문장을
    그대로 가져오거나 압축 없이 재사용 — 04번 §2 S-02 표: "새 문장을 짓지 않는다")
  - S-07 담당자 확인 사항의 뼈대(엔진 notices/pending_judgments를 08번·04번의
    "내부 표현 한글화 변환 기준"에 따라 사람이 읽는 문장으로 치환)

[LLM(ReportWriter)이 서술하는 것 — 종합추론 필요]
  - S-00 인사이동 감지(평가표 원문을 실제로 읽고 현재 직무/소속과 비교)
  - S-03 현재 상태 분석(교육이수→평가표 성과→게이트 판정 근거의 인과 서술,
    원문에 인과가 없으면 병렬 서술 — 04번 §6)
  - S-04 성장 이상향(페르소나 카드 원문을 그 사람의 직무 맥락으로 풀어쓰기)
  - S-05 성장 포인트 3단 구조, S-06 추천 행동 5개 항목 — 전부 그 사람의 실제
    데이터(평가표 인용, 자격/교육 현황)에서 나온 문장이어야 한다(템플릿 금지,
    04번 §5-1)

[샌드박스 제약 — 사용자에게 미리 알린 내용, Phase 3와 동일]
  사내 AI 게이트웨이가 이 환경에서 연결되지 않는다. ReportWriter의 각 narrate_*
  메서드는 게이트웨이 호출이 실패하면(None) "정답 없음"으로 지어내지 않고,
  그 사람의 실제 데이터(Gap 내용·평가표 인용)만으로 만든 안전한 대체 문장을 쓰고
  S-07에 "AI 서술 생성 실패 — 담당자 확인 필요"를 남긴다. 실제 서술 품질은
  게이트웨이가 열리는 사내망에서 검증해야 한다.

STEP 7.5(문장 품질 자동 검증)는 이 파일이 아니라 05_문장검증.py의 run_step_2_5를
그대로 호출한다 — 두 파일이 서로 다른 내용을 담지 않기 위해 검증 로직은 05번에만
둔다(08번 문서 원칙 그대로).
"""

import re
import sys
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
ROOT_DIR = BASE_DIR.parent
for _p in (str(ROOT_DIR), str(ROOT_DIR / "talent_recommendation"), str(BASE_DIR)):
    if _p not in sys.path:
        sys.path.insert(0, _p)

import importlib
_candidate_mod = importlib.import_module("01_후보자통합")
_engine_mod = importlib.import_module("02_규칙엔진")
_judge_mod = importlib.import_module("03_규칙엔진_LLM판정")
_verify_mod = importlib.import_module("05_문장검증")

_call_llm = _judge_mod._call_llm
FIXED_PURPOSE = _verify_mod.FIXED_PURPOSE

ROLE_MAP = {
    "사원": "입문실무자", "주임": "기초독립실무자", "계장": "실무주도자", "대리": "직무리더",
    "과장": "팀리더", "차석": "본부직무총괄", "선임": "전사조직총괄", "책임": "전사리더",
}

# ---------------------------------------------------------------------------
# 내부 표현 한글화 — 04번 [내부 표현 한글화 변환 기준] 표 그대로
# ---------------------------------------------------------------------------

_GAP_BRACKET_RE = re.compile(r"^\[GAP_[A-Z0-9_]+(?::[^\]]*)?\]\s*")
_CODE_PREFIX_RE = re.compile(r"^(?:VL-\d+|R-\d+(?:\([^)]*\))?(?:\[[^\]]*\])?(?:-\d+)?)\s*:\s*")
_INTERNAL_FUNC_RE = re.compile(r"\(is_[a-zA-Z_/]+\)")
_STRAY_LEVEL_RE = re.compile(r"\bL[123]\b")
# "리더십교육(COMP_LEADER)"처럼 괄호 안에 내부 코드(밑줄 포함 대문자/숫자
# 조합)가 그대로 노출되는 경우 — 05_문장검증의 FORBIDDEN_TERMS('COMP_[A-Z_]+'
# 등)가 실제로 잡아낸 사례(2026-09-16). "ESG"/"AI"/"HR"처럼 밑줄 없는 정상
# 업무 약어는 매칭하지 않도록 밑줄 포함을 조건으로 둔다.
_INTERNAL_CODE_PAREN_RE = re.compile(r"\([A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+\)")


def humanize(text):
    """엔진(02_규칙엔진)이 남긴 notices/pending_judgments 원문(코드 포함)을
    04번 [내부 표현 한글화 변환 기준]에 따라 팀장이 읽을 수 있는 문장으로 바꾼다."""
    t = _GAP_BRACKET_RE.sub("", text)
    t = _CODE_PREFIX_RE.sub("", t)
    t = _INTERNAL_FUNC_RE.sub("", t)
    t = _INTERNAL_CODE_PAREN_RE.sub("", t)
    t = _STRAY_LEVEL_RE.sub("", t)
    t = t.replace("AI 판단 실패(게이트웨이 응답 없음)", "자동 판정 결과를 받지 못함")
    t = re.sub(r"\s{2,}", " ", t).strip(" —-")
    if not t.endswith(("니다.", "함.", "요.", ".")):
        t += " — 담당자 확인이 필요합니다."
    return t.strip()


def ax_level_to_korean(ax_count, ax_level):
    """04번 [내부 표현 한글화 변환 기준] AX 항목 그대로."""
    if ax_level == "L1":
        return "AI 도구를 업무에 활용하는 단계"
    if ax_level == "L2":
        return "AI를 활용해 업무 방식을 개선·전환하는 단계"
    if ax_level == "L3":
        return "AI·데이터를 의사결정에 활용하는 단계"
    if ax_level == "L2_informal":
        return "실무 활용 경험 있음(공식 교육 미이수)"
    return "AI·생성형AI 관련 공식 교육 이수 이력이 아직 확인되지 않음"


def first_sentence(text, max_len=80):
    text = (text or "").strip()
    if not text:
        return ""
    m = re.search(r"[^.!?\n]+[.!?\n]", text)
    s = (m.group(0) if m else text).strip()
    return s[:max_len]


# 02_규칙엔진.GAP_LABELS의 AX 항목은 "(L1 도구활용)"처럼 내부 단계코드를 그대로
# 담고 있다(03_규칙엔진 원문 표현을 그대로 옮긴 값이라 그렇다 — 04_리포트서술이
# 처음 만든 문제가 아니다). 04번 [내부 표현 한글화 변환 기준]대로, 리포트에 실제
# 노출되는 모든 문장(narrate_* 프롬프트 입력 포함)은 이 함수를 거쳐야 한다.
_GAP_CONTENT_OVERRIDE = {
    "GAP_AX_URGENT": "AI 활용 공통역량(업무 도구 활용 단계) 미확보 + 평가에서 AI·디지털 보완 요구 확인",
    "GAP_AX_BASIC": "AI 활용 공통역량(업무 도구 활용 단계) 미확보",
    "GAP_AX_LEVEL": "과장 이상 AI 활용 심화(업무 방식 개선·전환 단계) 권장",
    "GAP_AX_FORMAL": "AI 실무 활용 경험은 있으나 공식 교육 미이수 — 체계화 필요",
}


def report_facing_content(gap):
    """narrate_* 프롬프트에 넣거나 fallback 문구·S-02 라벨에 쓸 때는 gap['content']를
    직접 쓰지 말고 반드시 이 함수를 거친다."""
    return _GAP_CONTENT_OVERRIDE.get(gap["code"], gap["content"])


# ---------------------------------------------------------------------------
# 원인1 대응(2026-09-16) — top3 성장 포인트가 서로 성격이 다른데도(법정교육/
# 자격/리더십/정성 등) 다 비슷한 "성과 정량화" 톤으로 쓰이던 문제. 이 카테고리는
# 이미 02_규칙엔진이 Gap을 만들 때 코드(GAP_EDU_LEGAL/GAP_CERT_*/GAP_EDU_LEADER
# 등)로 구분해둔 정보라 새로 지어낸 값이 아니다 — 지금까지 narrate_* 함수에
# 전달을 안 하고 있었을 뿐이다.
# ---------------------------------------------------------------------------

_CATEGORY_LABEL_RULES = [
    ("GAP_EDU_LEGAL", "법정교육"),
    ("GAP_CERT_", "자격"),
    ("GAP_EDU_LEADER", "리더십교육"),
    ("GAP_EDU_", "직무교육"),
    ("GAP_AX_", "AX(디지털) 역량"),
    ("GAP_QUAL", "정성(평가 기반)"),
]


def _single_category_label(code):
    for prefix, label in _CATEGORY_LABEL_RULES:
        if code.startswith(prefix):
            return label
    return "기타"


def gap_category_label(gap):
    """merged gap(_merged 하위 리스트 보유)이면 하위 항목들의 카테고리를 전부
    모아 "정성(평가 기반)+리더십교육"처럼 합쳐서 보여준다."""
    if gap.get("_merged"):
        labels = []
        for sub in gap["_merged"]:
            lbl = _single_category_label(sub.get("code", ""))
            if lbl not in labels:
                labels.append(lbl)
        return "+".join(labels) or "기타"
    return _single_category_label(gap.get("code", ""))


_CATEGORY_TONE_HINT = {
    "법정교육": "이 항목은 법정교육입니다 — 필요성을 설득하는 톤이 아니라 이수 여부·기한을 사실 그대로 확인시키는 톤으로 쓰세요.",
    "자격": "이 항목은 자격 취득입니다 — 어떤 시험을 언제까지 준비할지 취득 전략 관점으로 쓰세요.",
    "리더십교육": "이 항목은 리더십 교육입니다 — 관리자로서의 행동 변화 관점으로 쓰세요.",
    "직무교육": "이 항목은 직무 교육입니다 — 실무 적용 관점으로 쓰세요.",
    "AX(디지털) 역량": "이 항목은 AI·디지털 역량입니다 — 업무 도구 활용 관점으로 쓰세요.",
    "정성(평가 기반)": "이 항목은 평가표에서 확인된 정성적 보완점입니다 — 구체적 행동 변화 관점으로 쓰세요.",
}


def _category_tone_hint(category_label):
    """카테고리가 겹쳐 있어도("정성(평가 기반)+리더십교육") 포함된 각 성격의
    톤 지시를 전부 모아서 준다."""
    hints = [msg for key, msg in _CATEGORY_TONE_HINT.items() if key in category_label]
    seen = []
    for h in hints:
        if h not in seen:
            seen.append(h)
    return " ".join(seen)


# 원인2 대응(2026-09-16) — MGMT/ESG/HQ/ENERGY만 이 코드들에 해당한다(GAS는 이미
# GAP_LABELS에 구체적 자격명이 박혀 있어 지어낼 위험이 없다 — 02_규칙엔진.run_R01 참고).
_ABSTRACT_CERT_CODES = {"GAP_CERT_HQ_1", "GAP_CERT_HQ_2", "GAP_CERT_ENERGY_1"}


def _needs_cert_reference(gap):
    codes = [sub.get("code") for sub in gap.get("_merged", [gap])]
    return any(c in _ABSTRACT_CERT_CODES for c in codes)


# ---------------------------------------------------------------------------
# ReportWriter — 04번 §종합추론(S-00/S-03/S-04/S-05/S-06) 서술 생성.
# LLMJudge와 동일한 안전장치: 실패하면 None(호출부가 안전한 대체 문장 사용).
# ---------------------------------------------------------------------------

def _kv_parse(text, keys):
    """"라벨: 값" 줄 여러 개로 된 응답을 파싱한다(03_규칙엔진_LLM판정._parse_verdict와
    동일한 관례 — 플랫폼 전체가 하나의 응답 형식을 쓴다)."""
    if text is None:
        return None
    out = {}
    for key in keys:
        m = re.search(rf"{re.escape(key)}\s*[:：]\s*(.+)", text)
        if m:
            out[key] = m.group(1).strip().splitlines()[0].strip()
    if len(out) < len(keys):
        return None
    return out


# 08번 문서 PART 4-7 원문 "점수(숫자+점)를 리포트에 표시하지 않습니다"는
# 무조건 원칙인데(GRESB 같은 외부 지표도 예외 없음), 정작 narrate_* 프롬프트
# 어디에도 이 지시가 없었다 — STEP 2.5(05_문장검증)가 사후에 걸러내는 것만
# 믿고 있었던 것이다. 그래서 LLM이 원문 인용 없이 자기 문장으로 "GRESB
# 3점을 획득했습니다"처럼 점수를 직접 서술하면, 따옴표 인용이 아니라서
# 05_문장검증의 인용 예외에도 안 걸리고 그냥 STEP 2.5 FAIL로 리포트 생성이
# 막혀버렸다(실사용 사례, 2026-09-15). 애초에 LLM이 점수를 안 쓰게 프롬프트에
# 직접 지시하는 게 근본적인 수정이라, 자유 서술을 만드는 모든 narrate_*에
# 이 지시를 넣는다.
_NO_SCORE_INSTRUCTION = "점수를 숫자로 표시하지 마세요(예: '3점', '80점', 'A등급') — 성과나 수준은 항상 정성적인 문장으로 표현하세요."

# 05_문장검증.FORBIDDEN_PHRASE_STEMS(08번 문서 원문)도 _NO_SCORE_INSTRUCTION과
# 똑같은 문제를 갖고 있었다 — "부족합니다/미달입니다/지적되었다/역량을 강화/
# 성장에 도움이/KPI에 등록" 같은 표현을 STEP 2.5가 사후에 걸러내기만 하고,
# narrate_* 프롬프트 어디에도 쓰지 말라는 지시가 없었다. 그래서 LLM이 "~노력이
# 있으나 ~부족합니다"처럼 흔한 화법을 그대로 쓰면 STEP 2.5 FAIL로 리포트 생성이
# 막혔다(실사용 사례, 2026-09-16, H15172). 05_문장검증의 목록과 반드시 동기화
# 상태를 유지해야 한다 — 목록이 바뀌면 이 문자열도 같이 고친다.
_FORBIDDEN_PHRASE_INSTRUCTION = (
    "'역량을 강화', '성장에 도움이', 'KPI에 등록', '부족합니다', '미달입니다', '지적되었습니다'/"
    "'지적된다' 같은 표현은 절대 쓰지 마세요 — 그 대신 무엇이 어떤 상태인지, 그래서 어떻게 하면 "
    "되는지를 구체적 사실로 서술하세요."
)


class ReportWriter:
    def detect_job_change(self, current_job, current_team, evaluation_text):
        """S-00 — 08번 STEP 0. 평가표 전체를 읽고 현재 직무/소속과 다른 인사이동이
        명시적으로 서술되어 있는지 판정한다(단순 협업·지원 표현은 인사이동 아님)."""
        prompt = (
            f"현재 인사 데이터 기준 직무는 '{current_job}', 소속팀은 '{current_team}'입니다.\n\n"
            f"[전년도 평가표 원문]\n{evaluation_text[:4000]}\n\n"
            "평가표 전체를 처음부터 끝까지 읽고, 위 현재 직무/소속과 다른 실제 인사이동(전보·소속변경)이 "
            "명시적으로 서술되어 있는지 판단하세요. '~팀 업무 지원', '~팀과 협업' 같은 일시적 지원·협업 "
            "표현만 있는 경우는 인사이동이 아닙니다. 실제 인사이동이면 이전 직무명과 이전 소속팀명을 "
            "평가표 원문에서 그대로 찾아 답하세요.\n\n"
            "아래 형식으로만 답하고 다른 말은 붙이지 마세요:\n"
            "정답: <있음 또는 없음>\n"
            "이전직무: <있음일 때만 평가표 원문의 직무명, 없으면 (해당없음)>\n"
            "이전소속: <있음일 때만 평가표 원문의 소속팀명, 없으면 (해당없음)>\n"
            "확신도: <확실 또는 애매>"
        )
        parsed = _kv_parse(_call_llm(prompt), ["정답", "이전직무", "이전소속", "확신도"])
        if parsed is None:
            return None
        if parsed["정답"] not in ("있음", "없음"):
            return None
        if parsed["정답"] == "없음":
            return {"detected": False}
        return {"detected": True, "이전직무": parsed["이전직무"], "이전소속": parsed["이전소속"],
                "확신": parsed["확신도"].startswith("확실")}

    def narrate_current_state(self, V, candidate, top_gaps, persona, job_change=None):
        """S-03 — 04번 §2 S-03 + §6-2/6-4. 인과가 원문에 실제 있을 때만 인과
        연결어를 쓰고, 없으면 병렬 서술("별도로")로 되돌린다.

        persona(2026-09-16 추가) — 첫 문장은 "{직무명} {연차} {역할등급}는 ~하는 수준이
        기대됩니다" 형태로 역할·연차 기준 기대 수준을 먼저 요약해야 하는데, 이 요약 자체가
        파이프라인 어디에도 한 줄 문장으로 존재하지 않는다(페르소나 카드는 학습방향/현업행동/
        결과물 3개 항목뿐). S-04(narrate_persona)가 참고하는 것과 동일한 페르소나 카드를 여기도
        넘겨, 같은 기준으로 일관된 오프닝 문장을 만들게 한다.

        job_change(2026-09-16 추가, 원인3) — S-00이 인사이동을 감지하면 배너만
        띄우고, 본문(S-03)은 예전 직무 평가 내용과 현재 직무 Gap을 그냥 나란히만
        썼다(오재원 사례 — 왜 예전 ESG 평가가 지금 고객지원 직무에 의미가 있는지
        설명이 없음). detect_job_change가 이미 만들어 둔 이전직무/이전소속 정보를
        여기로도 넘겨, 그 연결을 한 문장으로 짚게 한다."""
        eval_ = candidate["평가표"] or {}
        gap_summary = "\n".join(f"- {report_facing_content(g)}" for g in top_gaps) or "(확정된 Gap 없음)"
        학습방향, 현업행동, 결과물 = persona
        role_line = f"{V['직무명']} {V['직무연차_표기']} {candidate['기본정보']['역할등급_표기']}"
        job_change_line = ""
        if job_change:
            job_change_line = (
                f"\n[인사이동 감지됨] 전년도 평가 당시 직무는 '{job_change['이전직무']}'"
                f"({job_change['이전소속']})였고, 현재 직무는 '{V['직무명']}'입니다. 전년도 평가 내용을 "
                f"인용할 때는, 그 내용(강점·보완사항)이 현재 '{V['직무명']}' 직무에 구체적으로 어떻게 "
                "연결·적용되는지(또는 왜 참고만 하고 그대로 적용하기 어려운지) 한 문장으로 반드시 "
                "짚어주세요 — 그냥 나란히 나열하지 마세요.\n"
            )
        prompt = (
            f"직원은 '{V['직무명']}' 직무를 {V['직무연차_표기']} 수행 중이며 역할 등급은 "
            f"'{candidate['기본정보']['역할등급_표기']}'입니다.\n\n"
            f"[이 역할·연차 기준 기대 수준(원문, 절대 값을 바꾸지 말 것)]\n"
            f"핵심 학습 방향: {학습방향}\n현업에서 보여야 할 행동: {현업행동}\n이 시기에 남겨야 할 결과물: {결과물}\n"
            f"{job_change_line}\n"
            f"[전년도 평가표 원문]\n{(eval_.get('원문_전체') or '')[:3000] or '(평가표 없음)'}\n\n"
            f"[현재 확정된 보완 필요 항목]\n{gap_summary}\n\n"
            f"위 정보로 '현재 상태 분석'을 작성하세요. 첫 문장은 반드시 \"{role_line}는 (위 기대 수준의 "
            "의미를 이 직무 맥락에 맞는 한 구절로 압축한 표현)하는 수준이 기대됩니다.\" 형태로, 역할·연차 "
            "기준 기대 수준을 한 문장으로 먼저 요약하세요(위 기대 수준 원문을 그대로 복붙하지 말고 의미만 "
            "유지해 압축할 것). 이어서 사실 문장을 추가해 전체 3~4문장으로 작성하세요. 오프닝 문장을 "
            "제외한 나머지 문장은 각 1개 사실만 담습니다. "
            "가능하면 ① 실제 이수한 교육 → ② 그 학습이 이어진 평가표상의 실제 성과·행동 → "
            "③ 그래서 어떤 기준을 충족/미충족으로 판단했는지 순서로 쓰되, 평가표 원문에 그 인과관계가 "
            "실제로 명시된 경우에만 '그래서/이어져/때문에/바탕으로' 같은 인과 연결어를 씁니다. "
            "두 사실 사이에 원문상 인과가 없으면 '별도로', '한편'으로 병렬 서술하세요. "
            "평가표의 강점·보완사항 근거를 인용할 때는 원문 그대로 따옴표로 인용하세요. "
            "'~인 것 같습니다' 같은 불확실한 표현, 승급/승격 등 인사 결정 관련 표현, 내부 코드나 "
            f"문서명은 절대 쓰지 마세요. {_NO_SCORE_INSTRUCTION} {_FORBIDDEN_PHRASE_INSTRUCTION}\n\n"
            "각 문장을 줄바꿈으로 구분해 '- '로 시작하는 목록으로만 답하세요(다른 말 붙이지 마세요)."
        )
        text = _call_llm(prompt)
        if text is None:
            return None
        lines = [re.sub(r"^-\s*", "", ln).strip() for ln in text.splitlines() if ln.strip()]
        lines = [ln for ln in lines if ln][:4]
        return lines or None

    def narrate_persona(self, V, job_name, persona):
        학습방향, 현업행동, 결과물 = persona
        prompt = (
            f"아래는 '{job_name}' 직무를 담당하는 직원의 Band·연차 기준 성장 이상향 정의(원문, 절대 값을 "
            f"바꾸지 말 것)입니다.\n핵심 학습 방향: {학습방향}\n현업에서 보여야 할 행동: {현업행동}\n"
            f"이 시기에 남겨야 할 결과물: {결과물}\n\n"
            "위 세 기준의 내용(의미)은 그대로 유지하되, 표현을 원문 그대로 복붙하지 말고 이 사람이 실제로 "
            f"담당하는 '{job_name}' 업무 맥락을 한 구절 이상 넣어 각각 2문장 이내로 풀어쓰세요. "
            f"내부 코드나 문서명은 쓰지 마세요. {_NO_SCORE_INSTRUCTION} {_FORBIDDEN_PHRASE_INSTRUCTION}\n\n"
            "아래 형식으로만 답하세요:\n학습방향: <2문장 이내>\n현업행동: <2문장 이내>\n결과물: <2문장 이내>"
        )
        parsed = _kv_parse(_call_llm(prompt), ["학습방향", "현업행동", "결과물"])
        return parsed

    def narrate_growth_point(self, V, candidate, gap, rank, merged_axes=None, avoid_names=None, category=None, cert_reference=None):
        """S-05 — 3단 구조(현재 상황/해결 행동/기대 변화) + 요약줄. merged_axes가
        있으면(2개 이상 GAP_QUAL 통합, 04번 §6-1) 여러 축을 하나의 포인트로 묶는다.

        avoid_names(2026-09-15 추가) — 자격(R01)/교육(R02)/페르소나(R05)처럼 서로
        다른 Gap이 같은 실제 근거(같은 교육과정명 등)를 가리킬 때, "영역명은 구체적
        이름이어야 한다"는 지시를 각자 충실히 따르다가 두 성장 포인트가 우연히
        똑같은 영역명으로 나와 STEP 2.5(S-05 영역명 중복)에서 떨어지는 실제 사례를
        발견했다. temperature=0라 재시도해도 똑같이 실패하므로, 이 리포트에서 이미
        쓰인 영역명을 프롬프트에 미리 알려줘 겹치지 않게 한다.

        category(2026-09-16 추가, 원인1) — 이 항목이 법정교육/자격/리더십교육/정성
        중 무엇인지. 지금까지 narrate_growth_point가 이 정보를 못 받아서 성격과
        무관하게 다 비슷한 톤으로 쓰였다.

        cert_reference(2026-09-16 추가, 원인2) — MGMT/ESG/HQ/ENERGY처럼 Gap 자체가
        "직무 연계 자격 1개 이상"처럼 구체적 자격명이 없는 본부에서, LLM이 실제로
        존재하는지 확인 안 된 자격명을 지어내는 문제(배우진 "재무설계사" 사례)의
        대응 — 직무기술서 필수/우대 자격 + 같은 직무군 동료 실제 보유 자격을
        참고자료로 주고, 그 목록 밖의 이름은 지어내지 말라고 명시적으로 못박는다."""
        eval_ = candidate["평가표"] or {}
        보완 = eval_.get("보완사항") or ""
        if merged_axes:
            content_desc = "; ".join(report_facing_content(g) for g in merged_axes)
        else:
            content_desc = report_facing_content(gap)
        avoid_line = (
            f"\n이미 다른 성장 포인트에서 다음 영역명을 썼습니다 — 이 이름들과 겹치지 않는, 이 항목만의 "
            f"구체적 이름을 쓰세요: {', '.join(avoid_names)}\n" if avoid_names else ""
        )
        category_line = f"\n[이 항목의 성격] {category} — {_category_tone_hint(category)}\n" if category else ""
        # 04_LLM프롬프트 §7-1 ④(재판단 금지)·§7-2(안전장치 3가지) 그대로
        # 반영(2026-09-16 정정 — 이전 버전은 "지어내지 마세요"만 있고 ①목록을
        # 재량으로 골라내지 말 것 ②인원 수를 리포트에 언급하지 말 것
        # ③"참고 사례·정답 아님·개설여부 별도확인" 톤을 명시적으로 요구하지
        # 않았다).
        reference_line = ""
        if cert_reference is not None:
            ref_parts = []
            jd = cert_reference.get("직무기술서")
            if jd:
                if jd.get("필수"):
                    ref_parts.append(f"직무기술서 필수 자격: {', '.join(jd['필수'])}")
                if jd.get("우대"):
                    ref_parts.append(f"직무기술서 우대 자격: {', '.join(jd['우대'])}")
                if jd.get("권장"):
                    ref_parts.append(f"직무기술서 권장 자격: {', '.join(jd['권장'])}")
            peer = cert_reference.get("동료사례")
            if peer and peer.get("certs"):
                ref_parts.append(f"같은 직무군 동료 실제 보유 자격(내부 참고용 — 인원 수는 아래 참고): {', '.join(peer['certs'])}")
            if ref_parts:
                reference_line = (
                    "\n[자격 참고자료 — 아래 목록은 이미 사내에서 실제로 보유가 확인된 자격입니다. "
                    "관련성이 낮아 보인다는 이유로 재량껏 걸러내지 말고, 목록에 있는 항목을 그대로 "
                    "구체적으로 언급하세요. 이 목록에 없는 자격명을 지어내지 마세요. '이 자격이 정답'이라는 "
                    "단정 톤이 아니라 '~가 참고 사례로 확인됩니다, 현재 개설 여부·최신 커리큘럼은 별도 확인이 "
                    "필요합니다'처럼 참고 사례 톤으로 쓰세요. 몇 명이 보유·이수했는지 인원 수는 리포트 문장에 "
                    "언급하지 마세요.]\n" + "\n".join(ref_parts) + "\n"
                )
            elif peer is not None and peer.get("peers_found", 0) < 2 and not jd:
                reference_line = (
                    "\n[자격 참고자료 없음] 같은 직무군 내 비교 가능한 동료가 2명 미만이라 사내 참고 사례가 "
                    "없습니다. [해결 행동] 또는 [현재 상황]에 '사내 유사 사례가 확인되지 않아 담당자의 별도 "
                    "조사가 필요합니다'라는 취지를 자연스럽게 포함하고, 특정 자격명을 지어내지 마세요.\n"
                )
            else:
                reference_line = (
                    "\n[자격 참고자료 없음 — 실제로 존재가 확인되지 않는 자격명을 지어내지 마세요. 특정 "
                    "자격명 대신 '직무 관련 전문자격 확인 필요'처럼 카테고리로만 언급하세요.]\n"
                )
        prompt = (
            f"직원은 '{V['직무명']}' 직무 담당, 역할 등급 '{candidate['기본정보']['역할등급_표기']}'입니다.\n"
            f"[확정된 보완 필요 항목]\n{content_desc}\n"
            f"{category_line}{reference_line}\n"
            f"[평가표 보완사항 원문]\n{보완 or '(없음)'}\n\n"
            f"[평가표 강점/역할/핵심가치 원문]\n{(eval_.get('강점') or '') + ' ' + (eval_.get('역할') or '') + ' ' + (eval_.get('핵심가치') or '')}\n"
            f"{avoid_line}\n"
            "이 보완 필요 항목에 대해 성장 포인트 하나를 아래 형식으로 작성하세요. 영역명은 자격명·교육명· "
            "평가 내용에서 직접 도출한 구체적 이름이어야 합니다(\"역량 강화\" 같은 포괄적 라벨 금지). "
            "요약줄과 [현재상황]은 서로 다른 문장이어야 합니다(요약줄=문제를 압축한 제목, [현재상황]=배경 "
            "설명). [현재상황]에는 위 원문 중 실제 관련된 문구를 따옴표로 그대로 인용하세요. [해결행동]은 "
            "'-합니다/-습니다' 같은 사실 서술형 어미로 끝내세요. [기대변화]는 "
            "반드시 '나는'을 주어로 하여, 이 사람의 실제 업무 맥락이 드러나는 구체적 문장으로 쓰되, "
            "'-할 수 있게 됩니다/-할 수 있습니다'처럼 보완 후 달라지는 능력·상태를 서술하는 어미로 끝내세요 "
            "(직무명만 바꾸면 다른 사람에게도 그대로 쓸 수 있는 문장 금지). [해결행동]과 [기대변화] 모두 "
            "'-겠습니다/-하겠다/-할 것입니다' 같은 다짐·의지형 어미(선언적 문장)는 금지입니다 — 이 사람이 "
            "보완점을 채우면 실제로 무엇을 할 수 있게 되는지 방향성을 서술하는 것이지, 하겠다는 다짐을 "
            "적는 것이 아닙니다. '~것 같습니다' 같은 불확실한 "
            "표현, 승급/승격 등 인사 결정 표현, 'KPI에 등록' 같은 행정적 결과 표현, 내부 코드는 금지입니다. "
            f"{_NO_SCORE_INSTRUCTION} {_FORBIDDEN_PHRASE_INSTRUCTION}\n\n"
            "아래 형식으로만 답하세요:\n영역명: <구체적 이름>\n요약줄: <1문장>\n현재상황: <1문장>\n"
            "해결행동: <-합니다/-습니다로 끝나는 1문장>\n기대변화: <나는 ~ -할 수 있게 됩니다로 끝나는 1문장>"
        )
        parsed = _kv_parse(_call_llm(prompt), ["영역명", "요약줄", "현재상황", "해결행동", "기대변화"])
        if parsed is None:
            return None
        return {"area": parsed["영역명"], "summary": parsed["요약줄"], "situation": parsed["현재상황"],
                "action": parsed["해결행동"], "change": parsed["기대변화"]}

    def narrate_recommended_action(self, V, growth_item, priority_rank, total_ranks, category=None):
        """category(2026-09-16 추가, 원인1) — 조하린 사례("법정교육/전문자격/
        리더십이 다 비슷한 '성과 정량화' 톤으로 써짐")의 원인은 이 함수가 이미
        서술된 문장(growth_item)만 받고 원래 Gap이 무슨 성격인지 정보 자체를
        못 받았기 때문이었다. 성격별로 다른 관점을 쓰도록 지시를 보강한다."""
        category_line = f"\n[이 항목의 성격] {category} — {_category_tone_hint(category)}\n" if category else ""
        prompt = (
            f"성장 포인트 '{growth_item['area']}'(현재 상황: {growth_item['situation']} / 해결 행동: "
            f"{growth_item['action']})에 대한 '추천 행동' 상세를 작성하세요. 이 항목은 {priority_rank}순위"
            f"({total_ranks}개 중)입니다.{category_line}\n"
            "아래 5개 항목을 각 1~2문장으로 작성하세요. 교육 우선순위는 1순위면 '지금 이 항목이 다른 것보다 "
            "선행되어야 하는 구체적 이유', 2순위 이후면 '1순위 완료 후 연계 학습으로 효과가 높은 이유'로 "
            "쓰세요. KPI 연계는 기대 결과물을 당해년도 KPI 목표로 전환하는 방향을 구체적으로 쓰세요("
            "'KPI에 등록' 같은 표현 대신 실제 활용 방향). [행동]과 [업무활용]은 '-합니다/-습니다' 같은 "
            "사실 서술형 어미로 쓰고, '-겠습니다/-하겠다/-할 것입니다' 같은 다짐·의지형 어미(선언적 문장)는 "
            "쓰지 마세요. 내부 코드·문서명은 쓰지 마세요. "
            f"{_NO_SCORE_INSTRUCTION} {_FORBIDDEN_PHRASE_INSTRUCTION}\n\n"
            "아래 형식으로만 답하세요:\n행동: <구체적 실행>\n기대결과물: <산출물 한글명 - 핵심 포함 요소>\n"
            "업무활용: <실제 업무 적용 1문장>\n교육우선순위: <시급성 이유 1문장>\nKPI연계: <전환 방향 1문장>"
        )
        parsed = _kv_parse(_call_llm(prompt), ["행동", "기대결과물", "업무활용", "교육우선순위", "KPI연계"])
        return parsed

    def narrate_extra_label(self, gap):
        prompt = (
            f"아래 보완 필요 항목에 대해 '추가 권장 사항' 항목 하나를 만드세요.\n항목: {report_facing_content(gap)}\n\n"
            f"{_NO_SCORE_INSTRUCTION} {_FORBIDDEN_PHRASE_INSTRUCTION}\n\n"
            "라벨(항목명, 5~15자)과 1문장 설명을 아래 형식으로만 답하세요:\n라벨: <이름>\n설명: <1문장>"
        )
        parsed = _kv_parse(_call_llm(prompt), ["라벨", "설명"])
        return parsed


# ---------------------------------------------------------------------------
# 결정론적 대체 문장(fallback) — 게이트웨이 호출이 실패했을 때만 쓴다. 그 사람의
# 실제 데이터(Gap 내용·평가표 인용)만 사용하고, 지어낸 판단은 절대 넣지 않는다.
# 사용될 때마다 generation_notes에 남겨 S-07에서 "AI 서술 생성 실패"로 노출한다.
# ---------------------------------------------------------------------------

def _fallback_growth_point(candidate, gap, merged_axes=None):
    eval_ = candidate["평가표"] or {}
    quote = first_sentence(eval_.get("보완사항") or eval_.get("원문_전체") or "")
    content = "; ".join(report_facing_content(g) for g in merged_axes) if merged_axes else report_facing_content(gap)
    situation = f'현재 기준 "{content}" 항목이 확인되지 않은 상태입니다.'
    if quote:
        situation += f' (평가표 근거: "{quote}")'
    return {
        "area": content,
        "summary": f"{content} 확인이 필요합니다.",
        "situation": situation,
        "action": f"{content}에 대한 이수·취득 계획을 담당자와 함께 수립합니다.",
        "change": f"나는 {content} 기준을 충족하여 현재 직무를 더 독립적으로 수행할 수 있게 됩니다.",
    }


def _fallback_recommended_action(growth_item):
    return {
        "행동": f"{growth_item['area']}에 필요한 교육 이수 또는 자격 취득 계획을 수립하고 실행합니다.",
        "기대결과물": f"계획서 — {growth_item['area']} 이수·취득 일정 포함",
        "업무활용": f"{growth_item['area']} 완료 후 현재 직무 수행에 직접 적용합니다.",
        "교육우선순위": "현재 확정된 보완 필요 항목이므로 우선 진행이 권장됩니다.",
        "KPI연계": "이 계획서를 당해년도 자기계발 KPI 목표로 활용할 수 있습니다.",
    }


def _fallback_persona(job_name, persona):
    학습방향, 현업행동, 결과물 = persona
    return {
        "학습방향": f"{job_name} 업무 맥락에서 {학습방향}",
        "현업행동": f"{job_name} 업무 수행 중 {현업행동}",
        "결과물": f"{job_name} 업무와 관련하여 {결과물}",
    }


def _fallback_current_state(V, candidate):
    b = candidate["기본정보"]
    lines = [f"{V['직무명']} 직무를 {V['직무연차_표기']} 수행 중이며, 역할 등급은 {b['역할등급_표기']}입니다."]
    eval_ = candidate["평가표"] or {}
    strength = first_sentence(eval_.get("강점") or "")
    if strength:
        lines.append(f'전년도 평가에서 "{strength}"가 강점으로 확인되었습니다.')
    return lines


# ---------------------------------------------------------------------------
# 리포트 조립
# ---------------------------------------------------------------------------

def _merge_qual_gaps(gaps):
    """04번 §6-1 — top3 중 GAP_QUAL이 2개 이상이면 하나의 성장 포인트로 통합.
    통합으로 개수가 3 미만이 되는 것은 정상(억지로 채우지 않음)."""
    quals = [g for g in gaps if g["code"] == "GAP_QUAL"]
    others = [g for g in gaps if g["code"] != "GAP_QUAL"]
    merged = []
    if len(quals) >= 2:
        merged.append({"code": "GAP_QUAL_MERGED", "content": "평가에서 확인된 보완 역량 강화",
                        "urgent": False, "_merged": quals})
    else:
        merged.extend(quals)
    result = others + merged
    result.sort(key=lambda g: -max((q.get("score", g.get("score", 0)) for q in g.get("_merged", [g]))))
    return result


def _dedupe_overlapping_gaps(gaps, judge, generation_notes):
    """원인1 대응(2026-09-16) — top3 중 카테고리가 달라도(자격/교육/리더십/정성)
    실질적으로 같은 스킬을 가리키는 항목이 섞여 나오는 문제. _merge_qual_gaps는
    "이름이 같은" GAP_QUAL끼리만 합치는데, 이건 이름이 아니라 "다른 규칙(R01/
    R02/R05)이 각자 감지한 서로 다른 코드의 내용이 실제로 겹치는" 경우라 별도
    판정이 필요하다. judge 없이는 내용이 겹치는지 판단할 수 없어 그대로 둔다
    (게이트웨이 미연결 시 이전과 동일하게 동작 — 회귀 아님)."""
    if judge is None or len(gaps) < 2:
        return gaps
    merged_away = [False] * len(gaps)
    result = []
    for i in range(len(gaps)):
        if merged_away[i]:
            continue
        group = [gaps[i]]
        for j in range(i + 1, len(gaps)):
            if merged_away[j]:
                continue
            verdict = judge.gaps_overlap(report_facing_content(gaps[i]), report_facing_content(gaps[j]))
            if verdict is not None and verdict.status == "CONFIRMED":
                group.append(gaps[j])
                merged_away[j] = True
        if len(group) > 1:
            combined_content = " / ".join(report_facing_content(g) for g in group)
            result.append({
                "code": "GAP_MERGED", "content": combined_content,
                "urgent": any(g.get("urgent") for g in group),
                "score": max(g.get("score", 0) for g in group), "_merged": group,
            })
            generation_notes.append(f"서로 다른 항목이 실질적으로 같은 내용으로 판정되어 하나로 통합됨({combined_content}) — 담당자 확인 필요")
        else:
            result.append(gaps[i])
    return result


def build_report(candidate, V, engine_out, judge=None, writer=None):
    """report_data(05_문장검증 스키마) + 실제 문장으로 채워진 report 딕셔너리를
    반환한다. writer가 None이면 모든 종합추론 항목이 fallback으로 채워진다."""
    generation_notes = []
    all_text = [FIXED_PURPOSE]
    b = candidate["기본정보"]
    eval_ = candidate["평가표"] or {}
    role_grade = b["역할등급_표기"]

    # --- S-00 인사이동 감지 ---
    s00 = None
    if candidate["평가표_찾음"] and writer is not None:
        verdict = writer.detect_job_change(V["직무명"], V["소속팀"], eval_.get("원문_전체") or "")
        if verdict is None:
            generation_notes.append("인사이동 감지 — AI 서술 생성 실패(게이트웨이 응답 없음), 담당자가 평가표를 직접 대조 필요")
        elif verdict.get("detected"):
            s00 = verdict
    elif candidate["평가표_찾음"] and writer is None:
        generation_notes.append("인사이동 감지 — 종합추론 필요(평가표 원문 대조), AI 서술 생성기 미연결")

    if s00:
        s00_text = (
            f"[안내] 직무 변경이 감지되었습니다 — 추천 내용 확인 필요\n"
            f"전년도 평가 직무: {s00['이전직무']} / {s00['이전소속']}\n"
            f"현재 직무: {V['직무명']} / {V['소속팀']} (현재 인사 데이터 기준)\n"
            "이 리포트의 교육·자격 추천 일부는 전년도 평가 데이터를 참조합니다. "
            "현재 직무에 최적화된 추천을 위해 담당자의 추가 조정을 권장합니다.\n"
            ">> 직무 변경으로 기준 연차를 조정하여 적용하였습니다."
        )
        all_text.append(s00_text)
    else:
        s00_text = None

    # --- S-01 기본정보 ---
    certs_formal = sorted({c.get("자격종류") for c in candidate["자격증"] if c.get("자격종류")})
    ax_count, ax_level = _engine_mod.classify_ax_level(V["교육이력"])
    if ax_count == 0 and ax_level is None:
        전체 = V.get("평가표_전체") or ""
        if any(kw in 전체 for kw in _engine_mod.AI_USAGE_KEYWORDS):
            ax_level = "L2_informal"
    ax_label = ax_level_to_korean(ax_count, ax_level)
    s01 = {
        "성명": b["성명"], "사번": b["사번"], "소속": V["소속팀"], "역할등급": role_grade,
        "담당직무": V["직무명"], "직무경험": V["직무연차_표기"],
        "보유자격증": certs_formal or ["없음"], "AX역량수준": ax_label,
    }

    # --- Gap 정리(top3 -> 성장 포인트, GAP_QUAL 통합 + 카테고리 간 내용 중복 통합) ---
    top3 = engine_out.get("top3_gaps", [])
    recommended = engine_out.get("recommended_gaps", [])
    growth_targets = _merge_qual_gaps(top3)[:3]
    growth_targets = _dedupe_overlapping_gaps(growth_targets, judge, generation_notes)

    job_group = _engine_mod.map_job_to_group(V["직무명"], V["소속팀"])

    s05_items = []
    for gap in growth_targets:
        merged_axes = gap.get("_merged")
        avoid_names = [it["area"] for it in s05_items]
        category = gap_category_label(gap)
        cert_reference = None
        if _needs_cert_reference(gap):
            # 원인2 — 직무기술서(talent_recommendation/job_structured_data.json)
            # 필수/우대 자격 + 같은 직무군 동료 실제 보유 자격을 함께 참고자료로 준다.
            peer_certs = _candidate_mod.get_job_group_peer_certs(job_group, exclude_사번=V["사번"]) if job_group != "unknown" else None
            cert_reference = {"직무기술서": _engine_mod.get_job_cert_reference(V["직무명"]), "동료사례": peer_certs}
        if writer is not None:
            item = writer.narrate_growth_point(V, candidate, gap, len(s05_items) + 1, merged_axes=merged_axes,
                                                avoid_names=avoid_names, category=category, cert_reference=cert_reference)
        else:
            item = None
        if item is not None and item["area"] in avoid_names:
            # avoid_names 지시를 줬는데도 겹치면(모델이 지시를 못 따른 경우) 뒤
            # 순번 항목에 순번을 붙여 최소한 STEP 2.5는 통과시킨다 — 그래도
            # S-07에 남겨 담당자가 실제로 겹치는 항목인지 확인하게 한다.
            item["area"] = f"{item['area']}({len(s05_items) + 1})"
            generation_notes.append(f"성장 포인트 영역명이 다른 항목과 겹쳐 자동으로 구분({item['area']}) — 담당자 확인 필요")
        if item is None:
            generation_notes.append(f"성장 포인트 서술 생성 실패({report_facing_content(gap)}) — 대체 문구 사용, 담당자 확인 필요")
            item = _fallback_growth_point(candidate, gap, merged_axes=merged_axes)
        item["_category"] = category
        # 2026-09-16 추가 — S-02 스냅샷이 이 area를 그대로 노출할지 판단하는 데
        # 씀(아래 참고). cert_reference가 쓰인 항목은 area가 "확정된 사실"이
        # 아니라 참고자료에서 LLM이 골라낸 추천 자격명이라 S-02에서 그대로
        # 단정 톤으로 보여주면 안 된다.
        item["_cert_reference_used"] = cert_reference is not None
        s05_items.append(item)
        all_text.extend([item["summary"], item["situation"], item["action"], item["change"]])

    # --- S-02 진단 스냅샷 ---
    row1 = f"{role_grade}로서 {V['직무명']} 직무를 {V['직무연차_표기']} 수행 중입니다."
    if growth_targets:
        # 04_LLM프롬프트 §2 S-02 행2·행3 — row2(행2)도 row3(행3)와 마찬가지로
        # narrate_growth_point가 이미 구체화한 s05_items[0]의 영역명을 그대로
        # 쓴다(2026-09-16 정정). 예전엔 여기 원본 Gap 라벨을 그대로 넣어서
        # "고객가치혁신 직무 심화 교육 미이수이 아직 확보되어 있지 않은
        # 상태입니다"처럼 "미이수"와 템플릿의 부정 표현이 겹쳐 어색해지고
        # (실사용 사례), GAP_QUAL 2개 이상 통합 시 "평가에서 확인된 보완 역량
        # 강화"라는 포괄 라벨이 구체 설명 없이 그대로 노출되는 문제가 있었다.
        # area는 이제 구체적이지만("AI 활용 공통역량(업무 도구 활용 단계)
        # 미확보"처럼) 그 자체로 이미 부정형 서술인 경우가 많아, 뒤에 "확보되어
        # 있지 않은 상태" 문구를 또 붙이면 부정이 두 번 겹치는 문제가 실제
        # 파일럿 데이터로 재현됐다(2026-09-16 재수정). area를 따옴표로 감싸
        # 그 자체의 표현(긍정형/부정형 어느 쪽이든)을 그대로 노출하고, 뒤에는
        # 항상 안전한 "항목이"(받침 있는 "항목" 고정이라 조사 오류 없음)로
        # 이어 붙여 area의 어미와 무관하게 항상 자연스럽다.
        #
        # 단, area가 cert_reference(참고자료) 경로로 나온 항목이면 예외다
        # (2026-09-16 추가, 사용자 확인) — 이 area는 "확정된 사실"이 아니라
        # LLM이 직무기술서·동료사례에서 골라낸 참고 자격명이라, S-05 카드
        # 본문에서만 "~가 참고 사례로 확인됩니다, 정답 아님" 톤으로 감싸 쓴다
        # (narrate_growth_point 프롬프트, 원인2). S-02는 그 감싸는 문맥 없이
        # area만 뽑아 쓰므로, 여기서는 구체 자격명 대신 원래 Gap 성격 그대로
        # 일반화된 표현을 쓴다 — §7 안전장치(정답 아님·재판단 금지)를 S-02까지
        # 일관되게 적용.
        def _s02_label(item):
            return "해당 직무에 대한 전문 자격증 취득" if item.get("_cert_reference_used") else item["area"]

        if s05_items[0].get("_cert_reference_used"):
            row2 = "해당 직무에 대한 전문 자격증 취득이 우선 보완 대상으로 확인되었습니다."
        else:
            row2 = f"'{s05_items[0]['area']}' 항목이 우선 보완 대상으로 확인되었습니다."
        # 상위 2개 항목이 둘 다 cert_reference 항목이면 동일한 일반화 문구가
        # 중복 표시될 수 있어 순서를 유지한 채 중복 제거.
        priority_labels = []
        for it in s05_items[:2]:
            label = _s02_label(it)
            if label not in priority_labels:
                priority_labels.append(label)
        row3 = ", ".join(priority_labels)
        row4 = s05_items[0]["change"]
        row5 = s05_items[0]["action"]
    else:
        row2 = "현재 확정된 보완 필요 항목이 없습니다."
        priority_labels = []
        row3 = row4 = row5 = "해당 없음"
    s02 = {"나의현재수준": row1, "지금채워야할부분": row2, "우선해결할것": row3,
           "채워지면달라지는것": row4, "올해활용방향": row5}
    all_text.extend([row1, row2, row3, row4, row5])

    # --- 페르소나 카드 조회(S-03 오프닝 문장과 S-04 본문이 함께 참고) ---
    band_num = _engine_mod.BAND_LEVEL[V["Band코드"]]
    persona = _engine_mod.PERSONA_CARDS[band_num][V["year_level"]]

    # --- S-03 현재 상태 분석 ---
    if writer is not None:
        s03 = writer.narrate_current_state(V, candidate, growth_targets, persona, job_change=s00)
    else:
        s03 = None
    if s03 is None:
        generation_notes.append("현재 상태 분석 서술 생성 실패 — 대체 문구 사용, 담당자 확인 필요")
        s03 = _fallback_current_state(V, candidate)
    all_text.extend(s03)

    # --- S-04 성장 이상향 ---
    s04 = writer.narrate_persona(V, V["직무명"], persona) if writer is not None else None
    if s04 is None:
        generation_notes.append("성장 이상향 서술 생성 실패 — 원문 기반 대체 문구 사용, 담당자 확인 필요")
        s04 = _fallback_persona(V["직무명"], persona)
    all_text.extend([s04["학습방향"], s04["현업행동"], s04["결과물"]])

    # --- S-06 추천 행동 ---
    s06_items = []
    for rank, item in enumerate(s05_items, start=1):
        if writer is not None:
            detail = writer.narrate_recommended_action(V, item, rank, len(s05_items), category=item.get("_category"))
        else:
            detail = None
        if detail is None:
            generation_notes.append(f"추천 행동 서술 생성 실패({item['area']}) — 대체 문구 사용, 담당자 확인 필요")
            detail = _fallback_recommended_action(item)
        s06_items.append({"title": item["area"], "action": detail["행동"], "artifact": detail["기대결과물"],
                           "usage": detail["업무활용"], "priority": detail["교육우선순위"], "kpi": detail["KPI연계"]})
        all_text.extend([detail["행동"], detail["기대결과물"], detail["업무활용"], detail["교육우선순위"], detail["KPI연계"]])

    used_titles = {it["title"] for it in s06_items}
    extra_gaps = [g for g in recommended if g["code"] != "GAP_QUAL" and report_facing_content(g) not in used_titles][:3]
    s06_extra = []
    for gap in extra_gaps:
        extra = writer.narrate_extra_label(gap) if writer is not None else None
        if extra is None:
            content = report_facing_content(gap)
            label, desc = content, f"{content} 이수·취득을 권장합니다."
        else:
            label, desc = extra["라벨"], extra["설명"]
        if label in used_titles or label in [e[0] for e in s06_extra]:
            continue
        s06_extra.append((label, desc))
        all_text.append(desc)

    # --- S-07 담당자 확인 사항 ---
    legal_courses = []
    for gap in top3 + recommended:
        legal_courses.extend(gap.get("미이수_교육") or [])
    s07_lines = []
    if s00:
        s07_lines.append(f"직무 변경이 감지되었습니다. 전년도에는 {s00['이전직무']}({s00['이전소속']}) 업무를 수행하였으나, 현재는 {V['직무명']} 직무로 변경되어 있습니다. 현재 직무 기준으로 교육·자격 추천의 적절성을 검토 후 확정하시기 바랍니다.")
        s07_lines.append("직무 변경으로 기준 연차를 조정하여 적용하였습니다.")
    if legal_courses:
        s07_lines.append(f"법정교육 이수 확인 필요: {', '.join(legal_courses)}의 이수 여부가 미완료로 확인되었습니다. 해당 직무의 이수 의무 대상 여부를 담당자가 확인 후 조치하시기 바랍니다.")
    for c in candidate.get("자격_재분류대기") or []:
        s07_lines.append(f"자격증 데이터 확인 필요: '{c.get('자격종류')}'이(가) 자격증현황에 등록되어 있으나 교육이수 기록으로 보입니다. 육성추천 화면의 확인 알림에서 처리해주세요(확인 시 자격증에서 제외되고 교육이력으로 반영됩니다).")
    for n in engine_out.get("notices", []):
        s07_lines.append(humanize(n))
    for n in engine_out.get("pending_judgments", []):
        s07_lines.append(humanize(n) + " (자동 판정 보류 — 담당자 확인 필요)")
    for note in generation_notes:
        s07_lines.append(note)
    s07_text = "\n".join(f"- {ln}" for ln in s07_lines) if s07_lines else "해당 없음"
    all_text.extend(s07_lines)

    report_data = {
        "fixed_purpose_text": FIXED_PURPOSE,
        "role_grade": role_grade,
        "certs_source_formal": certs_formal,
        "certs_shown": certs_formal,
        "certs_s07_required": [],
        "education_completed": [e.get("교육과정명") for e in V["교육이력"] if e.get("이수여부") == "Y" and e.get("교육과정명")],
        "education_s07_required": legal_courses,
        "s02_priority_labels": priority_labels,
        "s05_items": s05_items,
        "s06_items": [{"title": it["title"], "artifact": it["artifact"]} for it in s06_items],
        "s06_extra_labels": [label for label, _ in s06_extra],
        "s07_text": s07_text,
        "all_text": all_text,
    }

    report = {"s00": s00_text, "s01": s01, "s02": s02, "s03": s03, "s04": s04,
              "s05_items": s05_items, "s06_items": s06_items, "s06_extra": s06_extra,
              "s07_text": s07_text, "s07_lines": s07_lines}

    return {"report": report, "report_data": report_data, "generation_notes": generation_notes}


def generate_report_with_verification(candidate, V, engine_out, judge=None, writer=None, max_retries=1):
    """report_data가 05_문장검증.run_step_2_5를 통과해야만 STEP 8(PDF, Phase 5)로
    넘긴다 — 08번 문서 "실행 결과 판정 기준" 원칙 그대로. FAIL이 있으면 1회 재생성
    후 그래도 실패하면 FAIL 목록과 함께 그대로 반환한다(건너뛰지 않음)."""
    result = build_report(candidate, V, engine_out, judge=judge, writer=writer)
    fails = _verify_mod.run_step_2_5(result["report_data"])
    attempt = 0
    while fails and attempt < max_retries:
        attempt += 1
        result = build_report(candidate, V, engine_out, judge=judge, writer=writer)
        fails = _verify_mod.run_step_2_5(result["report_data"])
    result["step_2_5_fails"] = fails
    result["step_2_5_passed"] = not fails
    return result


def main():
    judge = _judge_mod.LLMJudge()
    writer = ReportWriter()
    gateway_ok = _call_llm("연결 확인용 메시지입니다. '정답: 확인' 이라고만 답하세요.") is not None
    if not gateway_ok:
        print(f"⚠ 사내 AI 게이트웨이({_judge_mod.LLM_BASE_URL})에 연결할 수 없습니다 — 예상된 샌드박스 제약입니다.")
        print("  아래는 게이트웨이 없이도 안전한 대체 문구로 리포트가 만들어지고, STEP 2.5 검증까지 도는지 확인하는 실행입니다.\n")

    for 사번 in _candidate_mod._PILOT_사번:
        candidate = _candidate_mod.build_candidate(사번)
        V = _engine_mod.build_V(candidate)
        engine_out = _engine_mod.run_rule_engine(V, judge=judge)
        b = candidate["기본정보"]
        if "error" in engine_out:
            print(f"=== {사번} {b['성명']} — 검증 실패: {engine_out['error']} ===\n")
            continue
        result = generate_report_with_verification(candidate, V, engine_out, judge=judge, writer=writer)
        status = "PASS" if result["step_2_5_passed"] else f"FAIL {len(result['step_2_5_fails'])}건"
        print(f"=== {사번} {b['성명']} ({b['역할등급_표기']}, {V['직무명']}) — STEP 2.5: {status} ===")
        print(f"  성장 포인트 {len(result['report']['s05_items'])}건, AI 서술 생성 실패(대체 사용) {len(result['generation_notes'])}건")
        if result["step_2_5_fails"]:
            for f in result["step_2_5_fails"]:
                print("    FAIL:", f)
        print()


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/05_문장검증.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
05_문장검증.py — 육성추천 Phase 4-2: 문장 품질 자동 검증(STEP 2.5)
=========================================
design_docs/08_리포트생성지시서.docx PART 4-7에 이미 파이썬 코드로 통째로
포함되어 있던 검증 스크립트를 그대로 옮긴 것이다. 04번 문서 §5(문장 생성 방식
원칙)를 "성실히 확인했다"는 서술이 아니라 기계적으로 검사하기 위한 절차이며,
08번 문서 자체가 "이 스크립트 전문을 그대로 옮겨쓴다"고 명시하고 있어 로직을
임의로 바꾸지 않았다 — 아래 check_* 함수·FORBIDDEN_* 목록·FIXED_PURPOSE 문구는
전부 08번 PART 4-7 원문 그대로다.

이 파일이 새로 추가한 것은 check_template_reuse(08번 원문에도 있음, 배치 1회
실행용)와 pilot 대상 self-test용 main()뿐이다.

STEP 8(PDF 생성, Phase 5)은 run_step_2_5()가 빈 리스트([])를 반환할 때만
진행한다 — 08번 PART 4-7 "실행 결과 판정 기준" 원문 그대로.
"""

import re

# ---------------------------------------------------------------------------
# 08_리포트생성지시서 PART 4-7 원문 그대로 — 임의 수정 금지(04번이 개정되면
# 08번과 함께 이 파일도 갱신한다).
# ---------------------------------------------------------------------------

FORBIDDEN_TERMS = [
    r"VL-\d+", r"GAP_[A-Z_]+", r"year_level", r"온톨로지", r"규칙엔진", r"\bRAG\b",
    r"페르소나\s*(카드|축|이상향)", r"\d+점\b", r"CK-\d+", r"Q-0\d", r"R-0\d", r"ART_[A-Z_]+", r"COMP_[A-Z_]+",
    r"\bL1\b", r"\bL2\b", r"\bL3\b", r"L2_informal", r"05번", r"06번", r"07번", r"09번",
    r"원본 재조회", r"\b(customer|planning|mgmt|gas|energy|hr|safety|new_biz|ai_it|finance)\b",
    # 2026-09-16 추가 — 04_LLM프롬프트가 금지하는 "내부 3축 분류명"(01_온톨로지
    # F-01 페르소나 3축의 원문 코드명). "학습방향"처럼 띄어쓰기 없는 압축 표기는
    # 리포트가 실제로 쓰는 자연어 표현("핵심 학습 방향" 등, 띄어쓰기 있음)과 겹치지
    # 않는 내부 전용 용어라 오탐 없이 안전하게 잡을 수 있다 — 02_규칙엔진 R-05가
    # gap 내용에 이 원문을 직접 박아 fallback 경로로 새던 실제 사례가 있었다.
    r"학습방향", r"현업행동", r"기대산출물",
]
# 08번 문서 원문은 r"페르소나"(단어 전체)를 그대로 금지했으나, 2026-09-15 실제
# 게이트웨이 결과에서 실직원의 AI 프로젝트 서술("다각적 관점의 페르소나...")이
# 오탐되는 걸 사용자가 발견했다 — "페르소나"는 STEP 6(01_온톨로지 "페르소나
# 이상향")이 가리키는 내부 설계 용어인 동시에 AX/UX 분야의 정상적인 실무 용어라
# 겹친다. 내부 설계 문서·화면에서만 쓰는 복합어("페르소나 카드/축/이상향")로
# 좁혀서, 직원이 실제로 페르소나 작업을 했다는 서술은 통과시키고 내부 설계
# 용어 유출만 잡는다.

# 어간 단위로 검사 — 활용형(예: 지적되었지만, 지적된다)까지 전부 잡기 위함
FORBIDDEN_PHRASE_STEMS = ["역량을 강화", "성장에 도움이", "KPI에 등록", "부족합니", "미달입니", "지적되"]

# 2026-09-16 신규 추가(08번 문서 원문에는 없음) — S-02/S-05/S-06 문장이 "~하겠습니다"
# 같은 다짐·의지형(선언적) 어미로 끝나는 사례가 실사용 리포트에서 발견됐다. [기대변화]는
# "보완점을 채우면 실제로 무엇을 할 수 있게 되는지"를 서술해야 하는데, 다짐형 어미는
# 방향성 서술이 아니라 결심 선언문이 되어버린다. 04_리포트서술.py의 narrate_growth_point/
# narrate_recommended_action 프롬프트에 어미 규칙을 추가한 것과 짝을 이루는 회귀 방지용
# 검사 — 프롬프트 지시를 LLM이 안 따라도 여기서 걸러 담당자 확인으로 넘긴다.
PLEDGE_ENDING_STEMS = ["겠습니다", "하겠다", "할 것입니다", "할 것이다"]


def check_internal_terms(all_text):
    """따옴표(" ") 안의 평가표 원문 인용은 검사에서 제외한다 — check_forbidden_
    phrases와 동일한 이유(04_LLM프롬프트 §6-2): 작성자의 서술이 아니라 원문 그대로
    인용된 부분이라서다. 2026-09-15 실제 사례로 발견 — "점수(숫자+점) 표시 금지"는
    08번 문서의 명시적 원칙이지만, 이 원칙이 막으려는 건 LLM이 지어내거나 내부
    판정 점수를 새로 서술하는 것이지, 평가표에 실제로 적힌 외부 지표(예: GRESB
    점수)를 원문 그대로 인용하는 것까지 막으려는 게 아니다 — 그런 인용은 이미
    "원문 그대로 따옴표로 인용하라"는 지시를 따른 결과이기 때문이다."""
    fails = []
    for t in all_text:
        t_no_quotes = re.sub(r'"[^"]*"', "", t)
        for pat in FORBIDDEN_TERMS:
            m = re.search(pat, t_no_quotes)
            if m:
                # 매칭된 위치 주변만 보여준다 — 문장 앞 40자만 자르면(예전 방식)
                # 정작 매칭이 뒤쪽에 있을 때 어디서 걸렸는지 전혀 안 보이는 문제가
                # 있었다(2026-09-15, 사용자가 실제 오류 메시지 보고 발견).
                s, e = max(0, m.start() - 20), min(len(t_no_quotes), m.end() + 20)
                excerpt = ("..." if s > 0 else "") + t_no_quotes[s:e] + ("..." if e < len(t_no_quotes) else "")
                fails.append(f"내부용어 노출: '{pat}' 패턴이 '{excerpt}'에서 발견")
    return fails


def check_forbidden_phrases(all_text):
    """따옴표(" ") 안의 평가표 원문 인용은 검사에서 제외한다 — 04_LLM프롬프트 §6-2
    참조. 원문 자체의 표현이지 작성자의 서술이 아니기 때문이다."""
    fails = []
    for t in all_text:
        t_no_quotes = re.sub(r'"[^"]*"', "", t)
        for p in FORBIDDEN_PHRASE_STEMS:
            if p in t_no_quotes:
                fails.append(f"금지표현: '{p}' 어간이 '{t[:40]}...'에서 발견")
    return fails


def check_pledge_ending(all_text):
    """따옴표(" ") 안의 평가표 원문 인용은 검사에서 제외한다 — check_forbidden_phrases와
    동일한 이유. PLEDGE_ENDING_STEMS 참고 — S-02/S-05/S-06 서술이 다짐형 어미로 끝나면
    실패 처리한다."""
    fails = []
    for t in all_text:
        t_no_quotes = re.sub(r'"[^"]*"', "", t)
        for p in PLEDGE_ENDING_STEMS:
            if p in t_no_quotes:
                fails.append(f"다짐형 어미(선언적 문장): '{p}' 가 '{t[:40]}...'에서 발견 — 방향성 서술로 수정 필요")
    return fails


def check_label_dup(report_data):
    fails = []
    labels = report_data["s02_priority_labels"]
    dups = set(x for x in labels if labels.count(x) > 1)
    if dups:
        fails.append(f"S-02 '우선 해결할 것' 라벨 중복: {dups}")

    areas = [it["area"] for it in report_data["s05_items"]]
    dups = set(x for x in areas if areas.count(x) > 1)
    if dups:
        fails.append(f"S-05 영역명 중복: {dups}")

    titles = [it["title"] for it in report_data["s06_items"]]
    dups = set(x for x in titles if titles.count(x) > 1)
    if dups:
        fails.append(f"S-06 제목 중복: {dups}")

    artifacts = [it["artifact"] for it in report_data["s06_items"]]
    dups = set(x for x in artifacts if artifacts.count(x) > 1)
    if dups:
        fails.append(f"S-06 기대 결과물 중복: {dups}")

    extra = report_data.get("s06_extra_labels", [])
    dups = set(x for x in extra if extra.count(x) > 1)
    if dups:
        fails.append(f"추가 권장 사항 라벨 중복: {dups}")

    # TOP3(S-06 제목)와 추가 권장 사항 라벨이 같은 단어를 공유하는지 간단 점검(완전
    # 동일 라벨만 자동 검출 가능, 주제 중복 여부는 04번 §5-7에 따라 작성자가
    # 육안으로도 재확인)
    overlap = set(titles) & set(extra)
    if overlap:
        fails.append(f"TOP3 추천 행동과 추가 권장 사항 라벨이 동일: {overlap}")
    return fails


def check_summary_situation_dup(report_data):
    fails = []
    for it in report_data["s05_items"]:
        if it["summary"].strip() == it["situation"].strip():
            fails.append(f"S-05 요약줄과 [현재 상황]이 동일: '{it['area']}'")
    return fails


def check_cert_coverage(report_data):
    fails = []
    source = report_data.get("certs_source_formal", [])
    shown = report_data.get("certs_shown", [])
    missing_s01 = [c for c in source if c not in shown]
    if missing_s01:
        fails.append(f"S-01 자격 누락: 06 정식 취득 자격 {len(source)}건 중 {len(source)-len(missing_s01)}건 표시 — 누락: {missing_s01}")

    required_s07 = report_data.get("certs_s07_required", [])
    s07 = report_data.get("s07_text", "")
    missing_s07 = [c for c in required_s07 if c not in s07]
    if missing_s07:
        fails.append(f"S-07 필수 자격 설명 누락: {missing_s07}")
    return fails


def check_education_coverage(report_data):
    # 교육 전건은 실행로그에서 조회·판정하되, 최종 S-07은 면담에 필요한 항목만 설명한다.
    fails = []
    required = report_data.get("education_s07_required", [])
    if not required:
        return fails
    s07 = report_data.get("s07_text", "")
    missing = [e for e in required if e not in s07]
    if missing:
        fails.append(f"S-07 필수 교육 설명 누락: {missing}")
    return fails


FIXED_PURPOSE = (
    "본 리포트는 구성원의 성장 방향과 교육·육성 필요사항을 확인하기 위한 팀장 면담용 참고자료입니다. "
    "개인의 강점과 보완 필요영역을 바탕으로 향후 성장계획 및 목표수립 시 활용하시기 바랍니다."
)


def check_fixed_purpose(report_data):
    value = report_data.get("fixed_purpose_text", "").strip()
    if value != FIXED_PURPOSE:
        return ["고정 목적문구 누락 또는 변형: 제목 아래 목적문구를 10번 템플릿의 고정문구와 동일하게 사용해야 함"]
    return []


_ROLE_GRADE_LEVEL_TO_NAME = {
    1: "입문실무자", 2: "기초독립실무자", 3: "실무주도자", 4: "직무리더",
    5: "팀리더", 6: "본부직무총괄", 7: "전사조직총괄", 8: "전사리더",
}
# 직급코드 자체가 "B3(6)"처럼 괄호를 포함하고 있어("역할명(직급코드)"로 감싸면
# "팀리더(B3(6))"처럼 괄호가 중첩된다), 바깥쪽 괄호와 안쪽 괄호를 구분하려면
# 코드 형식(B숫자(숫자))을 그대로 정규식에 박아야 한다 — [^)]+ 같은 일반화된
# 패턴은 안쪽 ')'에서 바깥쪽 매칭이 깨진다(실제로 이렇게 깨졌던 걸 발견·수정).
_ROLE_GRADE_RE = re.compile(r"^(.+)\((B\d\((\d+)\))\)$")


def check_role_grade_format(report_data):
    """v1.3(08번 문서)은 괄호 안을 "밴드약칭"(사원~책임)으로 검사했으나, 2026-09-14
    사용자가 리포트를 검토하고 사내에서 통용되는 원본 직급코드("B2(5)")로
    표시해달라고 명시적으로 요청해 검사 기준을 그렇게 바꿨다(01_후보자통합.
    parse_band_grade 변경과 짝을 이룸). 여전히 "정확한 역할명-코드 조합"인지는
    검사한다 — 괄호 안이 뭐든 통과시키던 v1.2 이전 상태로 되돌아간 게 아니다."""
    value = report_data["role_grade"]
    m = _ROLE_GRADE_RE.match(value)
    if not m:
        return [f"역할 등급 표기 형식 오류: '{value}' — '역할명(B숫자(숫자))' 형식이 아님"]
    role_name, grade_num = m.group(1), m.group(3)
    level_num = int(grade_num) - 1  # 01_후보자통합.parse_band_grade와 동일한 오프셋 보정
    expected_role = _ROLE_GRADE_LEVEL_TO_NAME.get(level_num)
    if expected_role is None:
        return [f"역할 등급 표기 오류: '{value}' — 직급코드가 사원~책임 8단계 범위를 벗어남"]
    if role_name != expected_role:
        return [f"역할 등급 표기 오류: '{value}' — 이 직급코드에는 '{expected_role}'만 결합 가능"]
    return []


def run_step_2_5(report_data):
    fails = []
    fails += check_internal_terms(report_data["all_text"])
    fails += check_forbidden_phrases(report_data["all_text"])
    fails += check_pledge_ending(report_data["all_text"])
    fails += check_label_dup(report_data)
    fails += check_summary_situation_dup(report_data)
    fails += check_fixed_purpose(report_data)
    fails += check_cert_coverage(report_data)
    fails += check_education_coverage(report_data)
    fails += check_role_grade_format(report_data)
    return fails


def check_template_reuse(all_people_report_data, job_titles):
    """200명 전원 생성 후 1회 실행 — 여러 사람의 '기대 변화' 문장에서 직무명만
    다르고 나머지가 동일하면 템플릿 재사용으로 간주."""
    fails = []
    seen = {}
    for eid, data in all_people_report_data.items():
        job = job_titles[eid]
        for it in data["s05_items"]:
            norm = it["change"].replace(job, "__JOB__")
            if norm in seen and seen[norm][0] != eid:
                fails.append(f"템플릿 재사용 의심: {seen[norm][0]}·{eid} — '{it['change'][:40]}...'")
            seen[norm] = (eid, it["change"])
    return fails


# ---------------------------------------------------------------------------
# self-test — report_data 스키마가 맞는 정상 케이스가 실제로 PASS하는지,
# 08번 예시의 위반 케이스(내부용어·중복·요약줄 반복)가 실제로 FAIL을 잡아내는지
# 구조 검증. 실제 리포트 검증은 04_리포트서술.py에서 이 모듈을 import해 실행한다.
# ---------------------------------------------------------------------------

def _sample_ok_report_data():
    return {
        "fixed_purpose_text": FIXED_PURPOSE,
        "role_grade": "팀리더(B3(6))",
        "certs_source_formal": ["가스산업기사"],
        "certs_shown": ["가스산업기사"],
        "certs_s07_required": [],
        "education_completed": ["생성형 AI 역량강화"],
        "education_s07_required": [],
        "s02_priority_labels": ["가스기사 취득", "AX 심화교육 이수"],
        "s05_items": [
            {"area": "가스기사 취득", "summary": "가스기사 취득이 아직 확인되지 않습니다.",
             "situation": "가스산업기사는 보유하고 있으나 상위 자격인 가스기사는 없어 팀리더 등급 기준 전문성 심화가 더 필요한 상태입니다.",
             "action": "가스기사 취득을 위한 학습 계획을 수립하고 응시 일정을 확정합니다.",
             "change": "나는 배관 안전 기준을 직접 해석하고 현장 판단을 독립적으로 수행할 수 있게 됩니다."},
        ],
        "s06_items": [
            {"title": "가스기사 취득 계획 수립", "artifact": "검토서 — 응시 일정 포함"},
        ],
        "s06_extra_labels": ["AX 실무 교육 이수"],
        "s07_text": "해당 없음.",
        "all_text": [
            FIXED_PURPOSE, "가스기사 취득이 아직 확인되지 않습니다.",
            "나는 배관 안전 기준을 직접 해석하고 현장 판단을 독립적으로 수행할 수 있게 됩니다.",
        ],
    }


def main():
    ok = _sample_ok_report_data()
    fails = run_step_2_5(ok)
    print(f"[정상 케이스] FAIL {len(fails)}건 (0건이어야 정상)")
    for f in fails:
        print("  -", f)

    bad = dict(ok)
    bad["all_text"] = ok["all_text"] + ["이 사람은 온톨로지 기준 GAP_CERT_GAS_2 판정으로 역량을 강화해야 합니다."]
    bad["s02_priority_labels"] = ["가스기사 취득", "가스기사 취득"]
    fails = run_step_2_5(bad)
    print(f"\n[위반 케이스] FAIL {len(fails)}건 (0건보다 많아야 정상 — 검증기가 실제로 잡아내는지 확인)")
    for f in fails:
        print("  -", f)

    # 2026-09-15 실사용 오탐 사례 재현 — 인용부호 예외 처리와 "페르소나" 패턴
    # 좁히기가 실제로 의도대로 동작하는지 확인.
    quote_ok = dict(ok)
    quote_ok["all_text"] = ok["all_text"] + [
        '별도로 ESG 대응체계 정비를 통해 "GRESB 평가에서 3점을 획득"한 것으로 평가표에 기록되어 있습니다.',
        "다각적 관점의 페르소나를 고려해 AI 프로젝트 관리 도구를 설계했습니다.",
    ]
    fails = run_step_2_5(quote_ok)
    print(f"\n[인용구·정상용어 케이스] FAIL {len(fails)}건 (0건이어야 정상 — 원문 인용 점수·실무 용어 '페르소나'는 통과해야 함)")
    for f in fails:
        print("  -", f)

    leak_ok = dict(ok)
    leak_ok["all_text"] = ok["all_text"] + [
        "이 사람은 최근 평가에서 80점 만점을 획득했습니다.",  # 인용부호 없이 LLM이 직접 점수를 서술 — 여전히 잡혀야 함
        "성장 페르소나 카드 기준으로 다음 단계를 안내합니다.",  # 내부 설계 용어 — 여전히 잡혀야 함
    ]
    fails = run_step_2_5(leak_ok)
    print(f"\n[내부용어 실제 유출 케이스] FAIL {len(fails)}건 (0건보다 많아야 정상 — 인용 예외가 과하게 넓어지지 않았는지 확인)")
    for f in fails:
        print("  -", f)

    # 2026-09-16 추가 — 다짐형 어미(선언적 문장) 검사가 실제로 잡아내는지, 원문 인용은
    # 여전히 통과하는지 확인.
    pledge_bad = dict(ok)
    pledge_bad["all_text"] = ok["all_text"] + ["나는 배관 안전 기준을 직접 해석하고 현장 판단을 독립적으로 수행하겠습니다."]
    fails = run_step_2_5(pledge_bad)
    print(f"\n[다짐형 어미 위반 케이스] FAIL {len(fails)}건 (0건보다 많아야 정상)")
    for f in fails:
        print("  -", f)

    pledge_quote_ok = dict(ok)
    pledge_quote_ok["all_text"] = ok["all_text"] + ['평가표 보완사항 원문에는 "반드시 완료하겠습니다"라고 적혀 있습니다.']
    fails = run_step_2_5(pledge_quote_ok)
    print(f"\n[다짐형 어미·원문 인용 케이스] FAIL {len(fails)}건 (0건이어야 정상 — 원문 인용은 통과해야 함)")
    for f in fails:
        print("  -", f)


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/06_PDF생성.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
06_PDF생성.py — 육성추천 Phase 5: PDF 생성
=========================================
design_docs/10_리포트_레이아웃_템플릿.docx PART 5의 ReportLab 스크립트(v1.8)를
그대로 이식한다. 색상 팔레트·컴포넌트 함수(section_header/plain_block/
tinted_block/info_grid_2x2/label_value_table_tinted/rank_card/action_card/
indented_paragraph/grey_note_block)·페이지 분할 방지 규칙(KeepTogether)·
S-05 2페이지 자동 폰트축소 로직은 10번 문서 원문 그대로이며, 임의로 바꾸지
않았다 — "이 문서에 정의된 색상 코드·여백값·폰트 크기·컴포넌트 함수는
고정값입니다. LLM은 이 값을 근거 없이 변경하지 않습니다"(10번 PART 0).

[10번 문서와 다르게 이 파일이 새로 한 것]
  - 폰트 파일: 08번 문서는 "맑은고딕 라이선스 제약 → Noto Sans KR 대체"라고만
    지시하고 실제 파일은 /tmp에 있다고 가정했다. 이 저장소엔 그런 폰트 파일이
    전혀 없었어서(다른 어떤 스크립트도 reportlab을 쓴 적이 없음 — 이동/승급은
    PDF 자동변환 자체를 뺐고, 직책자만 wkhtmltopdf를 쓴다), Google Fonts의
    Noto Sans KR 가변폰트(공식 배포본)를 내려받아 Regular/Bold 정적 인스턴스로
    변환하고, 한글 완성형(가~힣)·자모·라틴·리포트에서 실제 쓰는 기호(★※·—…
    따옴표 등)만 남기는 서브셋을 만들어 growth_recommendation/assets/fonts/에
    커밋해두었다(각 2.8MB, 총 5.6MB — 원본 가변폰트 10.4MB보다 훨씬 작다).
    ReportLab의 TTFont는 CFF(OpenType/PostScript) 외곽선을 지원하지 않아
    시스템에 있던 Noto Sans CJK(.ttc, CFF 기반)는 쓸 수 없었다 — 반드시 glyf
    기반 TrueType이어야 해서 별도로 준비했다.
  - to_pdf_data(): 04_리포트서술.build_report()가 만든 report 딕셔너리를 이
    파일의 generate_report(data, ...)가 기대하는 스키마로 변환하는 어댑터.
    10번 문서 자체에는 이 변환 로직이 없다(design_docs가 "data 딕셔너리는
    매번 채워 넣는다"고만 명시했을 뿐, 그 조립 방법은 04번 산출값과 연결하는
    이 프로젝트의 몫이다).

STEP 8(PDF 생성)은 08번 문서 STEP 7.5(문장 품질 자동 검증) 통과 후에만
진행한다 — generate_pdf_for_candidate()가 이 순서를 강제한다.
"""

import json
import sys
from datetime import date
from pathlib import Path

from reportlab.lib import colors
from reportlab.lib.pagesizes import A4
from reportlab.lib.styles import ParagraphStyle
from reportlab.lib.units import mm
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont
from reportlab.platypus import KeepTogether, PageBreak, Paragraph, SimpleDocTemplate, Spacer, Table, TableStyle

BASE_DIR = Path(__file__).resolve().parent
ROOT_DIR = BASE_DIR.parent
for _p in (str(ROOT_DIR), str(ROOT_DIR / "talent_recommendation"), str(BASE_DIR)):
    if _p not in sys.path:
        sys.path.insert(0, _p)

import importlib
_candidate_mod = importlib.import_module("01_후보자통합")
_engine_mod = importlib.import_module("02_규칙엔진")
_judge_mod = importlib.import_module("03_규칙엔진_LLM판정")
_writer_mod = importlib.import_module("04_리포트서술")

FONT_DIR = BASE_DIR / "assets" / "fonts"
_FONTS_REGISTERED = False


def _register_fonts():
    global _FONTS_REGISTERED
    if _FONTS_REGISTERED:
        return
    pdfmetrics.registerFont(TTFont("NotoKR", str(FONT_DIR / "NotoSansKR-Regular.ttf")))
    pdfmetrics.registerFont(TTFont("NotoKR-Bold", str(FONT_DIR / "NotoSansKR-Bold.ttf")))
    _FONTS_REGISTERED = True


# ============================================================
# 1. 레이아웃 상수 — 10번 문서 PART 1 실측 확정값 그대로(임의 변경 금지)
# ============================================================
PAGE_W, PAGE_H = A4
MARGIN_LR = 14 * mm
MARGIN_TB = 6 * mm
W = PAGE_W - 2 * MARGIN_LR
CW = W - 20 * mm  # 카드 좌우 패딩(10mm x 2) 차감 후 텍스트 너비

NAVY = colors.HexColor("#1B3A6B")
ORANGE_BG = colors.HexColor("#FDF0E8")
ORANGE_ACCENT = colors.HexColor("#E8722A")
BLUE_BG = colors.HexColor("#EBF4FB")
BLUE_BORDER = colors.HexColor("#CCDDEE")
BLUE_ACCENT = colors.HexColor("#2E6DB4")
GREEN_BG = colors.HexColor("#E8F5EE")
GREEN_ACCENT = colors.HexColor("#2E7D4F")
GREY_BG = colors.HexColor("#F5F5F5")
GREY_BORDER = colors.HexColor("#B7B7B7")
PLAIN_BORDER = colors.HexColor("#9FB0C3")
TEXT_DARK = colors.HexColor("#262626")
TEXT_GREY = colors.HexColor("#5A5A5A")

FIXED_REPORT_PURPOSE = (
    "본 리포트는 구성원의 성장 방향과 교육·육성 필요사항을 확인하기 위한 팀장 면담용 참고자료입니다. "
    "개인의 강점과 보완 필요영역을 바탕으로 향후 성장계획 및 목표수립 시 활용하시기 바랍니다."
)

RANK_PALETTE = [(ORANGE_BG, ORANGE_ACCENT), (BLUE_BG, BLUE_ACCENT), (GREEN_BG, GREEN_ACCENT)]


def _styles():
    """폰트 등록 이후에만 ParagraphStyle을 만들 수 있어(fontName 검증) 지연 생성한다."""
    _register_fonts()
    return {
        "title": ParagraphStyle("title", fontName="NotoKR-Bold", fontSize=16.5, leading=21, textColor=NAVY, wordWrap="LTR"),
        "title_meta": ParagraphStyle("title_meta", fontName="NotoKR", fontSize=9, leading=12, textColor=TEXT_GREY, alignment=2, wordWrap="LTR"),
        "header_bar": ParagraphStyle("header_bar", fontName="NotoKR-Bold", fontSize=12.3, leading=15, textColor=colors.white, wordWrap="LTR"),
        "body": ParagraphStyle("body", fontName="NotoKR", fontSize=9.0, leading=12.6, textColor=TEXT_DARK, wordWrap="LTR"),
        "body_bold": ParagraphStyle("body_bold", fontName="NotoKR-Bold", fontSize=9.0, leading=12.6, textColor=NAVY, wordWrap="LTR"),
        "label_navy": ParagraphStyle("label_navy", fontName="NotoKR-Bold", fontSize=9.2, leading=13, textColor=NAVY, wordWrap="LTR"),
        "accent_orange": ParagraphStyle("accent_o", fontName="NotoKR-Bold", fontSize=10.2, leading=14, textColor=ORANGE_ACCENT, wordWrap="LTR"),
        "rank_badge": ParagraphStyle("rank_badge", fontName="NotoKR-Bold", fontSize=10.5, leading=14, wordWrap="LTR"),
        "small_grey": ParagraphStyle("small_grey", fontName="NotoKR", fontSize=7.8, leading=11, textColor=TEXT_GREY, wordWrap="LTR"),
        "footer": ParagraphStyle("footer", fontName="NotoKR", fontSize=7.6, leading=10.5, textColor=TEXT_GREY, alignment=1, wordWrap="LTR"),
        "s04_intro": ParagraphStyle("s04_intro", fontName="NotoKR-Bold", fontSize=10.0, leading=14, textColor=NAVY, wordWrap="LTR"),
    }


# ============================================================
# 2. 재사용 컴포넌트 함수 — 10번 문서 PART 3 그대로
#    (모든 카드형 요소는 단일 Table, 중첩 없음 — 08번 PDF 기술 수칙 준수)
# ============================================================

def _P(text, style):
    return Paragraph(text, style)


def section_header(title_text, S):
    t = Table([[_P(title_text, S["header_bar"])]], colWidths=[CW])
    t.setStyle(TableStyle([
        ("BACKGROUND", (0, 0), (-1, -1), NAVY),
        ("LEFTPADDING", (0, 0), (-1, -1), 10 * mm), ("RIGHTPADDING", (0, 0), (-1, -1), 10 * mm),
        ("TOPPADDING", (0, 0), (-1, -1), 4), ("BOTTOMPADDING", (0, 0), (-1, -1), 4),
        ("VALIGN", (0, 0), (-1, -1), "TOP"), ("WORDWRAP", (0, 0), (-1, -1), "LTR"),
    ]))
    return t


def plain_block(paragraphs, border=PLAIN_BORDER):
    t = Table([[p] for p in paragraphs], colWidths=[CW])
    ts = [
        ("VALIGN", (0, 0), (-1, -1), "TOP"),
        ("LEFTPADDING", (0, 0), (-1, -1), 10 * mm), ("RIGHTPADDING", (0, 0), (-1, -1), 10 * mm),
        ("TOPPADDING", (0, 0), (-1, 0), 4), ("BOTTOMPADDING", (0, -1), (-1, -1), 4),
        ("TOPPADDING", (0, 1), (-1, -1), 3), ("BOTTOMPADDING", (0, 0), (-1, -2), 3),
        ("WORDWRAP", (0, 0), (-1, -1), "LTR"),
    ]
    if border:
        ts.append(("BOX", (0, 0), (-1, -1), 1.2, border))
    t.setStyle(TableStyle(ts))
    return t


def tinted_block(paragraphs, bg, border=None, min_height=None):
    t = Table([[p] for p in paragraphs], colWidths=[CW])
    ts = [
        ("BACKGROUND", (0, 0), (-1, -1), bg),
        ("VALIGN", (0, 0), (-1, -1), "TOP"),
        ("LEFTPADDING", (0, 0), (-1, -1), 10 * mm), ("RIGHTPADDING", (0, 0), (-1, -1), 10 * mm),
        ("TOPPADDING", (0, 0), (-1, 0), 4), ("BOTTOMPADDING", (0, -1), (-1, -1), 4),
        ("TOPPADDING", (0, 1), (-1, -1), 3), ("BOTTOMPADDING", (0, 0), (-1, -2), 3),
        ("WORDWRAP", (0, 0), (-1, -1), "LTR"),
    ]
    if border:
        ts.append(("BOX", (0, 0), (-1, -1), 1.2, border))
    t.setStyle(TableStyle(ts))
    if min_height:
        natural_h = t.wrap(CW, 5000)[1]
        if natural_h < min_height:
            ts.append(("BOTTOMPADDING", (0, -1), (-1, -1), 4 + (min_height - natural_h)))
            t.setStyle(TableStyle(ts))
    return t


def info_grid_2x2(pairs, S):
    data = []
    for (l1, v1), (l2, v2) in pairs:
        data.append([_P(f"<b>{l1}</b>", S["label_navy"]), _P(v1, S["body"]),
                     _P(f"<b>{l2}</b>", S["label_navy"]), _P(v2, S["body"])])
    colw = [CW * 0.15, CW * 0.35, CW * 0.15, CW * 0.35]
    t = Table(data, colWidths=colw)
    t.setStyle(TableStyle([
        ("VALIGN", (0, 0), (-1, -1), "TOP"),
        ("LEFTPADDING", (0, 0), (-1, -1), 10 * mm), ("RIGHTPADDING", (0, 0), (-1, -1), 4),
        ("TOPPADDING", (0, 0), (-1, -1), 5), ("BOTTOMPADDING", (0, 0), (-1, -1), 5),
        ("WORDWRAP", (0, 0), (-1, -1), "LTR"),
        ("GRID", (0, 0), (-1, -1), 0.6, PLAIN_BORDER), ("BOX", (0, 0), (-1, -1), 1.2, PLAIN_BORDER),
    ]))
    return t


def label_value_table_tinted(rows, S, label_w=0.24):
    data = [[_P(f"<b>{l}</b>", S["label_navy"]), _P(v, S["body"])] for l, v in rows]
    t = Table(data, colWidths=[CW * label_w, CW * (1 - label_w)])
    t.setStyle(TableStyle([
        ("BACKGROUND", (0, 0), (0, -1), BLUE_BG),
        ("VALIGN", (0, 0), (-1, -1), "TOP"),
        ("LEFTPADDING", (0, 0), (-1, -1), 8), ("RIGHTPADDING", (0, 0), (-1, -1), 8),
        ("TOPPADDING", (0, 0), (-1, -1), 4), ("BOTTOMPADDING", (0, 0), (-1, -1), 4),
        ("GRID", (0, 0), (-1, -1), 0.5, BLUE_BORDER), ("BOX", (0, 0), (-1, -1), 1.2, BLUE_ACCENT),
        ("WORDWRAP", (0, 0), (-1, -1), "LTR"),
    ]))
    return t


def rank_card(rank_no, area, summary, situation, action, change, S, min_height=None, font_scale=1.0):
    bg, accent = RANK_PALETTE[(rank_no - 1) % len(RANK_PALETTE)]
    if font_scale >= 0.999:
        badge_style, body_style = S["rank_badge"], S["body"]
    else:
        badge_style = ParagraphStyle(f"rank_badge_{rank_no}_{font_scale}", parent=S["rank_badge"],
                                      fontSize=S["rank_badge"].fontSize * font_scale, leading=S["rank_badge"].leading * font_scale)
        body_style = ParagraphStyle(f"body_{rank_no}_{font_scale}", parent=S["body"],
                                     fontSize=S["body"].fontSize * font_scale, leading=S["body"].leading * font_scale)
    badge_style = ParagraphStyle(f"rank_badge_{rank_no}_{font_scale}_c", parent=badge_style, textColor=accent)
    rows = [
        _P(f"{rank_no}순위 │ {area}", badge_style),
        _P(summary, body_style),
        _P(f"<b>[현재 상황]</b> {situation}", body_style),
        _P(f"<b>[해결 행동]</b> {action}", body_style),
        _P(f"<b>[기대 변화]</b> {change}", body_style),
    ]
    return tinted_block(rows, bg, border=accent, min_height=min_height)


def indented_paragraph(paragraph, pad_left=8, pad_right=8):
    t = Table([[paragraph]], colWidths=[CW])
    t.setStyle(TableStyle([
        ("LEFTPADDING", (0, 0), (-1, -1), pad_left), ("RIGHTPADDING", (0, 0), (-1, -1), pad_right),
        ("TOPPADDING", (0, 0), (-1, -1), 0), ("BOTTOMPADDING", (0, 0), (-1, -1), 3),
        ("VALIGN", (0, 0), (-1, -1), "TOP"), ("WORDWRAP", (0, 0), (-1, -1), "LTR"),
    ]))
    return t


def action_card(no, title, action, artifact, usage, edu_priority, kpi, S):
    title_p = indented_paragraph(_P(f"({no}) {title}", S["body_bold"]))
    rows = [("행동", action), ("기대 결과물", artifact), ("업무 활용", usage),
            ("[★] 교육 우선순위", edu_priority), ("KPI 연계", kpi)]
    return KeepTogether([title_p, label_value_table_tinted(rows, S, label_w=0.22)])


def grey_note_block(title, bullets, S):
    FIXED_NOTE = ("아래 항목은 우선순위상 뒤에 있을 뿐 성장에 꼭 필요한 영역입니다. "
                  "구성원과의 피드백 대화에서 1~3순위 다음 단계로 함께 언급해 주시기 바랍니다.")
    rows = [_P(FIXED_NOTE, S["small_grey"]), _P(f"<b>{title}</b>", S["body_bold"])]
    rows += [_P(f"· {b}", S["body"]) for b in bullets]
    return tinted_block(rows, GREY_BG, border=GREY_BORDER)


# ============================================================
# 3. 리포트 생성 함수 — 10번 문서 PART 5 generate_report() 그대로 이식
#    (페이지 구성 고정: 1p=S-01~S-04, 2p=S-05 전용, 3p~=S-06·S-07 — v1.6)
# ============================================================

def generate_report(data, output_path):
    S = _styles()
    story = []

    title_row = Table([[_P(data["report_title"], S["title"]), _P(data["title_meta"], S["title_meta"])]],
                       colWidths=[CW * 0.6, CW * 0.4])
    title_row.setStyle(TableStyle([
        ("VALIGN", (0, 0), (-1, -1), "BOTTOM"),
        ("LEFTPADDING", (0, 0), (-1, -1), 0), ("RIGHTPADDING", (0, 0), (-1, -1), 0),
    ]))
    story.append(title_row)
    story.append(Spacer(1, 6))

    story.append(tinted_block([_P(FIXED_REPORT_PURPOSE, S["body"])], BLUE_BG, border=BLUE_BORDER))
    story.append(Spacer(1, 6))

    if data.get("s00_notice"):
        rows = [_P(data["s00_notice"]["title"], S["accent_orange"])]
        rows += [_P(line, S["body"]) for line in data["s00_notice"]["lines"]]
        story.append(tinted_block(rows, ORANGE_BG, border=ORANGE_ACCENT))
        story.append(Spacer(1, 6))

    story.append(KeepTogether([section_header(data["s01_title"], S), info_grid_2x2(data["s01_pairs"], S)]))
    story.append(Spacer(1, 6))

    story.append(KeepTogether([section_header(data["s02_title"], S), label_value_table_tinted(data["s02_rows"], S)]))
    story.append(Spacer(1, 6))

    story.append(KeepTogether([section_header(data["s03_title"], S), plain_block([_P(t, S["body"]) for t in data["s03_paragraphs"]])]))
    story.append(Spacer(1, 6))

    s04_body = label_value_table_tinted(data["s04_rows"], S, label_w=0.24) if data.get("s04_as_table", True) \
        else plain_block([_P(f"<b>{l}</b> {v}", S["body"]) for l, v in data["s04_rows"]])
    story.append(KeepTogether([
        section_header(data["s04_title"], S), Spacer(1, 6),
        indented_paragraph(_P(data["s04_intro"], S["s04_intro"])), Spacer(1, 8), s04_body,
    ]))
    story.append(PageBreak())  # v1.5 — 1페이지는 S-01~S-04까지만

    # --- S-05 성장 포인트: 2페이지 전용, 자동 폰트축소로 한 페이지에 맞춤(v1.6) ---
    gp_list = data["s05_growth_points"]
    BOX_GAP = 10
    MIN_FONT_SCALE = 0.80

    if gp_list:
        header_h = section_header(data["s05_title"], S).wrap(CW, 5000)[1]
        n_boxes = len(gp_list)
        SAFETY_MARGIN = 18
        usable_h = (PAGE_H - 2 * MARGIN_TB) - header_h - BOX_GAP * n_boxes - Spacer(1, 2).wrap(CW, 5000)[1] - SAFETY_MARGIN

        def build_cards(scale):
            return [rank_card(i, gp["area"], gp["summary"], gp["situation"], gp["action"], gp["change"], S, font_scale=scale)
                    for i, gp in enumerate(gp_list, start=1)]

        chosen_scale = None
        scale = 1.0
        while scale >= MIN_FONT_SCALE - 1e-9:
            cards = build_cards(scale)
            total_natural = sum(c.wrap(CW, 5000)[1] for c in cards)
            if total_natural <= usable_h:
                chosen_scale = scale
                break
            scale = round(scale - 0.05, 2)

        if chosen_scale is not None:
            natural_heights = [c.wrap(CW, 5000)[1] for c in build_cards(chosen_scale)]
            extra_each = max(usable_h - sum(natural_heights), 0) / n_boxes
            target_heights = [h + extra_each for h in natural_heights]
            gp0 = gp_list[0]
            story.append(KeepTogether([
                section_header(data["s05_title"], S), Spacer(1, 2),
                rank_card(1, gp0["area"], gp0["summary"], gp0["situation"], gp0["action"], gp0["change"], S,
                          min_height=target_heights[0], font_scale=chosen_scale),
            ]))
            story.append(Spacer(1, BOX_GAP))
            for idx, gp in enumerate(gp_list[1:], start=1):
                story.append(rank_card(idx + 1, gp["area"], gp["summary"], gp["situation"], gp["action"], gp["change"], S,
                                        min_height=target_heights[idx], font_scale=chosen_scale))
                story.append(Spacer(1, BOX_GAP))
        else:
            # 최소 축소(0.80)로도 안 들어갈 만큼 내용이 김 — 축소 포기, 자연스럽게 다음 페이지로 흘려보냄
            gp0 = gp_list[0]
            story.append(KeepTogether([
                section_header(data["s05_title"], S), Spacer(1, 2),
                rank_card(1, gp0["area"], gp0["summary"], gp0["situation"], gp0["action"], gp0["change"], S),
            ]))
            story.append(Spacer(1, BOX_GAP))
            for i, gp in enumerate(gp_list[1:], start=2):
                story.append(KeepTogether([rank_card(i, gp["area"], gp["summary"], gp["situation"], gp["action"], gp["change"], S)]))
                story.append(Spacer(1, BOX_GAP))
        story.append(PageBreak())  # v1.5 — 3페이지부터 S-06 시작
    else:
        # v1.8 — Gap 0개일 때 S-05 생략 방지, 중립 안내카드로 대체
        story.append(KeepTogether([
            section_header(data["s05_title"], S), Spacer(1, 2),
            tinted_block([_P("현재 기준 대비 Gap 없음", S["body_bold"])], GREY_BG, border=GREY_BORDER),
        ]))
        story.append(Spacer(1, 10))

    # --- S-06 성장을 위한 추천 행동 ---
    act_list = data["s06_actions"]
    if act_list:
        act0 = act_list[0]
        story.append(KeepTogether([
            section_header(data["s06_title"], S), Spacer(1, 2),
            action_card(1, act0["title"], act0["action"], act0["artifact"], act0["usage"], act0["edu_priority"], act0["kpi"], S),
        ]))
        story.append(Spacer(1, 10))
        for i, act in enumerate(act_list[1:], start=2):
            story.append(action_card(i, act["title"], act["action"], act["artifact"], act["usage"], act["edu_priority"], act["kpi"], S))
            story.append(Spacer(1, 10))
    if data.get("s06_extra"):
        story.append(grey_note_block(data["s06_extra"]["title"], data["s06_extra"]["bullets"], S))
        story.append(Spacer(1, 6))

    # --- S-07 담당자 확인 사항 + 꼬리말 (한 블록으로 묶어 쪽 분리 방지) ---
    footer_block = [
        section_header(data["s07_title"], S),
        plain_block([_P(f"· {b}", S["body"]) for b in data["s07_bullets"]]),
        Spacer(1, 4), _P(data["disclaimer"], S["small_grey"]), Spacer(1, 2), _P(data["footer_meta"], S["footer"]),
    ]
    story.append(KeepTogether(footer_block))

    doc = SimpleDocTemplate(str(output_path), pagesize=A4, leftMargin=MARGIN_LR, rightMargin=MARGIN_LR,
                             topMargin=MARGIN_TB, bottomMargin=MARGIN_TB)
    doc.build(story)
    return output_path


# ============================================================
# 4. 04_리포트서술.build_report() 산출값 -> generate_report() 입력 스키마 어댑터
# ============================================================

PROMPT_VERSION = "v2.11"  # design_docs/04_LLM프롬프트.docx 현재 버전 — 하드코딩 대신 여기 한 곳에서 관리


def to_pdf_data(candidate, V, result):
    report = result["report"]
    b = candidate["기본정보"]
    s01 = report["s01"]

    s00_notice = None
    if report["s00"]:
        lines = report["s00"].split("\n")
        s00_notice = {"title": lines[0], "lines": lines[1:]}

    s02 = report["s02"]
    s02_rows = [
        ("나의 현재 수준", s02["나의현재수준"]), ("지금 채워야 할 부분", s02["지금채워야할부분"]),
        ("우선 해결할 것", s02["우선해결할것"]), ("채워지면 달라지는 것", s02["채워지면달라지는것"]),
        ("올해 활용 방향", s02["올해활용방향"]),
    ]

    s04 = report["s04"]
    s04_rows = [("핵심 학습 방향", s04["학습방향"]), ("현업에서 보여야 할 행동", s04["현업행동"]),
                ("이 시기에 남겨야 할 결과물", s04["결과물"])]

    s06_actions = [{"title": it["title"], "action": it["action"], "artifact": it["artifact"],
                     "usage": it["usage"], "edu_priority": it["priority"], "kpi": it["kpi"]}
                    for it in report["s06_items"]]
    s06_extra = None
    if report["s06_extra"]:
        s06_extra = {"title": "추가 권장 사항", "bullets": [f"{label}: {desc}" for label, desc in report["s06_extra"]]}

    s07_bullets = report["s07_lines"] or ["해당 없음"]

    year = V.get("기준연도") or date.today().year
    return {
        "report_title": "개인별 교육·육성 진단 리포트",
        "title_meta": f"CNCITY에너지 / 인사·조직문화팀 / {year}",
        "s00_notice": s00_notice,
        "s01_title": "S-01 기본 정보",
        "s01_pairs": [
            (("성명", s01["성명"]), ("사번", s01["사번"])),
            (("소속", s01["소속"]), ("역할 등급", s01["역할등급"])),
            (("담당 직무", s01["담당직무"]), ("직무 경험", s01["직무경험"])),
            (("보유 자격증", ", ".join(s01["보유자격증"])), ("AX 역량 수준", s01["AX역량수준"])),
        ],
        "s02_title": "S-02 한눈에 보는 핵심 요약",
        "s02_rows": s02_rows,
        "s03_title": "S-03 현재 상태 분석",
        "s03_paragraphs": report["s03"],
        "s04_title": "S-04 나의 성장 이상향",
        "s04_intro": f"{s01['역할등급']} 역할 등급의 {V['year_level']}년차 기준에서 {s01['담당직무']} 업무 수행 시 갖춰야 할 모습입니다.",
        "s04_as_table": True,
        "s04_rows": s04_rows,
        "s05_title": "S-05 성장 포인트",
        "s05_growth_points": report["s05_items"],
        "s06_title": "S-06 성장을 위한 추천 행동",
        "s06_actions": s06_actions,
        "s06_extra": s06_extra,
        "s07_title": "S-07 담당자 확인 사항",
        "s07_bullets": s07_bullets,
        "disclaimer": ("본 리포트는 내부 교육 기준에 따라 자동 생성되었습니다. 최종 육성 방향은 담당자 검토 후 확정합니다. "
                        "교육 우선순위 안내는 역량 개발계획 수립 참조용입니다."),
        "footer_meta": f"생성일: {date.today().strftime('%Y년 %m월 %d일')} │ 대상: {b['사번']} {b['성명']} │ "
                        f"리포트 양식 {PROMPT_VERSION} │ CNCITY에너지 인사/조직문화팀",
    }


def generate_pdf_for_candidate(사번, output_dir, judge=None, writer=None, year=2025):
    """STEP 7.5(문장 품질 자동 검증) 통과 후에만 STEP 8(PDF 생성)로 진행한다 —
    08번 문서 원칙 그대로. FAIL이 남아 있으면 PDF를 만들지 않고 예외를 낸다."""
    candidate = _candidate_mod.build_candidate(사번, year=year)
    V = _engine_mod.build_V(candidate, year=year)
    engine_out = _engine_mod.run_rule_engine(V, judge=judge)
    if "error" in engine_out:
        raise ValueError(f"{사번}: 입력 검증 실패 — {engine_out['error']}")
    result = _writer_mod.generate_report_with_verification(candidate, V, engine_out, judge=judge, writer=writer)
    if not result["step_2_5_passed"]:
        raise ValueError(f"{사번}: STEP 2.5 검증 실패로 PDF 생성 중단 — {result['step_2_5_fails']}")
    data = to_pdf_data(candidate, V, result)
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)
    stem = f"{사번}_{candidate['기본정보']['성명']}_육성리포트"
    out_path = output_dir / f"{stem}.pdf"
    generate_report(data, out_path)
    # PDF와 같은 내용을 화면에서 iframe 없이 바로 보여주기 위해(2026-09-15,
    # 사용자 요청 — 인재추천 결과 화면처럼 이름 선택 -> 상세 화면), PDF가 실제로
    # 그리는 것과 완전히 같은 to_pdf_data() 출력을 그대로 JSON으로도 남긴다.
    # 별도 스키마를 새로 만들지 않아 PDF와 화면 내용이 어긋날 일이 없다.
    (output_dir / f"{stem}.json").write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return out_path, result, candidate


def main():
    out_dir = BASE_DIR / "_생성결과"
    judge = _judge_mod.LLMJudge()
    writer = _writer_mod.ReportWriter()
    gateway_ok = _judge_mod._call_llm("연결 확인용 메시지입니다. '정답: 확인' 이라고만 답하세요.") is not None
    if not gateway_ok:
        print(f"⚠ 사내 AI 게이트웨이({_judge_mod.LLM_BASE_URL})에 연결할 수 없습니다 — 예상된 샌드박스 제약입니다.")
        print("  아래는 대체 문구 기반으로도 STEP 2.5를 통과하고 PDF가 정상 생성되는지 확인하는 실행입니다.\n")

    for 사번 in _candidate_mod._PILOT_사번:
        try:
            path, result, _candidate = generate_pdf_for_candidate(사번, out_dir, judge=judge, writer=writer)
        except ValueError as e:
            print(f"  실패: {e}")
            continue
        print(f"  생성 완료: {path.name} (성장포인트 {len(result['report']['s05_items'])}건, 대체문구 {len(result['generation_notes'])}건)")


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/07_결과연동.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
07_결과연동.py — 육성추천 Phase 6: 플랫폼 통합(출력 연동)
=========================================
01~06번이 만드는 "사번 하나당 PDF 리포트 1개"를 실제 플랫폼에 연결하는 계층이다.
서버(talent_recommendation/server.py)가 이 파일의 함수만 호출하면 되고, 대상자
목록 조회·배치 실행·결과 저장·기존 화면 연동까지 전부 이 파일이 책임진다.

[이 파일이 새로 연결하는 지점 — 대화에서 사용자와 이미 확인한 내용]
  1) talent_recommendation/육성추천_결과_샘플.json — 원래 다른 담당자가 수기로
     채워둔 예시 파일이었는데, 이 파일을 소비하는 27_공통_육성연계매핑.py와
     hr_card의 "육성추천현황" 탭이 기대하는 내용이 사실 이 새 파이프라인의
     출력과 정확히 같은 것임을 확인했다(대상자 10명이 이 파이프라인의
     _PILOT_사번과 동일, 추천행동/기대결과물/우선순위 필드도 우리 S-06 항목과
     1:1 대응). 그래서 파일명·소비 측 코드(27번의 YUKSEONG_FILE, hr_card
     service.py의 YUKSEONG_RESULT)는 그대로 두고, 이 파일이 실행할 때마다
     "대상자" 배열 중 이번에 처리한 사번만 실제 산출값으로 갱신한다(나머지
     사번은 건드리지 않음 — 아직 전 사원을 대상으로 돌리기 전이므로).
     최상위에 "_생성정보" 메타 키를 추가해 더 이상 손으로 쓴 예시가 아니라
     실제 파이프라인 산출값임을 남긴다(27번·hr_card 둘 다 "대상자" 키만
     읽으므로 이 메타 키는 기존 소비 코드에 영향 없음).
  2) growth_recommendation/output/index.json — 사번별로 "언제 어떤 리포트를
     만들었는지"의 이력. 인사카드(hr_card)에 새로 추가하는 "교육육성 리포트"
     탭(연도별 발행 PDF 목록)이 이 인덱스를 읽는다.
  3) PDF 실 파일 — growth_recommendation/output/{연도}/{사번}_{성명}_육성리포트.pdf
"""

import concurrent.futures
import io
import json
import sys
import zipfile
from datetime import date
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
ROOT_DIR = BASE_DIR.parent
for _p in (str(ROOT_DIR), str(ROOT_DIR / "talent_recommendation"), str(BASE_DIR)):
    if _p not in sys.path:
        sys.path.insert(0, _p)

import importlib
_candidate_mod = importlib.import_module("01_후보자통합")
_engine_mod = importlib.import_module("02_규칙엔진")
_judge_mod = importlib.import_module("03_규칙엔진_LLM판정")
_writer_mod = importlib.import_module("04_리포트서술")
_pdf_mod = importlib.import_module("06_PDF생성")
_verify_mod = importlib.import_module("05_문장검증")

OUTPUT_DIR = BASE_DIR / "output"
INDEX_FILE = OUTPUT_DIR / "index.json"
YUKSEONG_SAMPLE_FILE = ROOT_DIR / "talent_recommendation" / "육성추천_결과_샘플.json"


def list_roster(year=2025):
    """대상자 선택 화면용 — 공통01_인사종합 전체 인원(01_후보자통합과 동일한
    소스)에서 선택에 필요한 최소 필드를 뽑는다. year 기준 평가표 유무도 함께
    표시한다(2026-09-14, 사용자 요청 — 평가표 없는 사람을 미리 알 수 있게).
    파일 존재만 확인하므로 비용은 무시할 만하다."""
    df = _candidate_mod._load_roster()
    rows = []
    for _, row in df.iterrows():
        사번 = _candidate_mod._clean(row.get("사번"))
        if not 사번:
            continue
        _, _, role_label = _candidate_mod.parse_band_grade(row.get("직급명칭"))
        성명 = _candidate_mod._clean(row.get("성명")) or ""
        rows.append({
            "사번": str(사번).strip(),
            "성명": 성명,
            "소속팀": _candidate_mod._clean(row.get("부서명")) or "",
            # "본부명" 컬럼은 원본 데이터에서 전부 빈 값 — 실제 본부는 "상위부서명"에
            # 있다(01_후보자통합.build_candidate와 같은 수정, 2026-09-15).
            "본부": _candidate_mod._clean(row.get("상위부서명")) or "",
            "직무명": _candidate_mod._clean(row.get("직무명")) or "",
            "역할등급": role_label or "",
            "평가표있음": _candidate_mod.find_evaluation_file(성명, year=year) is not None if 성명 else False,
        })
    return rows


def resolve_name(사번):
    """평가표 조회·업로드 API가 사번만 받았을 때 파일명 규칙(성명 기준)에 쓸
    성명을 찾는다 — 프론트엔드가 잘못 보낸 이름으로 엉뚱한 파일을 만들지
    않도록 항상 서버 쪽 명부로 재확인한다."""
    for p in list_roster():
        if p["사번"] == 사번:
            return p["성명"]
    return None


def list_cert_reclassify_candidates():
    """육성추천 화면 우측 알림창용 — 전체 인원의 자격증현황에서 아직 담당자
    확인을 거치지 않은 "~양성교육이수" 오분류 항목을 전부 모은다(인재추천의
    "육성연계 검토대기" 알림과 같은 성격 — 리포트를 실행하지 않아도 화면
    진입 시 바로 보인다)."""
    candidates = []
    for person in list_roster():
        사번 = person["사번"]
        for c in _candidate_mod.get_certifications(사번):
            if _candidate_mod._is_training_completion_cert(c):
                candidates.append({
                    "사번": 사번, "성명": person["성명"],
                    "자격종류": c.get("자격종류"), "취득일자": c.get("취득일자"),
                })
    return candidates


def confirm_cert_reclassify(사번, 자격종류):
    """hr_data 원본(공통05_자격현황/공통04_교육이력)에서 실제로 행을 옮긴다
    (2026-09-15) — 인사카드 등 이 두 파일을 읽는 모든 화면이 재실행 없이 바로
    같은 결과를 본다."""
    _candidate_mod.reclassify_cert_to_education(사번, 자격종류)
    return list_cert_reclassify_candidates()


# ---------------------------------------------------------------------------
# 평가표(.docx) 파일 관리 — 인사카드 "연도별 평가" 탭의 "평가표 보기"/"업로드"가
# 이 함수들을 쓴다(2026-09-14, 사용자 요청 — 육성추천 화면이 아니라 인사카드
# 에서 관리). 파일 자체는 계속 growth_recommendation/평가표/에 둔다 — 이 파일을
# 실제로 읽어 쓰는 01_후보자통합.build_candidate가 참조하는 경로와 같아야
# 업로드하자마자 다음 리포트 생성에 바로 반영되기 때문이다.
# ---------------------------------------------------------------------------

_EVAL_FILENAME_RE = __import__("re").compile(r"^(.+) 평가표\((\d{4})\)\.docx$")


def list_evaluation_years(성명):
    """이 사람 이름으로 된 평가표 파일이 있는 연도 전체(최신순)."""
    years = []
    for path in _candidate_mod.EVAL_DIR.glob(f"{성명} 평가표(*).docx"):
        m = _EVAL_FILENAME_RE.match(path.name)
        if m and m.group(1) == 성명:
            years.append(int(m.group(2)))
    return sorted(set(years), reverse=True)


def get_evaluation_content(성명, year):
    """"평가표 보기" 버튼용 — 파싱된 텍스트(업적/강점/보완사항/역할/핵심가치)를
    반환한다. 원본 문구를 그대로 보여줘야 하므로(요약 금지 원칙, 03_규칙엔진
    3-1-0 근거와 동일한 이유) parse_evaluation_docx가 만드는 원문 그대로 쓴다."""
    path = _candidate_mod.find_evaluation_file(성명, year=year)
    if path is None:
        return None
    return _candidate_mod.parse_evaluation_docx(path)


def save_evaluation_file(성명, year, raw_bytes):
    """업로드된 파일을 저장하기 전에 실제로 docx로 열리는지만 검증한다(내부
    섹션 구조까지는 강제하지 않음 — 사용자 확정: "docx로 열리는지만" 검증).
    검증 실패 시 (False, 에러메시지), 성공 시 (True, 저장경로)."""
    import io
    import docx as docx_lib

    try:
        docx_lib.Document(io.BytesIO(raw_bytes))
    except Exception as e:  # noqa: BLE001 — 어떤 이유로 깨진 docx든 같은 방식으로 거절
        return False, f"올바른 .docx 파일이 아닙니다: {e}"

    _candidate_mod.EVAL_DIR.mkdir(parents=True, exist_ok=True)
    path = _candidate_mod.EVAL_DIR / f"{성명} 평가표({year}).docx"
    path.write_bytes(raw_bytes)
    return True, str(path)


def _infer_area_type(area_text):
    """S-06 제목 문구에서 27_공통_육성연계매핑.py가 기대하는 "우선순위영역"
    범주를 추정한다. 이 값은 27번 문서 자체가 명시하듯 어떤 점수·판정에도
    쓰이지 않는 참고 표시용이라(순수 매칭 힌트), 문구 기반 추정으로 충분하다."""
    if "자격" in area_text:
        return "자격"
    if "교육" in area_text:
        return "교육"
    if "AI" in area_text or "AX" in area_text:
        return "AX"
    return "정성(평가보완)"


def _build_yukseong_entry(candidate, report):
    b = candidate["기본정보"]
    추천행동 = [
        {
            "우선순위": i,
            "추천행동": it["title"],
            "우선순위영역": _infer_area_type(it["title"]),
            "기대결과물": it["artifact"],
        }
        for i, it in enumerate(report["s06_items"], start=1)
    ]
    return {
        "사번": b["사번"],
        "성명": b["성명"],
        "소속": b["소속팀"],
        "역할등급": b["역할등급_표기"],
        "리포트생성일": date.today().isoformat(),
        "추천행동": 추천행동,
    }


def _update_yukseong_sample(new_entries_by_사번):
    """기존 파일의 "대상자" 배열에서 이번에 처리한 사번만 교체하고, 나머지는
    그대로 둔다 — 아직 전 사원 대상 실행 전이라 손대지 않은 사번의 예시값을
    지우면 27번·hr_card가 그 사람에 대해 아무것도 보여줄 게 없어지기 때문이다."""
    if YUKSEONG_SAMPLE_FILE.exists():
        data = json.loads(YUKSEONG_SAMPLE_FILE.read_text(encoding="utf-8"))
    else:
        data = {"대상자": []}
    by_사번 = {p["사번"]: p for p in data.get("대상자", [])}
    by_사번.update(new_entries_by_사번)
    data["대상자"] = list(by_사번.values())
    data["_생성정보"] = {
        "생성기준": "growth_recommendation/07_결과연동.py — 실제 파이프라인 산출값(더 이상 수기 예시 아님)",
        "최종갱신일": date.today().isoformat(),
        "최종갱신_사번": sorted(new_entries_by_사번.keys()),
    }
    YUKSEONG_SAMPLE_FILE.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")


def _load_index():
    if INDEX_FILE.exists():
        return json.loads(INDEX_FILE.read_text(encoding="utf-8"))
    return {}


def _save_index(index):
    INDEX_FILE.parent.mkdir(parents=True, exist_ok=True)
    INDEX_FILE.write_text(json.dumps(index, ensure_ascii=False, indent=2), encoding="utf-8")


def get_report_history(사번):
    """hr_card "교육육성 리포트" 탭이 호출 — 이 사람이 지금까지 받은 리포트
    목록(연도별)을 최신순으로 돌려준다."""
    index = _load_index()
    return sorted(index.get(사번, []), key=lambda r: r["연도"], reverse=True)


def _find_index_entry(사번, year):
    index = _load_index()
    return next((r for r in index.get(사번, []) if r["연도"] == year), None)


def get_report_content(사번, year=2025):
    """육성추천 결과 화면 "개인별 세부" — PDF를 iframe으로 끼워 미리보기하던
    방식이 사내 PC 환경에서 PDF 뷰어가 막혀 있으면 스크린샷 같은 화면만 뜨고
    안 넘어가는 문제가 있어(2026-09-15, 사용자가 실제로 겪음), 인재추천 결과
    화면처럼 이름을 고르면 그 사람의 리포트 내용을 화면에 직접 렌더링하는
    방식으로 바꾼다. PDF와 내용이 어긋나지 않도록 06_PDF생성.
    generate_pdf_for_candidate가 PDF와 같이 저장해둔 to_pdf_data() 출력을
    그대로 읽어 돌려준다 — 별도 렌더링 스키마를 새로 만들지 않는다."""
    entry = _find_index_entry(사번, year)
    if entry is None:
        return None
    json_path = (OUTPUT_DIR / str(year) / entry["파일명"]).with_suffix(".json")
    if not json_path.exists():
        return None
    return json.loads(json_path.read_text(encoding="utf-8"))


def get_report_chat_context(사번):
    """하리(공용 챗봇)가 이 사람의 육성추천 리포트 실제 내용(S-01~S-07)을
    답변 근거로 쓸 수 있게 평문으로 만든다(2026-09-17, 사용자 요청 — "하리가
    전체 에이전트 질문에 답할 수 있어야 한다"). hr_card.build_employee_chat_context가
    이미 넣고 있는 "육성추천현황"(추천행동 vs 실제 이수 여부 대조 요약)과는
    다른 정보다 — 그건 "따라했는지" 확인용이고, 이건 "왜 그렇게 진단됐는지"
    리포트 원문 그 자체다. 가장 최근 연도 리포트만 쓴다(여러 해 리포트가 있어도
    과거 진단은 이미 갱신됐을 수 있어 최신 것만 근거로 삼는다). 아직 리포트가
    없으면 None(호출부가 이 블록만 생략)."""
    history = get_report_history(사번)
    if not history:
        return None
    data = get_report_content(사번, history[0]["연도"])
    if not data:
        return None
    lines = [f"=== [육성추천 리포트 — {history[0]['연도']}년 발행] ==="]
    if data.get("s00_notice"):
        lines.append(f"[{data['s00_notice']['title']}] " + " ".join(data["s00_notice"]["lines"]))
    lines.append("[한눈에 보는 핵심 요약]")
    for label, value in data["s02_rows"]:
        lines.append(f"- {label}: {value}")
    lines.append("\n[현재 상태 분석]")
    lines.extend(f"- {p}" for p in data["s03_paragraphs"])
    lines.append("\n[성장 포인트 — 확정된 보완 필요 항목]")
    for i, item in enumerate(data["s05_growth_points"], 1):
        lines.append(f"{i}. {item['area']} — {item['summary']}")
        lines.append(f"   현재상황: {item['situation']}")
        lines.append(f"   해결행동: {item['action']}")
        lines.append(f"   기대변화: {item['change']}")
    lines.append("\n[추천 행동]")
    for it in data["s06_actions"]:
        lines.append(f"- {it['title']}: {it['action']} (기대결과물: {it['artifact']})")
    lines.append("\n[담당자 확인 사항]")
    lines.extend(f"- {b}" for b in data["s07_bullets"])
    return "\n".join(lines)


def build_pdf_zip(사번_list, year=2025):
    """결과 화면 "전체 다운로드" — 이번에 생성에 성공한 사람들의 PDF를 zip
    하나로 묶어 돌려준다(2026-09-15, 사용자 요청 — 인재추천처럼 개별/전체
    다운로드를 다 지원). 생성 실패했거나 파일이 없는 사번은 조용히 건너뛴다
    (부분 성공을 막지 않기 위함 — 07_결과연동 run_batch와 같은 원칙)."""
    buf = io.BytesIO()
    with zipfile.ZipFile(buf, "w", zipfile.ZIP_DEFLATED) as zf:
        for 사번 in 사번_list:
            entry = _find_index_entry(사번, year)
            if entry is None:
                continue
            path = OUTPUT_DIR / str(year) / entry["파일명"]
            if path.exists():
                zf.write(path, arcname=entry["파일명"])
    return buf.getvalue()


# 한 명당 게이트웨이 호출이 ~20회 순차로 나가 리포트 생성이 느리다는 사용자
# 지적(2026-09-15)에 따라, 사람 단위로 동시에 몇 명까지 병렬 처리할지를 여기서
# 정한다. 병렬화해도 호출 "총횟수"는 그대로다 — 순서를 겹쳐서 벽시계 시간만
# 줄이는 것뿐이다. 다만 게이트웨이가 사내 자체 호스팅 서버라 동시접속 수만큼
# 부하가 걸리므로, 처음부터 전원 동시 실행하지 않고 3명으로 시작해 게이트웨이가
# 버티는지 본 뒤 필요하면 올리기로 사용자와 확정했다 — 이후 실제 배치 실행에서
# 게이트웨이가 3명 동시 부하를 잘 버티는 게 확인되어(2026-09-16) 7명으로 상향.
MAX_CONCURRENT_REPORTS = 7


def run_batch(사번_list, year=2025, judge=None, writer=None):
    """선택된 사번 전원에 대해 PDF 생성 + 인덱스 갱신 + 육성추천_결과_샘플.json
    갱신까지 한 번에 처리한다. 사람 단위로 성공/실패가 독립이라(한 명 실패가
    나머지를 막지 않음), 결과를 리스트로 모아 전부 반환한다.

    사람 단위 실행(_pdf_mod.generate_pdf_for_candidate 한 번 호출 = 그 사람의
    LLM 판정·서술 호출 전체)을 MAX_CONCURRENT_REPORTS명씩 동시에 돌린다
    (2026-09-15) — LLMJudge/ReportWriter 둘 다 인스턴스 상태를 갖지 않고
    매 호출마다 새 HTTP 요청만 보내는 순수 판정/서술 함수 모음이라 여러
    스레드가 같은 judge/writer 객체를 동시에 써도 안전하다. index/육성추천_
    결과_샘플.json 갱신은 각 스레드의 결과를 모은 뒤 메인 스레드에서만
    수행해 파일 쓰기 경합을 피한다.

    year는 "리포트를 생성한 달력 연도"가 아니라 "어느 해 평가표를 기준으로
    삼는지"다(01_후보자통합.build_candidate와 동일한 의미) — 0000_지침이 말하는
    "연초에 전년도 평가표로 리포트 발행" 흐름 그대로. 기본값 2025는 지금
    평가표 더미 데이터가 2025년분만 있어서다(date.today().year를 기본값으로
    쓰면 2026년엔 존재하지도 않는 "평가표(2026)" 파일을 찾다가 전원 평가표
    없음으로 처리되는 실제 버그가 있었다 — 실행해보고 발견해 수정)."""
    judge = judge or _judge_mod.LLMJudge()
    writer = writer if writer is not None else _writer_mod.ReportWriter()
    output_dir = OUTPUT_DIR / str(year)

    index = _load_index()
    yukseong_updates = {}
    results = []
    all_people_report_data = {}  # check_template_reuse용(2026-09-16) — 08번 STEP 2.5 "배치 실행" 절차
    job_titles = {}

    def _run_one(사번):
        try:
            return 사번, _pdf_mod.generate_pdf_for_candidate(사번, output_dir, judge=judge, writer=writer, year=year), None
        except Exception as e:  # noqa: BLE001 — 사람 단위 실패를 배치 전체 중단으로 번지게 하지 않는다
            return 사번, None, e

    with concurrent.futures.ThreadPoolExecutor(max_workers=MAX_CONCURRENT_REPORTS) as pool:
        outcomes = list(pool.map(_run_one, 사번_list))

    for 사번, outcome, error in outcomes:
        if error is not None:
            results.append({"사번": 사번, "성공": False, "사유": str(error)})
            continue
        path, result, candidate = outcome

        entry = {
            "연도": year,
            "생성일": date.today().isoformat(),
            "파일명": path.name,
            "성장포인트수": len(result["report"]["s05_items"]),
            "대체문구수": len(result["generation_notes"]),
        }
        index.setdefault(사번, [])
        index[사번] = [r for r in index[사번] if r["연도"] != year] + [entry]

        yukseong_updates[사번] = _build_yukseong_entry(candidate, result["report"])
        all_people_report_data[사번] = result["report_data"]
        job_titles[사번] = candidate["직무정보"]["직무명"]

        results.append({
            "사번": 사번, "성공": True, "성명": candidate["기본정보"]["성명"],
            "파일명": path.name, "성장포인트수": entry["성장포인트수"],
            "대체문구수": entry["대체문구수"],
            "AI서술_경고": "게이트웨이 미연결 또는 응답 실패로 일부 문구가 대체되었습니다 — 담당자 확인 필요" if entry["대체문구수"] else None,
            "템플릿중복_경고": None,
        })

    # 08_리포트생성지시서 STEP 2.5 "배치 실행" 절차 — 200명 전원 생성 후
    # check_template_reuse()를 반드시 실행해야 한다고 명시돼 있는데, 지금까지
    # 05_문장검증.py에 함수만 있고 실제로 호출하는 곳이 없었다(2026-09-16 발견).
    # 실패 메시지 형식("템플릿 재사용 의심: {사번1}·{사번2} — '...'")에서 사번
    # 두 개를 그대로 뽑아 해당 인원의 결과 항목에 경고를 붙인다.
    if len(all_people_report_data) > 1:
        for fail in _verify_mod.check_template_reuse(all_people_report_data, job_titles):
            head = fail.split(":", 1)[1].split("—", 1)[0].strip() if ":" in fail else ""
            flagged = [s.strip() for s in head.split("·") if s.strip() in all_people_report_data]
            for r in results:
                if r.get("성공") and r["사번"] in flagged:
                    r["템플릿중복_경고"] = "다른 대상자와 문장이 겹치는 것으로 의심됩니다 — 담당자 확인 필요"

    _save_index(index)
    if yukseong_updates:
        _update_yukseong_sample(yukseong_updates)
    return results


def main():
    print(f"전체 인원 {len(list_roster())}명 중 파일럿 {len(_candidate_mod._PILOT_사번)}명 실행")
    results = run_batch(_candidate_mod._PILOT_사번)
    for r in results:
        if r["성공"]:
            print(f"  OK {r['사번']} {r['성명']} -> {r['파일명']}")
        else:
            print(f"  FAIL {r['사번']} — {r['사유']}")


if __name__ == "__main__":
    main()

```


====================================================================================================
# 파일: growth_recommendation/__init__.py
====================================================================================================

```py

```


====================================================================================================
# 파일: growth_recommendation/service.py
====================================================================================================

```py
# -*- coding: utf-8 -*-
"""
growth_recommendation/service.py — server.py가 import하는 서비스 진입점
=========================================
01~07번 스크립트가 전부 파일명이 숫자로 시작해(01_후보자통합.py 등) 일반 import
문으로 불러올 수 없다 — 이 파일이 importlib로 07_결과연동.py(그 안에서 01~06번을
연쇄로 importlib 임포트한다)를 불러온 뒤, server.py·hr_card/service.py에는
평범한 함수 이름으로 다시 노출해주는 역할만 한다. hr_card/safety_guard 등 다른
에이전트 폴더의 service.py와 같은 자리다.
"""

import importlib
import sys
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
if str(BASE_DIR) not in sys.path:
    sys.path.insert(0, str(BASE_DIR))

_result_link = importlib.import_module("07_결과연동")

list_roster = _result_link.list_roster
resolve_name = _result_link.resolve_name
run_batch = _result_link.run_batch
get_report_history = _result_link.get_report_history
list_cert_reclassify_candidates = _result_link.list_cert_reclassify_candidates
confirm_cert_reclassify = _result_link.confirm_cert_reclassify
list_evaluation_years = _result_link.list_evaluation_years
get_evaluation_content = _result_link.get_evaluation_content
save_evaluation_file = _result_link.save_evaluation_file
get_report_content = _result_link.get_report_content
get_report_chat_context = _result_link.get_report_chat_context
build_pdf_zip = _result_link.build_pdf_zip
OUTPUT_DIR = _result_link.OUTPUT_DIR

```
